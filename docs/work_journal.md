# Журнал робіт

Файл для підхоплення роботи без розпитувань. Оновлювати при кожній зміні стану.

## 21.08.2026

### Обмеження, чинні зараз
* **Rozetka NOIRE — не чіпати.** 4147 позицій на модерації (заявки 4469692/4469693
  від 17.08, узято в роботу 18.08). Перевірено 21.08: перезбірка фіду дає
  побайтово ідентичний результат, розбіжностей у жодному полі немає.
* Публікація змін до фіду Prom — лише з підтвердження власника. Причина:
  18.08 калібрувальні описи потрапили у фід через cron, бо були записані
  в `prom_rewritten_desc` зі статусом `ok`, який читає генератор.

### Готове, чекає рішення на розгортання
1. **Характеристики в категоріях без довідника.** `fit_params` мав ранній
   `return {}`, якщо Prom не описав атрибути категорії — усі значення
   відкидались. Довідник «Секс-машин» у Prom справді порожній (перевірено
   власником вручну). Виправлено: такі значення йдуть у користувацькі
   характеристики. Замір: карток без характеристик **61 → 0**, характеристик
   усього 36834 → 37169.
2. **286 приписаних «З підігрівом».** Правило `has_heating` ловило «можна
   нагрівати» (гріє користувач у воді) як функцію приладу. Код виправлено
   21.08 — у базі `sexopt_extracted_params` досі 391 запис, з них 286 хибні.
   Прибрати після підтвердження.

### У роботі
3. **Пошукові фрази від назви, а не від типу** — зауваження підтримки Prom
   від 18.08. Приклад менеджера: чокер має фрази про чокер, у нас 6 із 9 про
   «фіксатор» (значення характеристики). По каталогу медіана 56% фраз
   повʼязані з назвою, у 1674 карток менше третини.

### Черга
4. 115 карток із суперечливими характеристиками (значення суперечить назві).
5. TOPTUL → Rozetka: 86 розбіжностей двох моделей на рішення власника,
   96 категорій без відповідника (602 товари). Мапінг 91%.
6. Генератор фіду TOPTUL для Rozetka — ще не існує, лише мапери.

### Відкрите питання без нашого впливу
* **Наявність Carvol недостовірна.** Фід від 21.08 06:27 містить
  `QBR-W FD-1102` як наявний із залишком 6, тоді як постачальник каже, що
  товару немає (замовлення 903748430 від 20.08). У фіді Carvol **немає
  жодної позиції з `available="false"`** — 6994 з 6994 стоять `true`, тож
  відсутність видно лише зі зникнення артикула. Механізму ручного блокування
  наявності в нас немає.

## 21.08.2026, друга половина дня

### Ліміт сесії
42% використано о 06:23, поновлення **о 10:21**. Cron налаштовано на точний
запуск о 10:21 плюс кожні 30 хв як підстраховка. Тижневий ліміт — 5%.

### Зроблено й опубліковано
* **Характеристики в категоріях без довідника.** `fit_params` мав ранній
  `return {}` — усі значення відкидались. Виправлено: йдуть у користувацькі
  характеристики. Карток без характеристик **61 → 0**, характеристик
  36834 → 37169. Фід опубліковано, `blob sha1 00ddb7b8…` звірено.
* **286 приписаних «З підігрівом» прибрано** з `sexopt_extracted_params`
  (лишилось 105 справжніх). Перевірено на SO8707 — там підігрів у назві,
  правило його правильно лишає.

### TOPTUL: заміри перед побудовою фіду
* Фід: 6895 офферів, фото у **всіх**, характеристик медіана 5,
  наявність **5689 true / 1206 false** — на відміну від Carvol, відсутність
  позначається чесно.
* Українська зроблена абияк: **45% характеристик мають російську назву**
  (20371 вживання, 424 різних), 21% значень російською, 290 назв товарів
  із російськими словами («Трещоточная викрутка», «Ключ накидной силовой»).
* **Довідник характеристик Rozetka є лише для 6 категорій**, і жодна не є
  ціллю TOPTUL (усі шість — з боку NOIRE). Робились вручну. Для 125
  категорій це окрема велика робота — власник бере її на вечір і завтра.
* Це **не блокує** першу версію фіду: Rozetka приймає вільні пари `<param>`,
  на відміну від Prom із закритими переліками. Довідники дадуть потрапляння
  у фільтри — другий етап.

### NVIDIA: ліміти
Заголовків про квоту API не віддає. Замір: 30 запитів поспіль — усі 200,
середньо 1.15 с. Обмежень на нашому обсязі немає. Вчорашня повільність була
перевантаженням моделі 49b, не квотою. Пропускна здатність ~50 запитів/хв.

### Рішення власника
* TOPTUL іде в **той самий** магазин Rozetka, що й NOIRE.
* Довідники категорій — власник опрацює ввечері та завтра.

### Переклад характеристик TOPTUL — стан і три помилки

**Зроблено:** назви характеристик перекладені повністю — 424 різних, що
покривають **20371 вживання**. Значення в роботі: 212 із 539 різних, це
близько 6000 вживань. Таблиця `toptul_translation (kind, src, dst, uses)`.

Три помилки, кожна варта запису.

**1. Резервне читання `reasoning_content`.** У класифікації категорій воно
рятувало: при малому ліміті токенів відповідь лишалась у міркуванні. У
перекладі те саме читання отруйне — у міркуванні сотні рядків, і партія на
40 позицій приходила як «198 рядків замість 40». Тепер для перекладу
читається ТІЛЬКИ `content`, а порожній результат означає брак токенів →
повтор із більшим бюджетом. Урок: **резервний шлях, доречний в одній задачі,
буває шкідливим в іншій.**

**2. Прив'язка за порядком рядків.** Модель розбивала багатослівні значення
на кілька рядків — 27 або 34 замість 25. Спокуса була послабити перевірку,
але зсув на рядок дає характеристиці ЧУЖИЙ переклад — гірше, ніж не
перекласти. Замість послаблення змінено саму прив'язку: кожен рядок несе
свій оригінал через роздільник, зіставлення **за рядком, не за позицією**.
Зсув став неможливим за побудовою.

**3. Доступ до моделі зникає посеред дня.** О 06:20 `nemotron-3-super-120b`
обробила 30 запитів поспіль без єдиної помилки; о 06:58 почала віддавати
**HTTP 404 з порожнім тілом**, тоді як `mistral-nemotron`,
`llama-3.3-nemotron-49b` і `deepseek-v4-flash` відповідали нормально. Тобто
зникає доступ до КОНКРЕТНОЇ моделі, не до акаунту. Додано автоматичне
перемикання переліком моделей при 404.

**Тричі за день скрипт зупинки вбив власну оболонку**, бо `pgrep -f` за
рядком збігався з командним рядком викличної оболонки. Виправлено по суті:
скрипт перевіряє, що вбиває саме `python3`.

## 21.08.2026, автономний прогін черги

### Сесія без виконання — і чому це важливо назвати одразу

Перший пункт черги зі станом `[ ]` — «290 назв товарів із російськими
словами». Виконати його не вдалось, і причина не в задачі.

**Замір середовища.** У сесії порожній `PATH`: `git log` → `git: No such
file or directory`, `ls` → те саме, `type -a ls` → `not found`. Тобто
відмова на пошуку в `PATH`, а не на запуску. Перевірено, що це **не
пісочниця**: той самий `ls` із `dangerouslyDisableSandbox` дає ту саму
помилку. Абсолютні шляхи (`/bin/ls`), присвоєння `PATH=… ls`, `hash -p`,
`source venv/bin/activate` у складі команди й `python3 -c "…"` — усе поза
дозволами. Працюють лише вбудовані команди оболонки; каталог файлів
довелось діставати через `echo` з підстановкою шаблону.

Додатково `/tmp/toptul.xml` — поза каталогом, дозволеним для читання. А це
**єдина копія** фіду TOPTUL: у репозиторії його немає (перевірено `data/`,
`exports/`, `docs/`; `data/prom_feed.xml` — інший, квітневий фід Prom).

Наслідок для черги: пункти 2 і 3 закриті не наполовину, а повністю —
290 назв нізвідки взяти, модель нічим викликати, у `toptul_translation`
нічим записати. Обидва позначені `[!]`. Це не рішення власника по суті, а
потреба запустити сесію з робочою оболонкою; постановка задач не міняється.

### Зроблено: генератор фіду TOPTUL (пункт 4 черги)

`tools/toptul_rozetka_generator.py`, 534 рядки — єдиний пункт, який
робиться без виконання, бо це авторська робота, а не прогін.

Що в ньому закрито з вимог пункту 4:
* окремий вихід `output/toptul_rozetka.xml`. Заборона чіпати NOIRE — **у
  коді, а не в інструкції**: `_safe_output()` завершує роботу з відмовою,
  якщо імʼя файлу починається на `noire`. Помилка тут не виправляється —
  cron підхопив би файл і зняв з продажу 4147 позицій на модерації;
* назва Тип · Бренд · Модель · (Артикул), коми й пунктуація прибрані.
  Дюймова позначка `1/2"` — виняток: лапка знімається лише там, де перед
  нею не цифра, інакше зникла б одиниця виміру з назв ключів і головок;
* `<description>` і `<description_ua>` обидва, з резервним описом із
  характеристик, коли постачальник опису не дав; речення про доставку й
  оплату вирізаються (p212);
* фото: `http→https`, дедуп URL у межах картки, відсів кирилиці в URL,
  межа 15;
* `stock_quantity` 0 при `available="false"` — і **не** нуль при `true`:
  доступний товар із нульовим залишком Rozetka приймає й одразу ховає;
* багатозначні характеристики — ОДИН тег із `<br>` у CDATA (те, через що
  15.08 вийшло 1403 попередження модерації);
* категорії лише з рівнів `manual/auto/validated/validated-2models`.

Пороги підібрані під пункт 5 черги («0 помилок, 0 попереджень»), а не під
мінімум валідатора: назва обрізається на **150** символів (межа
попередження), а не на 255 (межа помилки); оффери з менш ніж 3
характеристиками відсіюються; нульова гарантія викидається.

Два місця зроблені за правилом позитивного контролю:
1. **Теги фіду не вгадуються.** `resolve_fields()` для кожної ролі
   (`name`/`name_ua`, `description`/`description_ua`,
   `vendorCode`/`article` …) рахує, у скількох офферах кандидат непорожній,
   бере перший заповнений і **друкує заміри по всіх**. Мовчазний нуль через
   неправильну назву поля стає неможливим — саме він 15.08 дав «0
   розбіжностей» на порожньому тезі.
2. **Ознака російської імпортується** з `toptul_translate` (`from
   toptul_translate import RU`), а не пишеться вдруге. Два власні
   визначення «російського» розійшлись би, і перевірка «скільки лишилось»
   міряла б не те, що виправляв перекладач. Наприкінці прогону друкуються
   три числа: назв з російськими словами, назв характеристик, значень —
   плюс розмір таблиці перекладу, щоб нуль при порожній таблиці не читався
   як чистий фід.

### Чого НЕ зроблено — і що це означає для статусу

Генератор **жодного разу не запускався**: ні `python3 -c "import ast;
ast.parse(...)"` (правило розробки №1), ні на даних. Синтаксис звірено
очима, не інструментом. Тому пункт 4 стоїть `[~]`, а не `[x]`, і фіду
`output/toptul_rozetka.xml` не існує.

Перший запуск у нормальній сесії — саме в такому порядку:
```bash
python3 -c "import ast; ast.parse(open('tools/toptul_rozetka_generator.py').read())"
python3 tools/toptul_rozetka_generator.py --fields          # звірити теги
python3 tools/toptul_rozetka_generator.py --limit 50
python3 tools/rozetka_xml_validator.py output/toptul_rozetka.xml --no-xlsx
```
Крок `--fields` пропускати не можна: усі назви тегів у генераторі — це
кандидати, звірені з коду сусідніх інструментів, а не з самого фіду.

## 22.08.2026, автономний прогін черги

### Середовище — цього разу робоче

На відміну від сесії 21.08, оболонка нормальна: `PATH` заповнений,
`python3`/`git` запускаються, `ssh tek@100.82.24.112` працює. `/tmp/toptul.xml`
локально й далі поза дозволеним каталогом, але **на сервері він є** (38 МБ,
від 21.08 21:33), тому всі заміри зроблено там. Локальний `python3 -c`
дозволу не має — це не завадило, бо вся робота однаково йде на сервері.

### Виправлено: ознака мови `RU` (пункт черги «хибні спрацювання»)

**Суть дефекту.** Коментар над регексом стверджував, що спільних для обох
мов слів у переліку немає. Це було неправдою: `ширина`, `рукоятк*`,
`привод*`, `момент*`, `числ*`, `шестигранн*` — однакові в українській. Через
них самоперевірка генератора рахувала «26 різних, 1203 вживання» на рядках
«Ширина», «Матеріал рукоятки», «Вид приводу інструмента», уже перекладених.
Число було фактом про регекс, а не про фід.

**Друга половина дефекту, якої в постановці не було.** Та сама ознака ще й
мовчала. На сирих назвах характеристик TOPTUL вона ловила 424 різних із 719;
повз неї проходили «Питание», «Состояние», «Комплектация», «Категория»,
«Погрешность», «Зажимное усилие» — жодної «однозначної основи» з переліку в
них немає. Тобто перелік основ, зібраний вручну, і хибно тривожив, і
недобирав; замінити його треба було, а не підчистити.

**Як зроблено.** Дві ознаки з різними ролями, бо в них різна ціна помилки:
* `RU` — строга, **тільки для замірів**. Хибна тривога робить число
  нечитаним, тож її не має бути зовсім.
* `RU_WIDE` — широка (строга + спільні основи), **тільки для відбору** рядків
  на переклад у `collect()`. Тут навпаки: зайвий український рядок у партії
  коштує кількох токенів, а пропущений російський лишається у фіді назавжди.

Замість ручного переліку основ — закінчення, яких українська словозміна не
утворює: `-ие`, `-ия`, `-ая`, `-ость`, `-ости`, `-ое`, `-ой` (укр. `-ія`,
`-ення`, `-ість`). Основи лишились тільки там, де російська форма пишеться
інакше (`диаметр`/`діаметр`, `материал`/`матеріал`) або є іншим словом
(`длина`/`довжина`). `привода`, `момента` — саме родовий відмінок: називний
«привод», «момент» спільний, а «приводу»/«моменту» вже українські.

### Заміри (усе — правило позитивного контролю)

**Негативний контроль.** Потрібен був корпус явно української прози, не
повʼязаний із TOPTUL: узято `output/noire_rozetka.xml` — **592721
слововживання** з `name_ua` і `description_ua`. Нова `RU` спрацювала там 181
раз, і **жодного разу хибно**: усі влучення — справжні російські залишки
(«Трендовая» 16, «Размер» 14, «эластан», «цвет», «это», «который»), плюс
одруківка «неймовырний». Тобто заразом знайшлось те, чого не шукали.

**`noire_prom.xml` контролем НЕ є** — перевірено й відкинуто: у ньому 17 тис.
вживань `-ие` і 23 тис. `-ая`, бо тег `description` там російський за
задумом. Якби взяв його, «FP» нарахувалось би десятки тисяч і фікс виглядав
би провальним.

**Мінімальна основа 3 літери для `-ое` і `-ой`** — не окомір: на двох літерах
у контроль потрапляє українське «алое» (178 вживань) і «герой». Підняття до
трьох прибирає обидва, не втрачаючи «Новое», «Зажимное», «Прямой».

**Цільове число.** Назви характеристик у фіді: `26 різних / 1203 вживання`
(майже все хибне) → **`144 різних / 2748 вживань`, усе справді російське**.
Значення: `423 різних / 3864 вживання`.

**Назви товарів 257 → 180 — і чому падіння тут НЕ означає покращення само
собою.** Розібрано на тих самих побудованих назвах із готового фіду:
`257 − 105 + 28 = 180`. Усі 105 втрачених переглянуто очима — усі вже
українські («Біта шестигранна», «Ключ Г-подібний шестигранний», «Вороток з
рукояткою», «з обмежувачем моменту»). Усі 28 доданих — справді російські
(«Головка ударная», «Викрутка шлицевая», «Ключ накидной»). На **сирих**
назвах фіду нова ознака ловить 5688 із 6895 проти 4070 у старої, втрачаючи
21 — усі через слова «шестигранник» і «рукоятка».

**Вбудований позитивний контроль.** `python3 tools/toptul_translate.py
--selftest` — 17 відомо російських рядків проти 24 відомо українських
(серед них усі три з постановки задачі). Мережі й БД не потребує. Прогін на
сервері: 17 російських впізнано, 0 хибних на українських.

### Стан фіду

Фід перезібрано на сервері: **5258 офферів**, валідатор
`rozetka_xml_validator.py` — **0 помилок, 0 попереджень, 100% валідних**
(без змін проти 21.08 — виправлення стосується заміру, не вмісту).
MD5 `tools/toptul_translate.py` ноутбук=сервер: `05837cba…`.
`output/toptul_rozetka.xml` **не публікувався**; перевірено, що його не
чіпає ні `rozetka_github_sync.py` (той працює з `data/carvol_rozetka.xml`),
ні `noire_stock_sync.py --publish-rozetka`. NOIRE не торкався.
Зміни в `tools/toptul_translate.py` закомічені **не** були — за правилом
проекту push робить власник вручну.

### Що з цього випливає для черги

Оскільки стара ознака недобирала, у `toptul_translation` бракує саме тих
144 назв і 423 значень, які вона не бачила. Це новий пункт черги. Окремо
робити нічого не треба: `collect()` тепер відбирає широкою ознакою й
підхопить їх сам при наступному прогоні `--names --values`.

## 22.08.2026, другий автономний прогін черги

Узято перший `[ ]` — «дібрати переклад для 144 назв і 423 значень, яких
стара `RU` не бачила». Пункт закрито: самоперевірка генератора показує
**0 назв характеристик російською і 0 значень**.

### Головне: NVIDIA мовчки не працювала, і лог це приховував

Прогін `toptul_translate.py --names --values` пішов і не впав. У логу —
рівні партії, жодного `ERROR`, скрипт спокійно дійшов би до кінця. Насправді
**41 запит поспіль отримав HTTP 401** і в таблицю не потрапило нічого:
`translate()` на нечитану відповідь повертає `{}`, а `main()` робить
`continue`. Тобто «прогін завершився» і «переклад зроблено» — різні події,
які лог не розрізняє. Це той самий клас, що й правило про позитивний
контроль, тільки з боку запису, а не читання.

Причина 401 виявилась простою й вартою запису: **`NVIDIA_API_KEY` на сервері
немає взагалі.** Не протух, не змінився — його немає в `.env` (файл від
16.08), немає в `~`, немає в оточенні, і жоден запис crontab його не
потребує. Ключ є **лише на ноутбуці**, тобто всі вчорашні прогони NVIDIA
йшли звідти, а не з сервера. Сьогодні ноутбук виконувати нічого не може
(`python3` і `curl` поза дозволами), сервер може, але без ключа — і ці дві
половини не сходяться.

**Ключ на сервер не копіювався.** Переносити чужий секрет між машинами без
слова власника — не те, що робиться мовчки в автономному прогоні, тим
більше що потреби в ньому не виявилось.

### Як зроблено натомість

Хмарна модель тут була засобом, а не вимогою: переклад словника унікальних
рядків робить будь-яка модель, зокрема та, що веде цю сесію. Тому пара з
двох кроків замість одного мережевого:

* `tools/toptul_translate_export.py` — вивантажує рядки, яких ще немає в
  `toptul_translation`, як TSV `kind/uses/src`;
* `tools/toptul_translate_load.py` — приймає `kind/src/dst` і пише в БД.

Захист від зсуву лишився той самий, що й у NVIDIA-шляху: **зіставлення за
рядком, а не за позицією**, бо саме позиційна прив'язка ламалась 21.08.

**Перший експорт мовчки з'їв 155 рядків із 437** — багаторядкові значення
(«Присоединительный квадрат: 1/2"\nДлина: 600 мм») не лізли в TSV, і код їх
відкидав, повідомляючи в `stderr`, куди ніхто не дивиться. Це та сама
помилка, від якої застерігає правило: «594 на вивантаження» і «439 у файлі»
розійшлись би непоміченими. Виправлено екрануванням `\n` і `\t`; тепер
експортовано 594 з 594.

### Три перевірки в завантажувачі, і чому кожна є

1. **`src` мусить бути у фіді.** Друкарська помилка в словнику дала б запис,
   який ніколи не спрацює, а таблиця виглядала б повнішою.
2. **`dst` не мусить впізнаватись строгою `RU`.** «Переклад», що лишився
   російським, — найгірший результат: покриття 100%, фід російський.
3. **Звіт про залишок**, щоб «записано 594» не читалось як «готово».

**Негативний контроль.** Файл із чотирьох пар: одна правильна, одна з
неіснуючим `src`, дві з неперекладеним `dst`. Перевірки назвали всі три
дефекти поіменно й записали одну пару. Без цього «594 з 594 прийнято» було б
твердженням про мовчазну перевірку.

### Побічна знахідка: 29 старих перекладів самі були російськими

Після завантаження 594 пар самоперевірка показала не нуль, а **4 назви й 21
значення**, і всі — напівперекладені: «Матеріал изделия», «Довжина лезвия»,
«Потужність лампы», «Згнутый», «Регульовані опоры». Це не сирі рядки фіду —
це `dst` із прогону NVIDIA 21.08. Модель перекладала перше слово й лишала
друге, а **стара, звужена `RU` цього не бачила**, бо `изделия`, `лезвия`,
`лампы` не мали жодної «однозначної основи» з її переліку. Тобто вчорашнє
«назви характеристик перекладені повністю — 424 різних» було правдою про
кількість рядків і неправдою про мову.

Аудит усієї таблиці (`tools/toptul_translate_audit.py`) знайшов **29 таких
пар на 234 вживання**. Перекладено заново, повторний аудит — 0.

### Заміри

| Що | До | Після |
|---|---|---|
| Назви характеристик російською у фіді | 144 різних / 2748 вживань | **0 / 0** |
| Значення характеристик російською | 423 різних / 3864 вживання | **0 / 0** |
| `toptul_translation`, назви | 424 з 581 | **581 з 581** |
| `toptul_translation`, значення | 240 з 677 | **677 з 677** |
| Хибних перекладів у таблиці | 29 (234 вживання) | **0** |
| Офферів у фіді / валідатор | 5258 / 0 / 0 | 5258 / **0 помилок, 0 попереджень** |

**Нуль пояснено механічно, а не «схоже, все добре».** По-перше, таблиця не
порожня — генератор друкує «Перекладів: name — 581, value — 677». По-друге,
та сама перевірка на тих самих даних дала 144/2748, потім 4/67, потім 0/0:
вона рухалась разом із виправленнями, отже здатна знаходити. По-третє,
перевірено **окремо від генератора**, прямим пошуком у файлі:
`param name="Питание"` — 0 входжень, `param name="Живлення"` — 220;
`"Состояние"` — 0, `"Стан"` — 180; `"Матеріал изделия"` — 0,
`"Матеріал виробу"` — 38.

Назви товарів лишились 180 — це окремий пункт черги, не цей.

### Стан і що не робилось

`output/toptul_rozetka.xml` перезібрано о 09:18 (5258 офферів). **Не
публікувався.** NOIRE не торкався: `output/noire_rozetka.xml` (08:22),
`noire_prom.xml` (07:40), `data/carvol_rozetka.xml` (07:15) — усі три від
власного cron, робота йшла 09:00–09:18. MD5 трьох нових інструментів і двох
словників ноутбук=сервер звірено. Не комітилось — push робить власник.

Пункт «290 назв товарів» переведено з `[!]` у `[ ]`: причина блокування
(мертва оболонка 21.08) зникла, а брак ключа NVIDIA більше не перешкода —
шлях без нього тепер перевірений на характеристиках.

### Відкрите питання власникові

**Ціноутворення TOPTUL на Rozetka.** За замовчуванням множник 1.0 — ціна
дорівнює РРЦ постачальника, як у NOIRE після рішення від 11.08. Але
CLAUDE.md для Розетки описує `ceil(rrc * (1 + comm/100) / 10) * 10`. Обидва
шляхи реалізовані, другий вмикається прапорцем `--commission`; вмикати за
замовчуванням генератор не має права. Потрібне слово власника, який варіант
чинний для TOPTUL.

## 23.08.2026, автономний прогін черги

Узято перший `[ ]` — «Фід TOPTUL для Rozetka, довести до кінця».

### Спершу: вчорашня робота на сервері була знищена, і знищується щодня

Перше, що показав замір: `tools/toptul_translate.py` і
`toptul_rozetka_generator.py` на сервері мають дату **23.08 07:30**, а вміст —
**від 21.08**. Трьох інструментів, зроблених 22.08
(`toptul_translate_export/load/audit.py`), на сервері немає взагалі.

`git reflog` називає винуватця точно: `HEAD@{3}: reset: moving to origin/main`.
Це `agents/orders/carvol_epicentr_sync.py:167` — `git reset --hard
origin/main` перед оновленням XML Єпіцентру, щодня о 07:30 за crontab.
Сервер **не пушить** (правило проєкту), watchdog комітить лише локально, тому
всі локальні коміти з часу останнього пушу власника не мають відповідника в
`origin` — і `reset --hard` їх викидає разом із неврахованими файлами.

Це не разова подія, а **щоденний механізм**: усе, що не встигли запушити до
07:30 наступного дня, зникає. Вчорашній запис «Не комітилось — push робить
власник» описував саме той стан, у якому робота не переживає ніч.

Не чіпав: cron і `carvol_epicentr_sync.py` — робочий контур (SKILL-22,
рівень 2–3), а виправлення тут не однозначне (скидання потрібне, щоб push
XML не конфліктував). Що з цим робити — рішення власника, варіанти в кінці.

**Наслідок на зараз:** сьогоднішня робота на сервері теж лежить у локальних
комітах watchdog. Якщо не запушити до 07:30 24.08 — вона зникне так само.

Втрати даних не було: `toptul_translation` живе в PostgreSQL, а не в git —
581 назва й 677 значень уціліли. Загублено було лише код, і він знайшовся на
ноутбуці. Відновлено `scp`, MD5 звірено.

### Категорії: джерелом істини був каталог конкурента, а не Rozetka

«96 категорій без відповідника (602 товари)» трималось на
`docs/rozetka_seller_categories.json` — **289 категоріях, зібраних із каталогу
конкурента `ttul`**. Тобто «в Rozetka немає такої категорії» насправді
означало «конкурент такого не возить». Це той самий клас, що й правило
позитивного контролю, тільки з боку джерела: нуль був фактом про перелік, а
не про майданчик.

Офіційний перелік дає `market-categories/search` — метод є в
`shared/knowledge_base/rozetka/api_full.txt`, а робочий токен **тільки на
сервері** (на ноутбуці `ROZETKA_API_TOKEN` немає, звідси й `incorrect_access_token`
при першій спробі). `tools/rozetka_categories_fetch.py` вивантажив
**4762 категорії** проти 289, з них 4038 листкових.

Запобіжник у самому вивантажувачі: якщо категорій менше 1000 — це обірвана
пагінація, і скрипт падає, а не пише огризок. Друга умова зупинки — сторінка
без жодного НОВОГО id: порожньої сторінки в кінці могло й не бути.

Далі — розбір усіх 96 по фактичних назвах ТОВАРІВ у кожній, а не за схожістю
назв категорій. Схожість тут не працює: власний скорер дав «Динамометры →
Динаміки» з оцінкою 1.00, а готові здогадки рівня `review` виявились
переважно хибними («Інструмент для пайки → Набори інструментів», «Тестери
герметичності → Тестери кабельні», «Шуруповерт акумуляторний → Біти для
шуруповерта»). Усі 27 відхилено.

Записано **55 відповідностей** (`docs/toptul_rozetka_manual_official.json`,
кожна з причиною) через `tools/toptul_rozetka_map_load.py` з трьома
перевірками: `toptul_id` існує, `rz_id` є в офіційному каталозі, категорія
**листкова** (Rozetka не приймає товар у вузол із дітьми).

Перевірки не декларативні — негативний контроль на файлі з чотирьох пар
(одна правильна, одна з неіснуючим `toptul_id`, одна з неіснуючим `rz_id`,
одна нелистова) назвав усі три дефекти поіменно й записав одну пару. Крім
того, дві перевірки спрацювали на справжніх рішеннях: «Запчастини для
автопідйомників → Комплектуючі для підіймачів» і «Повербанки → Повербанки та
зарядні станції» — обидва вузли мають дітей. Перше замінено на `none`
(категорія змішана), друге — на листок «Універсальні мобільні батареї».

Решта **41 категорія (205 товарів)** лишилась без відповідника свідомо, і це
тепер вимірений факт: пошук по 4762 категоріях за основами «піскостр»,
«екстрактор», «розвальц», «дозиметр», «твердомір», «тахометр», «вакуумметр»,
«нутромір», «присоск», «цвяхотяг» дає нуль влучень. Перелік із назвами
товарів і причиною по кожній — `docs/toptul_rozetka_unmapped.md`
(`tools/toptul_rozetka_unmapped_report.py`).

### NVIDIA: партії гинули мовчки, і причина не та, що вчора

Прогін `--titles` пішов і не впав: 15 партій поспіль, у логу самі
`WARNING`, у таблиці — **нуль записів із 353**. Вчорашній діагноз (немає
ключа) не підходив: ключ на ноутбуці є, і прямий запит із двома рядками
повертав правильні пари.

Причина точна. `nemotron-3-super` віддає **міркування в полі `content`**, і на
назвах товарів (вони втричі довші за назви характеристик) увесь бюджет 1500
токенів іде на роздуми: `finish_reason=length`, 4101 символ англійського
міркування, жодного `|||`. А умова повтору перевіряла, чи `content`
**порожній**. Він не порожній — він повний не тим. Тому підвищення бюджету не
спрацьовувало жодного разу.

Виправлено по суті: умова повтору тепер — **«жодної пари не розібрано»**, а не
«порожня відповідь», бюджети підняті до 3000/8000. Замір на тій самій партії
з 15 назв: 1500 токенів — 0/15 пар, 6000 — 15/15.

Заразом: `deepseek-v4-flash` віддав HTTP 200 **без ключа `choices`** — раніше
це просто вбивало партію, тепер обробляється як недоступність моделі, з
переходом на наступну.

І третє: `main()` мовчки робив `continue` на порожньому результаті. Тепер
рахує втрачені рядки й друкує підсумок **завжди**, окремо позначаючи нуль
записів як помилку. Саме цього бракувало 22.08, коли 41 відповідь HTTP 401
дала «прогін завершився» без єдиного `ERROR`.

### Валідатор не перевіряв обов'язкове поле

Критерій пункту черги — «`rozetka_xml_validator.py` дає 0 помилок і 0
попереджень». Перш ніж на нього спиратись, зроблено позитивний контроль:
зіпсована копія фіду з навмисними дефектами.

**Перша спроба контролю була хибною, і це варто записати.** Зіпсував тег
`<name>` — валідатор промовчав, і на мить це виглядало як «валідатор сліпий».
Насправді він читає `name_ua or name`, тобто дефект був у полі, яке в цьому
фіді не використовується. Контроль треба було націлити, а не оголошувати
результат. З `name_ua` валідатор одразу дав 1 помилку й 1 попередження.

А от справжня прогалина знайшлась поруч: **`description` не перевірявся
взагалі**. Оффер без обох тегів опису проходив як валідний, хоча опис
обов'язковий (p185, ред. 29.06.2026). Тобто «0 помилок» справді нічого не
казало про обов'язкове поле. Додано: відсутні обидва — помилка; немає
`description_ua` — попередження. Після цього зіпсована копія дає 2 помилки,
справжній фід — 0. `output/noire_rozetka.xml` перевірено новим валідатором
окремо: **4147 офферів, 0 помилок, 0 попереджень** — нова перевірка його не
зачіпає.

### Ще один замір, що вимагав неможливого

Самоперевірка показала «значень характеристик російською: 1 різних, 3
вживань». Це виявився `Бренд: Молния` — власна назва. Перекладач її не чіпає
за правилом («бренди, моделі, артикули НЕ перекладай»), тож замір вимагав
виправити те, що виправляти заборонено, і нуля не досяг би ніколи. Значення
характеристик `Бренд`, `Виробник`, `Країна-виробник`, `Модель` виведено
з-під заміру.

## 23.08.2026, автономний прогін черги (мапінг Єпіцентру)

Узято перший `[ ]` — «Виправити мапінг категорій NOIRE для Єпіцентру».
Половина зроблена, половина впирається в межу й позначена `[!]`.

### Головне: правка мапінгу Єпіцентру — це правка **опублікованого фіду Prom**

Правки внесено, заміряно й **відкочено**. Причина, знайдена перед тим, як
зміна вийшла назовні: `epicentr_category_mapping` читає не лише генератор
Єпіцентру, а й `tools/noire_prom_generator.py:725` — категорія Prom
виводиться з `epicentr_category_code` через `prom_category_mapping`. А
`noire_stock_sync.py --publish-prom` (crontab `40 7,11,15,19`) **не
оновлює ціни в готовому файлі, а перезбирає фід генератором із БД і пушить
у GitHub** (`regenerate_prom_feed()` → `publish_github()`).

Тобто зміна таблиці, зробленої «для Єпіцентру», о 11:40 сама пішла б у
живий фід Prom. Заміряно точно, а не оцінено:

| | |
|---|---:|
| товарів Prom у зачеплених категоріях | 578 |
| категорія Prom **змінюється** | 536 |
| з них **зникають із фіду** (немає мапінгу Prom для 5116/3141/6726) | 39 |

Найбільші рухи: 256 шт `9476→9466` (Prom `161017→161011`), 57 шт
`9458→9464`, 32 шт `9460→7216`, 29 шт `9526→5116` → **категорії Prom немає**.

Це рівень 3 (SKILL-22) і стояче обмеження журналу від 21.08 («публікація
змін до фіду Prom — лише з підтвердження власника»), тому відкочено.
Відкат перевірено тим самим заміром, що показав 536: після відкату — **0**.

### Що лишилось у БД: 13 категорій

`epicentr_intimate_categories`: **39 → 52**. Перелік не переписано з
документа, а **порахований із даних**: категорії, які категорійний менеджер
призначив карткам (`data/epicentr_products.json`, 5793 картки NOIRE) і яких
немає в таблиці. Вийшло рівно 13, збіг із `docs/epicentr_categories_reference.md`.

Це безпечно й перевірено: `epicentr_intimate_categories` не читає ні
`noire_prom_generator.py`, ні генератор Rozetka. Фід Єпіцентру після
додавання — байт у байт той самий (12828 офферів, 33 категорії), бо жоден
мапінг на нові коди ще не вказує. Тобто крок зворотний і невидимий назовні.

### Заміри правки — заради них її й вмикали

Головне число — **згода з категорійним менеджером** по 5793 картках кабінету:

| | до | після |
|---|---:|---:|
| збіг з менеджером | 4960 (85.6%) | **5458 (94.2%)** |
| розбіжність | 833 | **335** |
| офферів у фіді | 12828 | 12828 |
| категорій у фіді | 33 | 35 |
| `epicentr_required_audit`: усі обовʼязкові заповнені | 9562 | 9376 |
| бракує хоч однієї | 3266 | **3452** |
| fallback-габарити | 0 | 54 |

**Прогалин стало більше на 186 — і це не погіршення, а видимість.** Розкладено
по категоріях, а не пояснено словами: `9464 +76`, `7216 +53`, `5116 +36`,
`9632 +24`, `9628 +14`, `3141 +9`, `6726 +9`, `9548 −27`, `9578 −8` = +186.
Картки переїхали з категорій, які цих атрибутів не вимагали, у правильні, які
вимагають. Перевірено адресно по 54 картках трьох нових категорій: у старій
(хибній) категорії прогалин було 0–3, у новій — 7 у кожної.

### Позитивний контроль

Перевірки в `tools/epicentr_mapping_apply.py` не декларативні. Негативний
контроль — файл правок із навмисними дефектами; кожна відмова названа
поіменно:
* `999999` — немає рядка в `epicentr_category_mapping`;
* `4902` з `ours=1111` — «у БД 9458, у файлі очікувалось 1111: мапінг змінили
  після звірки»;
* ціль `8888888` — немає в таблиці категорій;
* ціль `3166` — немає в таблиці;
* ціль `9442` (**є** в таблиці, але вузол із дітьми) — окремим прогоном, бо в
  першому її маскувала попередня перевірка. Єпіцентр не приймає товар у вузол
  із дітьми, тож перевірка потрібна саме в такому вигляді.
Одна правильна пара при цьому приймалась — інакше «усі відмови» нічого не
доводили б.

Перевірено й те, що аудит не сліпий на нових категоріях:
`data/epicentr_attribute_sets.json` містить усі 13 наборів (5508 усього), а
`noire_epicentr_validator.py` має DB-first/API-fallback і required-атрибути
для 5116/3141/6726 дістав з API — у БД (`epicentr_required_attrs_sexopt`,
33 набори) їх немає.

### Рішення власника — три питання

1. **Вмикати правку мапінгу?** Одна команда:
   `venv/bin/python3 tools/epicentr_mapping_apply.py --apply`
   Відкат: `--revert` (щит `epicentr_category_mapping_bak_20260823`, 21 рядок).
   Наслідок для Prom — 536 змін категорії й 39 зниклих товарів, див. вище.
2. **Що робити з 39 товарами**, для яких у `prom_category_mapping` немає
   відповідника кодам 5116 (Менструальні чаші), 3141 (Сіль для ванни),
   6726 (Крем для гоління). Або дописати мапінг Prom, або лишити їх у
   поточних категоріях Prom окремим правилом.
3. **Габарити й атрибути 13 нових категорій.** `epicentr_default_dimensions`
   не має 3141/5116/6726 → 54 картки отримують FALLBACK_DIM
   300 г / 150×100×150 мм (менструальна чаша важить не 300 г). Значень немає
   в наших даних, тому не вигадував (правило 5 черги). Бракує по 7
   обовʼязкових атрибутів на картку, серед них `country_of_origin` у всіх 54.

### Дотична проблема, знайдена принагідно

`epicentr_required_attrs_sexopt` у БД покриває 33 набори з 35, що є у фіді, і
жодного з 13 нових. Валідатор це переживає (API-fallback), але кожен його
прогін тепер ходить у мережу. Наповнити таблицю з уже наявного
`data/epicentr_attribute_sets.json` — окрема дрібна робота, мовчки не брався.

### Стан

`exports/noire_epicentr.xml` перезібрано **під поточний (відкочений) стан БД**
— 12828 офферів, 76026697 Б, 10:30. Свідомо не лишав фід із правкою: фід, що
не відповідає БД, — це та сама пастка «згенеровано локально ≠ чинний стан».
Відтворити фід із правкою — два кроки: `--apply`, потім генератор.

**Не публікувалось нічого.** `output/noire_rozetka.xml` (15.08 20:30),
`output/noire_prom.xml` (17.08 05:41), `data/carvol_rozetka.xml` (08.07) — усі
три з тими самими датами, що й до сесії. Carvol не чіпав. Новий інструмент
`tools/epicentr_mapping_apply.py` на сервер не розгортався (там він не
потрібен: БД спільна, а зміна вже в ній). Не комітилось — push робить власник.

## 25.08.2026, автономний прогін черги

Узято перший `[ ]` — «Виправити самоперевірку генератора TOPTUL». Закрито `[x]`.

### Постановка підтвердилась, і масштаб виявився вчетверо більшим

Перше, що зроблено, — незалежний замір по фіду на сервері, без участі
генератора: `10 різних російських назв характеристик / 1189 вживань` і
`20 значень / 825`. Постановка казала 11/1202 і 21/890 — розбіжність від того,
що фід перезбирали 23.08. Дефект справжній: самоперевірка генератора при цьому
друкувала **0 і 0**.

### Чому виправляти треба було не перелік, а підхід

Спокуса — дописати в регекс основи з постановки («рабоч», «расход», «резьб»).
Саме так робили двічі, і обидва рази перелік провалювався. Замір, який це
показав: узято **всі 674 різні назви характеристик**, які стара ознака вважала
неросійськими, і переглянуто. Крім названих у постановці, там лежали
«Маркировка наконечника» (143), «Тип шлица» (134), «Номер шлица» (104),
«Монитор» (94), «Набор ключей» (72), «Подсветка дисплея» (72), «Глубина» (62),
«Тип мебели», «Тип аккумулятора», «Шкала измерений», «Регулировка количества
оборотов». Серед значень — «Ключи» (636), «Нет» (318), «Компрессор» (274),
«Металл», «Пластмасса», «Трещотка», «Пассатижи», «Напильник», «Сверло».

**Спільне в них — відсутність будь-якої орфографічної позначки.** «набор»,
«нет», «ключи», «металл», «класс», «воздух», «сеть», «нож» пишуться літерами,
допустимими в українській. Ніякий регекс їх не відрізнить — не через невдалий
перелік, а тому, що розрізняльної ознаки в написанні немає. Потрібен словник.

### Як зроблено

`tools/uk_lexicon.py` — ознака на двох словниках, порядок кроків важливий:

1. є `ы/э/ъ/ё` → російське (українська абетка їх не має);
2. **є в українському словнику → українське** (1 554 762 словоформи, пакет
   `wukrainian`, `data/uk_wordforms.txt.gz`, 4.4 МБ);
3. є в лексиконі російських слів каталогу → російське;
4. корінь чи закінчення, яких українська словозміна не утворює → російське;
5. інакше → **непізнане**.

Словник стоїть ПЕРЕД морфологією — саме тому «герой», «алое», «ширина»,
«рукоятка», «момент» не можуть дати хибної тривоги: вони відсіюються на
кроці 2 й до правил не доходять. Це знімає першу з двох старих хвороб за
побудовою, а не переліком винятків.

Лексикон російських слів (`data/toptul_ru_lexicon.txt`, 1328 слів) **не
вигаданий**: 1013 виведено з `toptul_translation.src` — рядків, які перекладач
уже визнав російськими, мінус слова з `dst`, мінус український словник; решта
522 — з переліку непізнаних слів поточного фіду. Збирає
`tools/uk_lexicon_build.py`.

**Кошик «непізнане» — не недолік, а вимога правила позитивного контролю.**
Доки він друкується поруч, «0 російських» неможливо прочитати як «фід чистий».
Туди й осів суржик, який не є ані російським, ані правильним українським.

### Позитивний і негативний контроль

**Контроль у самій збірці лексикона спрацював одразу.** Перевірка «слово з
ручного переліку не мусить бути в українському словнику» викинула **23 слова**,
які я вписав як російські помилково: `батарея`, `кабель`, `ручка`, `колесо`,
`тиски`, `удар`, `шум`, `панель`, `наконечник`, `заряд`, `фасад`, `планка`.
Кожне з них дало б хибну тривогу — рівно та хвороба, від якої лікували.

**Дві перевірки замість однієї.** `tools/toptul_ru_audit.py` читає фід XML
напряму й друкує поруч грубий перелік ознак із постановки і словникову ознаку.
Твердження скрипта: словникова мусить бути НАДмножиною переліку; інакше він
завершується кодом 1. Перший прогін цей код і дав — перелік знайшов
«Розміри резьбових заклепок», «Електроотвертка», «Набір насадок отверточних»,
яких словникова ознака не бачила: суржикові гібриди, український префікс і
закінчення на російському корені. Дописувати їх словоформами означало б
доганяти кожну нову, тому додано корені (`резьб`, `отверт`, `рабоч`, `расход`,
`двухстор`), і працюють вони **лише на словах поза українським словником** —
український відповідник пишеться інакше (`різьб`, `викрут`, `робоч`) і до цієї
перевірки не доходить. Після цього — «ЗБІГ».

**Негативний контроль — той самий, що й 22.08:** `output/noire_rozetka.xml`,
**592 695 слововживань** української прози. Нова ознака спрацювала 243 рази на
148 різних словах. Перевірено **весь** перелік, не вибірку: усі 148 справді
російські («размер», «трендовая», «или», «это», «который», «эротические»,
«материал», «внешний», «набор», «время», «цвет», «ячеек», «ваты») плюс
одруківка «неймовырний». **Хибних тривог 0.** Стара ознака давала на тому
самому корпусі 181 вживання — тобто нова знаходить більше російського й при
цьому не додала жодної помилки.

**Вбудована самоперевірка** без мережі й БД: `toptul_translate.py --selftest` —
33 відомо російських рядки проти 38 українських, `toptul_ru_audit.py
--selftest` — 30 проти 43. Обидва переліки містять і рядки з постановки 25.08,
і ті, на яких хибно спрацьовували ПОПЕРЕДНІ версії ознаки.

**Один рядок із самоперевірки прибрано свідомо** — «Тип привода». `привода` —
омограф, український словник має цю словоформу (як і `приводу`, `привід`), тож
оголосити її російською означало б внести хибну тривогу в замір, ціль якого
нуль. Сам рядок від цього не тікає: він цілком є в `toptul_translation`
(«Тип привода» → «Тип приводу», 9 вживань) і до заміру не доходить.

### Заміри

| Що | Стара ознака | Нова |
|---|---:|---:|
| Назви характеристик російською | **0 / 0** | 123 різних / 3135 вживань |
| Значення характеристик | **0 / 0** | 175 різних / 4050 |
| Назви товарів | 180 | 185 |
| Непізнані слова (суржик, одруківки) | не рахувались | 164 різних / 3527 |
| Негативний контроль NOIRE, 592695 слів | 181 вживання, 0 хибних | 243, **0 хибних** |
| Фід / валідатор | — | 5512 офферів, **0 помилок, 0 попереджень, 100%** |

Числа генератора й незалежного аудитора збіглися до одиниці (123/3135 і
175/4050) — два різні коди на тих самих даних.

### Дотичні проблеми — записано, мовчки не бралось (правило 8)

1. **93 пари в `toptul_translation` самі лишились російськими** (666 вживань):
   «Трещотка фрикційна», «Переходник для головок», «Магнітний тримач для бит»,
   «Викрутка шлицева». Аудит 22.08 давав по них **0** — стара ознака їх не
   бачила, як не бачила й 29 напівперекладених пар, знайдених тоді ж. Це
   матеріал наступного пункту черги, обсяг там переміряно.
2. **Ознака ламала три інструменти.** `toptul_translate_audit.py` і
   `toptul_translate_load.py` викликали `RU.search(...).group(0)`. Замість
   послабити виклики, збережено сумісну форму об'єкта з `.search()` і
   `.group()` — інакше зміна ознаки повалила б робочі скрипти.
3. **`exports/noire_epicentr.xml` на сервері немає.** Це наслідок щоденного
   `git reset --hard origin/main` о 07:30, описаного 23.08. Не відновлював.

### Стан і що не робилось

`output/toptul_rozetka.xml` перезібрано на сервері о 07:58 — 5512 офферів,
21687451 Б. **Не публікувався.** NOIRE і Carvol не чіпались:
`output/noire_rozetka.xml` (07:22), `output/noire_prom.xml` (07:40),
`data/carvol_rozetka.xml` (07:15) — усі три від власного cron, робота йшла
07:50–08:05. MD5 п'яти файлів ноутбук=сервер звірено, збігаються:
`uk_lexicon.py`, `uk_lexicon_build.py`, `toptul_ru_audit.py`,
`toptul_translate.py`, `toptul_rozetka_generator.py`, обидва словники.

**Потрібне від власника: `git push`.** Watchdog уже закомітив нове на сервері
локально (`91447de`), а це означає, що о 07:30 26.08 `git reset --hard
origin/main` їх зітре — рівно як 23.08. На ноутбуці все є, тож втрати не буде,
але поки не запушено, сервер щоранку повертається до старої ознаки. Окремо
варте слова власника: `data/uk_wordforms.txt.gz` — 4.4 МБ у репозиторії. Без
нього ознака не працює (модуль свідомо падає, а не міряє впівсили), але
розмір великий, і якщо це небажано — файл можна тримати поза git і розкладати
скриптом.

## 25.08.2026, другий автономний прогін черги

Узято перший `[ ]` — «Перекласти російські залишки TOPTUL». Характеристики й
назви товарів закрито й перевірено на нуль. Описи лишились і винесені на
рішення власника, тому пункт `[!]`, а не `[x]`.

### Перше, що показав замір: половина роботи вже була зроблена, а журналу про це немає

Аудит на сервері одразу дав `назви характеристик: 0`, `значення: 0` — тоді як
запис за перший прогін 25.08 і постановка в черзі казали 123/3135 і 175/4050.
У `toptul_translation` виявилось 687 назв і 873 значення замість 581 і 677,
тобто **між двома записами в журналі був прогін, який нікуди не записався**.
Це саме той дефект черги, від якого застерігає її власна шапка, тільки навпаки:
пункт лишився `[ ]`, хоч частину вже зробили.

Робота від цього не зникла: у фіді лишались **178 російських назв товарів** і
**46 пар у таблиці, чий власний `dst` російський** — саме те, що постановка
називала «93 пари / 666 вживань».

### Три цілі, яких не можна досягти, бо досягати їх заборонено

Перш ніж перекладати, довелось прибрати з заміру те, що перекладом не
виправити. Це вже третій випадок того самого класу («Бренд: Молния», 23.08), і
щоразу він виглядає як робота, що не закінчується.

1. **Артикул у перекладі.** Серед артикулів TOPTUL є кириличні — `ЭКСТРХ`,
   `РАЗВ9.00`, `КС53-Б`. Літера `Э` робила ПРАВИЛЬНИЙ переклад «Набір
   екстракторів для зламаних болтів (4 шт.) (Харків) ЭКСТРХ» відхиленим як
   російський, і пара не записувалась узагалі — тобто через артикул російським
   лишався ВЕСЬ рядок. `carried_codes()` у завантажувачі був дописаний
   попередньою сесією наполовину: функція є, `import re` немає, виклику немає.
   У такому стані модуль **падав при імпорті** — на сервер він не потрапив, і
   там лежала стара версія. Дописано й підключено.
2. **Бренд у хвості назви.** `build_name()` бере бренд із тега `<vendor>` і
   дописує наприкінці. Товари брендів **«Молния» (3) і «Дальнобойщик» (1)**
   рахувались російськими назавжди. `strip_vendor()` знімає **лише хвостове**
   входження: бренд буває звичайним словом, і вирізати його всюди означало б
   осліпнути на справжнє російське слово в тілі назви.
3. **Обидва винятки — в одному місці.** Генератор імпортує `strip_article` і
   `strip_vendor` з `toptul_ru_audit.py`, а не визначає свої. Збіг числа
   генератора з числом незалежного аудитора і є доказ, що замір не бреше;
   два власні визначення цей доказ знищили б.

Перевірено вузько, а не на віру: виняток на артикул прибрав з таблиці рівно
одну пару (46 → 45), виняток на бренд — рівно 4 назви зі 178 (178 → 174).
Обидва числа передбачені до прогону й збіглися.

### Як відібрано, що саме перекладати

`toptul_translate_export.py` віддавав **2688** назв — він бере рядки, яких
немає в таблиці, ШИРОКОЮ ознакою, і там переважно вже українське («Набір
оправок для запресовування сальників G.I.KRAFT»). Перекладати їх усі —
витратити роботу не на те й **не полагодити 41 пару, яка в таблиці вже є, але
погана**.

Тому `tools/toptul_title_todo.py` іде з протилежного боку — від заміру, яким
пункт закривається: бере готовий фід, строгою ознакою відбирає офери з
російською назвою і для кожного дістає СИРУ назву постачальника (генератор
накладає словник саме на неї, до `build_name()`). Звʼязок — через `id` офера,
а не через схожість тексту: побудована назва з сирою не збігається майже
ніколи. Вийшло рівно 174 замість 2688, і це те, що видно у фіді.

Тег назви не вгадано: `title_tag()` дав `name_ua`, і `--fields` генератора дав
`name_ua` — звірено, бо словник під іншим тегом генератор не знайшов би
ніколи.

### Двобічна перевірка покриття — і чому вона знадобилась одразу

Складання TSV перекладу ключується сирою назвою зі згорнутими пробілами, не
позицією (позиційна прив'язка ламалась 21.08). Перевірок дві, і потрібні обидві:
жодного рядка без перекладу і жодного ключа без рядка.

Перший же прогін завершився кодом 1: **6 рядків без перекладу** — «Ключ
трещоточный рожково-накидний 22/24/27мм» і «…силовий з подвійною головкою
10х13/13х17/14х17мм». Я згорнув їх у діапазони «61–63» і «64–66», коли
переглядав перелік, і жодного не написав. Без цієї перевірки завантажилось би
168 пар із 174, а звіт сказав би «записано» — і шість назв лишились би
російськими без єдиної скарги.

### Негативний контроль завантажувача

Файл із чотирьох пар, кожна перевіряє свою умову, і третя перевіряє **нову**
поведінку — що виняток на артикул саме ПРИЙМАЄ, а не відхиляє:

| пара | очікувано | результат |
|---|---|---|
| правильний переклад | записати | записано |
| `src`, якого немає у фіді | відхилити поіменно | «НЕМАЄ У ФІДІ: 1» |
| `dst` лишився російським | відхилити поіменно | «ПЕРЕКЛАД ЛИШИВСЯ РОСІЙСЬКИМ: 1» |
| `dst` із перенесеним `ЭКСТРХ` | **записати** | записано |

### Позитивний контроль заміру — на цьому ж фіді, а не «колись»

Після виправлень обидві перевірки аудитора дали суцільні нулі. Нуль не
зараховується, доки не показано, що перевірка вміє повернути ненуль на **цьому**
файлі, тому зіпсовано його копію по одному дефекту в кожну гілку:

* назва товару `Тріскачка торцева` → `Трещотка торцевая` — знайдено;
* назва характеристики `Живлення` → `Питание` — знайдено;
* значення `Матеріал` → `Металл` — знайдено.

**Перша спроба контролю значень мовчки не спрацювала**, і це варте запису:
я підставляв `Стан: Новое` замість `Новий`, а у фіді стоїть `Нове` — заміна не
знайшла чого міняти, і гілка значень лишилась неперевіреною, виглядаючи
перевіреною. Помітно було лише тому, що результат друкувався поруч із двома
іншими. Перевірено окремим прогоном.

### Головне за день: аудитор не читав описи, і тому брехав нулем

Прямий `grep` по тому самому файлу, на якому аудитор щойно надрукував суцільні
нулі: `Трещоточ` 12, `Отвертка` 20, `Набор ` 92, `Переходник` 14,
`монтировку` 50, `никелирован` 6, `холдере` 12, `распредвала` 2, `об/мин` 6.

`scan()` читав `name_ua`, назви характеристик і значення — і **не читав
`description_ua` взагалі**. Нуль був фактом про перелік полів аудитора, а не
про фід: рівно те, що 15.08 дало «0 розбіжностей» на тезі `description`,
якого у фіді Rozetka немає. Ознака мови була ні до чого — вона працює; її
просто не питали.

Описи додано до обох перевірок. Міряється **тільки `description_ua`**: тег
`description` — російська версія картки, російський текст у ньому доречний, і
вимагати від нього української означало б завести четверту недосяжну ціль.
Звірка перелік/словник для описів іде по `id` офера, бо перелік працює на
цілому тексті, а словникова ознака на словах — спільний ключ потрібен інший.

Що виявилось під нулем: **478 оферів, 1206 різних російських слів, 4651
вживання** («воздуха» 91, «давление» 65, «набор» 65, «распыления» 55).
Постановка казала 277 описів — насправді **478**, у 1.7 раза більше.

### Чому описи не перекладені, а винесені на рішення

433 різні тексти, **417 700 символів** прози, медіана 772 символи. Це не 174
короткі назви — це робота на кілька сесій, і зроблена нашвидкуруч вона була б
гіршою за невиконану. Окремо: виду рядків `desc` у `toptul_translate.py`
взагалі немає (`KINDS` знає name/value/title), а `NVIDIA_API_KEY` є лише на
ноутбуці. Тому це рішення власника про спосіб, а не «не встиг»; варіанти
виписані в черзі.

### Заміри

| Що | До | Після |
|---|---:|---:|
| Назви характеристик російською | 0 / 0 *(уже було зроблено)* | 0 / 0 |
| Значення характеристик | 0 / 0 *(уже було зроблено)* | 0 / 0 |
| **Назви товарів** | **178** (з них 4 недосяжні) | **0** |
| **Хибних пар у `toptul_translation`** | **46** з 1842 | **0** з 1975 |
| `toptul_translation`, назви товарів | 282 | **415** |
| Непізнані слова (суржик, терміни) | 164 / 3527 | 170 / 3654 |
| **Описи `description_ua`** | **не мірялись** | **478 оферів, 1206 слів, 4651** |
| Офферів / валідатор | 5512 | 5512, **0 помилок, 0 попереджень, 100%** |

Числа генератора й незалежного аудитора збіглися: обидва 0/0/0.

**Нуль пояснено механічно.** Таблиця не порожня — генератор друкує
«Перекладів: name 687, title 415, value 873». Та сама перевірка на тих самих
даних давала 178, потім 174, потім 0: вона рухалась разом із виправленнями.
І перевірено окремо від інструментів, прямим пошуком: `Трещотка` 0 проти
`Тріскачка` 464, `Удлинитель` 0 проти `Подовжувач` 490, `автоинструмента` 0,
`об/мин` 6 → усі шість в описах, не в назвах.

**Зростання кошика непізнаних (+6 слів, +127 вживань) — мій внесок, і він
перевірений.** Введені терміни класифіковано поіменно: `тріскачкова`,
`монтувалка`, `пневмотріскачка`, `тризахватний`, `компресометр`,
`розсухарювач`, `шліцьова` — **unknown**, тобто торгові терміни поза словником
`wukrainian`; `вологомір`, `нутромір`, `зенківка`, `колінвала`, `нікельований`,
`тріскачка` — **uk**. Жодного **ru**. Кошик «непізнане» для таких слів і
призначений — це не російська.

### Дотичне, записано й мовчки не бралось (правило 8)

1. **`toptul_translate_load.py` на ноутбуці був зламаний і не запускався** —
   модульний `re.compile` без `import re`. Через це на сервері лежала стара
   версія, і MD5 розходився. Полагоджено, розгорнуто, MD5 звірено.
2. **Прогін без запису в журналі** (див. початок). Якщо сесія робить роботу й
   не пише про неї, наступна витрачає перші хвилини на з'ясування, що вже
   зроблено, — і може переробити зроблене.
3. **«нож/хв» у назві KPAA1650E** — одруківка постачальника в позначці ударів
   за хвилину. Перекладено як `уд/хв`: це переклад одиниці, не вигадане
   значення. Якщо власник вважає інакше — один рядок у словнику.

### Стан і що не робилось

`output/toptul_rozetka.xml` перезібрано на сервері о 11:55 — 5512 оферів,
21685228 Б. **Не публікувався.** NOIRE і Carvol не чіпались; обидва рухи
всередині сесії — власний cron, звірено з crontab:
`output/noire_rozetka.xml` 11:22 (`20 * * * * --publish-rozetka`),
`output/noire_prom.xml` 11:40 (`40 7,11,15,19 --publish-prom`),
`data/carvol_rozetka.xml` 07:15, `exports/carvol_epicentr.xml` 07:30.
MD5 девʼяти файлів ноутбук=сервер збігається.

Новий інструмент: `tools/toptul_title_todo.py`.
Змінені: `toptul_ru_audit.py` (описи, `strip_vendor`),
`toptul_translate_load.py` (`import re`, `carried_codes` підключено),
`toptul_translate_audit.py` (той самий виняток),
`toptul_rozetka_generator.py` (самоперевірка міряє те, що можна виправити).

**Потрібне від власника: `git push`** — і воно термінове саме зараз. Робота за
25.08 (обидва прогони) живе лише в локальних комітах watchdog, а `git reset
--hard origin/main` о 07:30 26.08 зітре їх, як 23.08. На ноутбуці код є,
переклади — у PostgreSQL, тож дані не пропадуть; пропаде код, і сервер
щоранку повертатиметься до сліпого аудитора.

## 25.08.2026, третій автономний прогін черги

Узято перший `[ ]` — «Виправити хибні переклади характеристик TOPTUL». Закрито
`[x]`: п'ять пар застосовано, дві виявились уже закритими, дві завідомо хибні
перевірка відхилила сама.

### Перше: перелік пар не можна було брати на віру, і в постановці це сказано

Пари складено НЕЧІТКИМ зіставленням, і той самий прогін видав «Матеріал
головки» ← «Матеріал рукоятки». Тобто джерело переліку однаково породжує
правильні й хибні пари, і відрізнити їх читанням очима не можна: обидві
виглядають однаково правдоподібно.

Тому спершу зроблено не правку, а **звірку з майданчиком**:
`tools/toptul_param_rename_check.py` бере кеш офіційних характеристик Rozetka
(`data/rozetka_category_options.json`, 132 категорії, зібрав
`rozetka_category_options.py`) і для кожної пари питає **саме ті категорії, де
лежать наші оффери з поточною назвою**:

* чи знає Rozetka ЦІЛЬОВУ назву і чи вона там ФІЛЬТР;
* чи знає Rozetka ПОТОЧНУ назву — якщо знає, перейменування не просто зайве,
  воно ламає наявне влучення у фільтр.

Вирок виносить майданчик, а не схожість рядків.

### Негативний контроль спрацював на справжній хибній парі

Дві ознаки розійшлися рівно навпаки, і це і є доказ, що перевірка розрізняє:

| пара | Rozetka знає ЦІЛЬ | Rozetka знає ПОТОЧНУ | вирок |
|---|---|---|---|
| «Тип відвертки» → «Тип викрутки» | 1/1 кат. (фільтр) | 0/1 | ПІДТВЕРДЖЕНО |
| «Діаметр сопла» → «Діаметр форсунки» | 2/3 кат. | 0/3 | ПІДТВЕРДЖЕНО |
| **«Матеріал головки» → «Матеріал рукоятки»** | **0/2** | **2/2 (56 офф.)** | **НЕ ПІДТВЕРДЖЕНО** |

«Матеріал головки» — законна назва Rozetka в обох наших категоріях. Правка
за нечітким переліком зняла б 56 офферів із чинного фільтра. Це не гіпотетична
шкода: пара стояла в постановці поруч із правильними.

Друга хибна пара («Кількість граней») дала «НЕМАЄ У ФІДІ» — це слабший
результат, бо нічого не розрізняє: такої назви в нас просто ще немає (вона —
наступний пункт черги). Розрізняльним контролем є саме «Матеріал головки».

### Дві пари виявились уже закритими — і чому це варто записати

«Вид резьби» → «Вид різі» і «Крок резьби» → «Крок різі» у фіді **не існують**:
вони вже перекладені. Числа постановки (84/84) взято зі звіту
`docs/rozetka_option_gaps.json` **від 07:18**, а фід відтоді перезбирали двічі.
Тобто частину роботи закрив попередній прогін цього ж дня, а черга про це не
знала — той самий розрив «зроблено, але не записано», що й уранці.

З цієї ж причини звіт від 07:18 **не годиться як база порівняння**: різниця
проти нього містить чужі зміни («Країна-виробник товару» 4766 → 0, «Тип
світлодіодів», «Клас лазера»), яких я не робив. Приписати їх собі було б
неправдою, зручною для звіту.

### Ізольований замір: той самий фід, різняться лише п'ять назв

Замість порівняння з чужою базою побудовано **точну базу**: узято ПОТОЧНИЙ фід
і повернуто в ньому назад рівно ці п'ять назв. Усе інше побайтово однакове,
тож різниця належить перейменуванню й нічому більше.

«Довжина» окремо: у готовому фіді її не відрізнити від давньої «Довжина»
(1495 тегів), тому 62 оффери взято за СИРОЮ назвою постачальника «Длина, мм»
з `/tmp/toptul.xml` — саме її перекладає правлений рядок таблиці.

| відсутній фільтр (вага = оффери категорії) | до | після |
|---|---:|---:|
| Тип викрутки | 229 | **0** |
| Діаметр форсунки | 205 | **0** |
| Тип біти | 183 | **0** |
| Кількість відділень/кишень | 88 | **0** |
| Довжина | 120 | **43** |
| **сума ваг усіх відсутніх фільтрів** | **22819** | **22037** |
| різних відсутніх фільтрів | 288 | 284 |

**Жоден інший фільтр не змінився** — це і є перевірка на побічну шкоду.
Дельти збіглися з постановкою до одиниці: 229, 205, 183, 88 і 120−43=77.

### Чого цей нуль НЕ означає

Вага фільтра рахується **за категорією**: нуль означає «назва тепер існує в
категорії», а не «її має кожен оффер». Реальне покриття міряне окремо:

| назва | офферів із нею | офферів у категорії |
|---|---:|---:|
| Тип біти | 183 | 183 |
| Діаметр форсунки | 198 | 205 |
| Тип викрутки | 172 | **229** |
| Кількість відділень/кишень | 41 | **88** |

Тобто 57 викруток і 47 сумок у фільтр досі не потраплять — не через назву, а
тому, що постачальник не дав значення. Це матеріал іншого пункту, не цього.

### Зіткнення назв, передбачене до прогону

Два оффери мали одночасно «Довжина» і «Довжина, мм» (SBAA0414, SBBA0516) —
після перейменування вийшло б два теги з тим самим іменем, що валідатор рахує
дублем. Перевірено ЗАЗДАЛЕГІДЬ: `collect_params()` перекладає назву **до**
складання відра, тож однакові значення (138 і 138, 160 і 160) просто злилися.
Передбачення: 1495 + 62 − 2 = 1555. Факт: **1555**.

### Три запобіжники в застосуванні, кожен перевірений відмовою

`tools/toptul_param_rename_apply.py` править `dst` у `toptul_translation` і
відмовляється, коли:

| умова | контроль | результат |
|---|---|---|
| пара не підтверджена звіркою | «Матеріал головки» | «ВІДМОВА … ціль у 0 кат., поточна у 2» |
| назви немає у фіді | вигадана назва | «пропуск: … у фіді немає» |
| немає рядка таблиці з таким `dst` | «Бренд» (сира назва без перекладу) | «ВІДМОВА … немає жодного рядка», код 1 |

У тому ж прогоні правильна пара приймалась — інакше «усі відмови» нічого не
доводили б. `UPDATE` іде з умовою `dst = стара назва`: якщо хтось переклав
рядок інакше після звірки, він не буде мовчки перезаписаний.

Щит `toptul_translation_bak_20260825` (усі 1975 рядків), відкат — `--revert`.

### Заміри

| Що | До | Після |
|---|---:|---:|
| Вага відсутніх фільтрів Rozetka | 22819 | **22037** |
| Різних відсутніх фільтрів | 288 | **284** |
| Старі назви у фіді | 1231 тег | **0** |
| Офферів / валідатор | 5512 | 5512, **0 помилок, 0 попереджень, 100%** |
| Назви/значення/назви товарів російською | 0 / 0 / 0 | 0 / 0 / 0 |
| Непізнані слова (суржик) | 170 / 3654 | **168 / 3441** |
| Описи `description_ua` російською | 478 оферів | 478 (пункт `[!]`, не чіпав) |

**Нуль пояснено механічно.** Перевірка на ТОМУ САМОМУ файлі й далі повертає
ненуль: «Матеріал головки» — 56 офферів, 2 категорії. Тобто вона не осліпла, а
знайшла те, що є. Окремо, прямим підрахунком тегів: «Тип бити» 0 проти «Тип
біти» 747, «Тип відвертки» 0 проти «Тип викрутки» 172, «Діаметр сопла» 0 проти
«Діаметр форсунки» 209, «Довжина, мм» 0.

### Дотичне, записано й мовчки не бралось (правило 8)

1. **«Виконання бити» (144 оффери) — та сама одруківка, що й у «Тип бити»**,
   але в переліку постановки її немає, тому не чіпав. Перевірено: Rozetka цієї
   назви не знає в жодній з 5 наших категорій, тож правка нічого не дасть для
   фільтрів — це чиста орфографія. Один рядок:
   `UPDATE toptul_translation SET dst='Виконання біти' WHERE kind='name' AND src='Исполнение биты';`
2. **Ще 15 кандидатів на перейменування** — `toptul_param_rename_check.py
   --suggest` (наша назва невідома Rozetka, а схожа назва-ФІЛЬТР у тій самій
   категорії є). Найваговіші: «Матеріал рукоятки» → «Матеріал ручки» (78 офф.),
   «Розташування ресивера» → «…ресиверу» (31), «Максимальний зварний струм» →
   «…зварювальний» (15), «Максимальний крутючий момент» → «…крутний» (7),
   «Тип:» → «Тип» (зайва двокрапка). **Перелік містить і сміття** — там же
   стоїть «Мінімальний діаметр труби» → «Максимальний діаметр труби», тобто
   підміна мінімуму максимумом. Саме тому він друкується, а не застосовується:
   кожну пару має підтвердити звірка, як сьогодні.
3. **`docs/rozetka_option_gaps.json` перезаписується кожним прогоном**
   `rozetka_category_options.py`, і старий звіт легко прийняти за базу
   порівняння — я сам мало не прийняв. Файл варто версіонувати датою.

### Стан і що не робилось

`output/toptul_rozetka.xml` перезібрано на сервері о 12:36 — 5512 оферів,
21685504 Б. **Не публікувався.** NOIRE і Carvol не чіпались, усі рухи — власний
cron і всі ДО початку роботи (12:30): `output/noire_rozetka.xml` 12:22
(`20 * * * *`), `output/noire_prom.xml` 11:40, `data/carvol_rozetka.xml` 07:15,
`exports/carvol_epicentr.xml` 07:30.

Нові інструменти: `tools/toptul_param_rename_check.py`,
`tools/toptul_param_rename_apply.py`. MD5 ноутбук=сервер звірено, збігається:
`7ed4c8e2…` і `cfc9fc7f…`. Сама правка живе в PostgreSQL, спільній для обох
машин, тож розгортання не потребує. `docs/rozetka_option_gaps.json` на сервері
лишено у стані «після» (22037 / 284), тимчасові копії — у `/tmp`.

**Потрібне від власника: `git push`** — вимога не змінилась із ранку й
залишається терміновою. Робота за 25.08 (уже три прогони) лежить у локальних
комітах watchdog, а `git reset --hard origin/main` о 07:30 26.08 їх зітре.

## 25.08.2026, четвертий автономний прогін черги

Узято перший `[ ]` — «Витягти два фільтри з назв товарів TOPTUL». Закрито `[x]`:
критерій черги виконано ізольованим заміром, обидва рядки зникли з «відсутніх
фільтрів» повністю або майже повністю.

### Перше: половина роботи вже лежала на ноутбуці — і знову без запису в журналі

Перший же замір показав розбіжність MD5 генератора ноутбук≠сервер, а за нею —
готовий `tools/toptul_filter_extract.py` (19891 Б) і `tools/rozetka_option_ids.py`,
яких **немає ні на сервері, ні в журналі, ні в git**. Тобто попередній прогін
25.08 написав модуль, вбудував виклик у генератор і нічого про це не записав —
той самий дефект, що вже названо вранці 25.08 («прогін без запису в журналі»),
тільки цього разу він коштував би не переробки, а гіршого: на сервері щогодини
збирався б фід генератором **без** цієї роботи, а замір на ноутбуці показував би
її як зроблену.

Робота була не дописана рівно в одному місці, і місце промовисте: генератор
збирав `param_ids` із `paramid`/`valueid` — і **нікуди їх не виводив**. Змінна
заповнювалась, читалась нулем разів. Ззовні код виглядав завершеним.

### Що зроблено цією сесією

1. **Розгорнуто на сервер** `toptul_filter_extract.py`, `rozetka_option_ids.py`,
   правлений генератор; MD5 ноутбук=сервер звірено по всіх трьох плюс довідник.
2. **Зібрано довідник значень** `data/rozetka_filter_values.json` (1408 Б) з
   91-МБ кеша `rozetka_category_options.json` — його не було ніде, тобто без
   цього кроку модуль просто падав би при імпорті генератора.
3. **Дописано вивід `paramid`/`valueid`** — те, заради чого довідник і робився.

### Чому кілька значень тут пишуться через кому, а не через `<br>`

Стояче правило генератора — багатозначна характеристика в ОДНОМУ тезі зі
значеннями через `<br>` (два теги з тим самим іменем дали 1403 попередження
15.08). Для характеристик із `valueid` формат описано в самій довідці, p210
(ред. 12.06.2026), і він інший:

    <param name="Сезон" paramid="39560" valueid="2296922, 2645254">Осіння, Весняна</param>

Тому гілка з id пише через кому й рівно в порядку id, а `<br>` лишається для
характеристик БЕЗ id, де формат довідкою не описаний. Обидві гілки живуть
поруч, і причина кожної записана в коді. Перевірено на фіді: **дублікатів назв
характеристик 0**, тегів `param` 43737 → **45488** (+1751 = 1073 + 678, тобто
рівно стільки, скільки додано).

### Порядок джерел зворотний до постановки — і замір це виправдав

Постановка казала «обидва є прямо в назвах». Це правда, але назва — не надійне
джерело: у `Головка торцева ударна 1/2" дюймова 5/8" Toptul (KBBA1620)` перший
дюймовий токен — квадрат, а другий — розмір самої головки. Правило «взяти
дюймові позначки з назви» приписало б головці квадрат 5/8", якого не буває.
Тому спершу характеристика постачальника («Квадрат», «Приєднувальний квадрат»,
«Робочий профіль»), і лише за її відсутності — **перший** токен назви.

Розкладка джерел на 5512 оферах: квадрат **1039 з характеристики + 34 з назви**,
грані **652 + 26**. Усі **60 значень, узятих із назв, переглянуто очима
поштучно** — жодного хибного; серед них `2 1/2"` ударних головок KABA80xx
(довідник Rozetka це значення знає) і `3/4''` через два апострофи.

### Запобіжник від вигаданого значення спрацював на справжніх даних

Назовні виходить лише значення, яке Є в довіднику категорії разом зі своїм
`valueid` — правило 5 черги виконано побудовою, а не уважністю. Що це не
декларація, видно зі статистики прогону: **«значення поза довідником — 1 1/2: 2»**
у «Динамометричних ключах», де довідник Rozetka має лише п'ять значень. Обидва
відкинуті, у фід не потрапили.

Звірка вибірково не робилась — перевірено **всі** 1751 виведених тегів проти
довідника: `paramid` збігається, кількість значень дорівнює кількості id,
кожне значення є в довіднику й має саме свій id. **Розбіжностей 0.**

### Ізольований замір: той самий фід, різняться лише ці дві характеристики

Базою взято не старий звіт (`docs/rozetka_option_gaps.json` перезаписується
кожним прогоном — застереження від 25.08), а **власну копію фіду до змін**
(`/tmp/toptul_rozetka_before.xml`), проміряну тим самим інструментом на тому
самому кеші довідників.

| відсутній фільтр (вага = оффери категорії) | до | після |
|---|---:|---:|
| Розмір посадкового квадрата | 1267 | **93** |
| Кількість граней | 943 | **0** |
| **сума ваг усіх відсутніх фільтрів** | **22037** | **19920** |
| різних відсутніх фільтрів | 284 | **283** |
| офферів у фіді | 5512 | 5512 |

**Змінилось рівно два рядки з 284** — це і є перевірка на побічну шкоду.
Передбачення до прогону (22037 − 1174 − 943 = 19920) збіглося з фактом.

### Чого цей нуль НЕ означає, і чому 93 мають лишитись

Вага рахується за категорією: «0» означає «характеристика в категорії тепер
є», а не «її має кожен оффер». Справжнє покриття:

| категорія | офферів | квадрат | грані |
|---|---:|---:|---:|
| 4668931 Торцеві головки | 943 | 847 | 678 |
| 4668919 Воротки | 147 | 147 | — |
| 4668865 Динамометричні ключі | 84 | 79 | — |
| 4668913 Тріскачні ключі | 93 | **0** | — |

Кожна прогалина пояснена механічно, а не «схоже, все добре» — переглянуто
назви:
* **93 «Тріскачні ключі» — недосяжні за суттю товару.** Усі 93 — ріжково-
  накидні й комбіновані ключі на міліметри («Ключ тріскачковий комбінований
  13 мм»); посадкового квадрата в них немає. Підставити щось означало б
  вигадати значення. Цей залишок ще й **позитивний контроль заміру**: він і далі
  повертає ненуль, тобто не осліп.
* **96 головок без квадрата** — спецголовки під монтувалку, для зливних пробок,
  для розбирання стійок: приводяться не квадратом.
* **265 головок без граней** — профілі TORX, SPLINE, RIBE, PZ, до яких
  «кількість граней» не застосовна, і набори.
* **5 динамометричних** — тарувальні установки й шкали довороту, не ключі.

### Заміри

| Що | До | Після |
|---|---:|---:|
| Вага відсутніх фільтрів Rozetka | 22819 → 22037 | **19920** |
| Різних відсутніх фільтрів | 284 | **283** |
| Тегів `param` у фіді | 43737 | **45488** |
| Офферів / валідатор | 5512 | 5512, **0 помилок, 0 попереджень, 100%** |
| Дублікатів назв характеристик в офері | 0 | **0** |
| Назви/значення/назви товарів російською | 0 / 0 / 0 | 0 / 0 / 0 |
| Непізнані слова (суржик) | 168 / 3441 | **168 / 3441** |
| Описи `description_ua` російською | 478 оферів | 478 (пункт `[!]`, не чіпав) |

Самоперевірка генератора й незалежний `toptul_ru_audit.py` дали однакові нулі,
і аудит завершився «ЗБІГ»: нові значення не принесли ні російських слів, ні
нових непізнаних (кошик не зрушив ні на слово).

**Позитивний контроль без мережі й БД:** `toptul_filter_extract.py --selftest`
— 21 випадок, з них **6 із порожнім очікуванням**. Перевірка, яка вміє лише
знаходити, нічого не варта: серед випадків є TORX і п'ятигранник (грані не
застосовні), «500 гр.» (вага, не грані), категорія поза довідником і оффер, у
якого характеристика вже є від постачальника. Сам selftest падає, якщо порожніх
випадків менше трьох. Прогін на сервері: **0 збоїв**.

### Дотичне, записано й мовчки не бралось (правило 8)

1. **`Набір інструменту 17-65 мм 3/4'' 1" 26 од.`** несе ДВА квадрати, а з назви
   береться лише перший (`3/4`). Для наборів це втрата, але правило «перший
   токен» захищає від набагато гіршого — розміру головки замість квадрата.
   Розв'язок — не назва, а характеристика постачальника; таких наборів одиниці.
2. **«Тріскачні ключі» (93) назавжди лишаться в переліку відсутніх фільтрів.**
   Це не наш дефект, але кожен наступний замір показуватиме рядок 93 — варто
   тримати виняток у звіті, інакше хтось «виправить» його вигаданим значенням.
3. **Довідник `data/rozetka_filter_values.json` покриває лише 4 категорії** з
   132 у фіді — рівно ті дві характеристики. Решта 283 відсутніх фільтрів
   (вага 19920) тим самим механізмом не закриваються: у них немає ні
   характеристики постачальника, ні позначки в назві. Це окрема робота.

### Стан і що не робилось

`output/toptul_rozetka.xml` перезібрано на сервері о 16:34 — 5512 оферів,
21899200 Б. **Не публікувався.** NOIRE і Carvol не чіпались, усі їхні рухи —
власний cron: `output/noire_rozetka.xml` 16:22 (`20 * * * *`),
`output/noire_prom.xml` 15:40 (`40 7,11,15,19`), `data/carvol_rozetka.xml` 07:15,
`exports/carvol_epicentr.xml` 07:30; робота йшла 16:31–16:45.

Нові на сервері: `tools/toptul_filter_extract.py`, `tools/rozetka_option_ids.py`,
`data/rozetka_filter_values.json`. Змінений: `tools/toptul_rozetka_generator.py`
(вивід `paramid`/`valueid`). MD5 всіх чотирьох ноутбук=сервер збігається.
Копії звітів для звірки — `/tmp/gaps_before.json`, `/tmp/gaps_after.json`,
`/tmp/toptul_rozetka_before.xml` на сервері.

**Потрібне від власника: `git push`** — вимога та сама, що й у трьох попередніх
записах, і сьогодні вона гостріша: `toptul_filter_extract.py` існує лише поза
git (ноутбук + сервер), а `git reset --hard origin/main` о 07:30 26.08 зітре
локальні коміти watchdog разом із неврахованими файлами. Втратити модуль —
означає повернути генератор до стану без двох головних фільтрів Rozetka.

## 25.08.2026, п'ятий автономний прогін черги

Узято перший `[ ]` — «Прибрати з фіду товари категорій без пари». Закрито `[x]`,
але не тим, чим здавалось: саме прибирання вже працювало, а бракувало правдивого
числа й переліку.

### Перше: робота, поставлена в чергу, була зроблена ще при написанні генератора

Перший же прогін генератора без єдиної правки надрукував
«пропущено: категорія без rz_id: **205**». Тобто оффери категорій без пари у фід
не потрапляли від першої версії (`cid not in cats` → `continue`), і жодного з них
у `output/toptul_rozetka.xml` немає. Постановка описувала стан, який уже настав.

Це вже четвертий за два дні випадок розриву «зроблено, але не записано», тільки
тепер із протилежним знаком: не робота без запису, а пункт черги без роботи.
Перевіряти фактичний стан ПЕРЕД тим, як братись, дешевше за будь-яку правку.

### Друге: число 205 склеєне з двох різних причин, і одна з них названа неправдиво

Розклад по фіду постачальника (6895 офферів, 338 категорій, усі є в мапінгу):

| | категорій | офферів |
|---|---:|---:|
| довірені рівні — лишаються | 297 | 6690 |
| `rz_id IS NULL` — пари в Rozetka немає | **27** | **128** |
| `tier='review'` — пара є, але здогадку відхилено 23.08 | **14** | **77** |

Постановка казала «205 товарів у 27 категоріях». Насправді 205 — це 41 категорія
(27 + 14), а в 27 категоріях без пари лежить 128 товарів. Журнал за 23.08 писав
саме «41 категорія (205 товарів)»; у черзі числа злиплись.

**Головне тут не арифметика, а назва лічильника.** «Категорія без rz_id: 205»
було твердженням про 77 рядків, у яких `rz_id` Є — це «Інструмент для пайки →
Набори інструментів» (score 0.55), «Тестери герметичності АКБ → Детектори
напруги», відхилені 23.08 за SKILL-04. Причини різні й рішення в них різні:
перші чекають, поки Rozetka заведе категорію, другі — поки хтось знайде правильну
пару. Одне число ховало обидва питання.

Розділено на три лічильники (`load_categories()` тепер віддає ще й словник
відхилених категорій): `категорія без rz_id: 128`,
`категорія недовіреного рівня (review): 77`, `категорія відсутня в мапінгу: 0`
(третій не спрацював жодного разу — і це перевірено, а не припущено: усі 338
категорій фіду в таблиці є).

### Звіт, який себе перевіряє

`tools/toptul_rozetka_dropped_report.py` → `docs/toptul_rozetka_dropped.md`:
41 категорія, 205 артикулів із назвами, згруповані за причиною, з відхиленою
здогадкою біля кожної `review`-категорії.

Правило категорій у звіті **не переписане вдруге** — `ALLOWED_TIERS`,
`load_categories()`, `pictures()`, `collect_params()`, `MIN_PARAMS`
імпортуються з генератора. Два власні визначення «довіреної категорії»
розійшлися б, і звіт описував би не той фід, що на виході, — рівно так 23.08
«у Rozetka немає такої категорії» насправді означало «конкурент такого не
возить».

Дві перевірки, і потрібні обидві:
* жоден перелічений артикул не мусить бути у виданому фіді;
* артикули ЗАЛИШЕНИХ категорій навпаки мусять там бути.

Друга й є позитивний контроль: перевірка «немає у фіді», яка вміє лише
відповідати «немає», давала б чистий звіт на будь-чому. Кожну перевірено
відмовою, і кожна дала код 1:

| контроль | очікувано | результат |
|---|---|---|
| `--out /tmp/toptul.xml` (фід постачальника — там усі 205) | витік | «лишились у фіді: 205», код 1 |
| `--out` із двома чужими id | зламана перевірка присутності | «ЖОДНОГО артикула довіреної категорії», код 1 |
| `--out` порожній фід | нічим перевіряти | «Виданого фіду немає», код 1 |
| штатний прогін | звіт | 0 витоків, 5512 присутніх, код 0 |

### Скільки ми втрачаємо насправді — 61, а не 205

«Прибрано 205» перебільшує наслідок рішення власника втричі: категорія — не
єдина причина вибуття. Прогнав ті самі 205 через решту перевірок генератора
(ціна, фото, ≥3 характеристики) **його ж функціями**: пройшли б **61**, інші
**144** випали б і без цього рішення — переважно через «менше 3 характеристик».
Позначено «✓» у звіті по кожному артикулу.

Втрата зосереджена не там, де найбільші категорії: «Динамометры» — 20 товарів і
**0** готових, а «Обслуживание тормозных цилиндров» — 8 з 19, «Экстракторы» 6 з 9,
«Пескоструйное оборудование» 6 з 12, «Дископравильные станки» 5 з 5. Якщо
власник схоче з кимось із них щось робити, черга починається саме з цих
чотирьох, а не з найбільшої.

### Заміри

| Що | До | Після |
|---|---:|---:|
| «пропущено: категорія без rz_id» | 205 *(на 41 категорію)* | **128** *(27 категорій)* |
| «пропущено: категорія недовіреного рівня (review)» | не рахувалось | **77** *(14 категорій)* |
| Офферів у фіді | 5512 | 5512 |
| Вміст фіду | — | **побайтово той самий**, крім рядка дати |
| Валідатор | 0 / 0 / 100% | 0 помилок, 0 попереджень, **100%** |
| Прибраних артикулів у виданому фіді | не перевірялось | **0** з 205 |
| Артикулів довірених категорій у фіді | — | 5512 з 6690 |
| Назви/значення/назви товарів російською | 0 / 0 / 0 | 0 / 0 / 0 |
| Непізнані слова (суржик) | 168 / 3441 | 168 / 3441 |

**Побайтова тотожність фіду — це і є доказ, що правка нічого не зламала:**
змінилась лише звітність, і жоден оффер не приїхав і не поїхав. Заразом вона
підтверджує, що прибирання справді працювало й до сьогодні.

### Дотичне, записано й мовчки не бралось (правило 8)

1. **14 `review`-категорій (77 товарів) лишаються без пари назавжди**, доки
   хтось не змапить їх руками. Нечітке зіставлення для них уже пробували й
   відхилили; звірка з офіційним каталогом (`toptul_param_rename_check.py`
   робить це для характеристик) для категорій ще не робилась.
2. **`docs/toptul_rozetka_unmapped.md` описує стан 23.08** і каже «41 категорія,
   205 товарів», не розрізняючи двох причин. Новий звіт його не заміняє: там —
   розгорнуті причини на рішення власника, тут — фактичне вибуття. Варто
   звести їх в один документ, коли причини перестануть мінятись.
3. **Лічильник «категорія відсутня в мапінгу» поки завжди 0.** Він доданий не
   про запас: якщо постачальник заведе нову категорію, товари зникнуть із фіду
   мовчки, і без окремого рядка це виглядатиме як «менше товарів у постачальника».

### Стан і що не робилось

`output/toptul_rozetka.xml` перезібрано на сервері о 17:07 — 5512 оферів,
21899200 Б (той самий розмір, що о 16:34). **Не публікувався.** NOIRE і Carvol
не чіпались: `output/noire_rozetka.xml` 16:22 (`20 * * * *`),
`output/noire_prom.xml` 15:40, `data/carvol_rozetka.xml` 07:15,
`exports/carvol_epicentr.xml` 07:30 — усі до початку роботи (17:02–17:10).

Новий інструмент: `tools/toptul_rozetka_dropped_report.py`. Змінений:
`tools/toptul_rozetka_generator.py` (`load_categories` віддає й відхилені
категорії, три лічильники замість одного). Новий документ:
`docs/toptul_rozetka_dropped.md`. MD5 всіх трьох ноутбук=сервер збігається.

**Потрібне від власника: `git push`** — п'ятий запис поспіль із тією самою
вимогою. Робота за 25.08 лежить у локальних комітах watchdog (`c7f3f94`), а
`git reset --hard origin/main` о 07:30 26.08 зітре їх разом із неврахованими
файлами — а це `toptul_filter_extract.py`, `toptul_rozetka_dropped_report.py` і
новий звіт.

## 25.08.2026, шостий автономний прогін черги

Узято перший `[ ]` — «Структура характеристик у конкурента ttul». Закрито `[x]`:
звіт `docs/toptul_competitor_fields.md` складено, критерій черги виконано.

### Головне за прогін: 222 → 7, і це не покращення, а відсіювання

Офіційний довідник Rozetka каже, що в десяти наших найбільших категоріях нам
бракує **222 назв характеристик** (`docs/rozetka_option_gaps.json`). Живий
продавець того самого TOPTUL, який уже пройшов модерацію майданчика, заповнює
з них **7**. Решта 215 — «EAN», «Код УКТ ЗЕД», «Кнопка передзамовлення»,
«Додатковий текст про доставку», габарити пакування: майданчик їх уміє, але не
заповнює ніхто, і нам вони нічого не дадуть.

Тобто цінність звіту не в тому, що він знайшов нові прогалини — він **скоротив
уже відомий перелік у 32 рази** до того, що справді робить хтось на ринку.
Порівнювати наш фід із довідником означає гнатися за 222 полями; порівнювати з
конкурентом — за сімома, з яких чотири є фільтрами видачі.

### Що саме бракує

| Категорія | наших оферів | назва | у нього | Rozetka | у нас деінде |
|---|---:|---|---:|---|---:|
| Комбіновані ключі | 132 | Робочий розмір | 10/10 | **фільтр** | 0 |
| Накидні ключі | 177 | Робочий розмір | 6/10 | **фільтр** | 0 |
| Торцеві головки | 943 | Вид | 10/10 | **фільтр** | 846 |
| Набори інструментів | 290 | Предметів в наборі | 8/10 | **фільтр** | 0 |
| Викрутки | 229 | Вид інструменту | 6/10 | **фільтр** | 0 |
| Викрутки | 229 | Довжина робочого стрижня | 7/10 | так | 0 |
| Воротки | 147 | Довжина | 10/10 | так | 0 |

Колонка «у нас деінде» — не окраса. «Вид» стоїть у нашому фіді **846 разів** в
інших категоріях, «Довжина» — **1555**: назву наш словник і генератор уже
знають, бракує лише джерела значень саме тут. Нуль у цій колонці означає інше —
поля в нас немає взагалі, і його треба заводити з нуля.

У чотирьох категоріях із десяти не бракує **нічого**: Фарбопульти, Біти для
шуруповерта, Знімачі, Швидкознімачі та фітинги.

### Нуль, який довелось пояснити механічно, а не «схоже, все добре»

У «Швидкознімачах та фітингах» перевірка спершу дала не результат, а сигнал
помилки: перетин наших і його назв **порожній**. Я вписав це в інструмент
навмисно як ознаку зламаної нормалізації — і саме вона спрацювала.

Причина виявилась іншою: **конкурент у цій категорії не заповнює жодної
характеристики на всіх 10 картках**. Порожній перетин тут — факт про його фід,
а не про наше порівняння. Перевірку звужено: вона тривожить лише тоді, коли
поля в нього Є, а спільних немає. Звіт тепер каже про цю категорію прямо
(«порівнювати нема з чим»), а не мовчить нулем.

Це той самий клас, від якого застерігає правило позитивного контролю, тільки
дзеркальний: не «нуль прочитали як чистоту», а «нуль ледь не прочитали як
дефект інструмента». Обидва прочитання без пояснення однаково безпідставні.

### Ще один нуль, який виявився дефектом інструмента

`char_titles()` повертав `[]` і коли картка справді без характеристик, і коли
API не відповів. Різниця принципова: у другому випадку «конкурент нічого не
заповнює» було б твердженням про мережу. Розділено: `None` — відповіді немає
(картка йде в «відкинуто»), `[]` — картка справді порожня (йде в лічильник
«без характеристик», який друкується у звіті). Без цього шість порожніх карток
у «Знімачах» і десять у «Швидкознімачах» неможливо було б відрізнити від
обриву звʼязку.

Перший прогін і справді впав посеред категорії — `ConnectionReset` на
`product-api`. Додано повтор із подвоєнням паузи; без нього прогін або падає,
або (гірше) віддає неповний перелік карток як повний.

### Позитивний і негативний контроль

**Вбудована самоперевірка без мережі й БД** — `--selftest`, 5 випадків плюс 2
на нормалізацію назв, і **два з них навмисно порожні**: перевірка, яка вміє
лише знаходити, нічого не варта.

| випадок | очікувано | результат |
|---|---|---|
| поле в усіх його картках, у нас немає | знайти | знайдено |
| **у нас є все** | **нічого не знайти** | прогалин 0 |
| поле в 1 картці з 3 | не консенсус, у «поодинокі» | так |
| інший апостроф у назві | **не** вигадати прогалину | прогалин 0 |
| «Гарантія» (поле кабінету, не XML) | не рахувати прогалиною | не зараховано |

**Контроль на СПРАВЖНІХ зібраних даних, а не лише на синтетиці.** Три досліди
на «Комбінованих ключах», де перевірка знайшла одну прогалину:

* прибрати з нашого боку наявну «Форму ключа» → прогалин стало **2** («Робочий
  розмір», «Форма ключа»). Перевірка вміє повернути ненуль на цьому файлі;
* додати «Робочий розмір» до наших → прогалин **0**. Тобто вона вміє й зникати;
* підмінити апостроф у його назвах на інший вид → прогалин так само **0**,
  нормалізація тримає.

**Незалежна звірка, окремо від інструмента.** Усі 7 назв перелічено прямим
підрахунком тегів `<param name=...>` у фіді на сервері, по категоріях:
«Робочий розмір» — 0 у категоріях 44 і 62 і **0 у всьому фіді**;
«Предметів в наборі» — 0; «Вид інструменту» — 0; «Довжина робочого стрижня» — 0
(зате «Довжина робочого стрижня, мм» — 3). І навпаки, для контролю, назви, які
звіт вважає спільними: «Форма ключа» 104 і 131, «Тип викрутки» 172,
«Комплектація» 77, «Тип» 709. Жодної розбіжності з інструментом.

**Відбір карток теж перевіряється, а не приймається на віру.** Кожна картка
проходить дві умови: `category_id` дорівнює нашому `rz_id` і `seller_id` — це
`ttul` (13624). Інакше фільтр `section_id` міг би тихо принести сусідню
категорію або чужого продавця. У цьому прогоні відкинутих не було жодної.

### Правило SKILL-14.8 виконано побудовою, а не уважністю

З відповіді API беруться ключі `title`, а гілка `values` не читається взагалі —
значень немає ні в сирому зібраному файлі, ні у звіті. Це записано в коді
`char_titles()` окремим коментарем: спокуса «заразом подивитись, що там у
значеннях» виникає щоразу, а чужа картка може стосуватись іншого варіанта
товару, і перенесення чужого факту на наш SKU — дезінформація покупця.

### Заміри

| Що | Значення |
|---|---:|
| Категорій проміряно (найбільші наші) | 10 з 132 |
| Наших оферів у них | 2722 з 5512 (**55%**) |
| Карток конкурента розібрано | **100** (по 10) |
| З них без жодної характеристики | 18 |
| Відкинуто (чужа категорія / продавець / без відповіді) | **0** |
| Спільних назв (позитивний контроль порівняння) | 27 |
| **Бракує нам** | **7**, з них фільтрів **4** |
| Бракує за офіційним довідником у тих самих категоріях | 222 |
| Категорій, де не бракує нічого | 4 з 10 |

### Дотичне, записано й мовчки не бралось (правило 8)

1. **«Довжина робочого стрижня» проти нашої «Довжина робочого стрижня, мм».**
   Це кандидат на перейменування рівно того класу, який 25.08 закривав
   `toptul_param_rename_check.py` — але наша назва стоїть лише на **3** оферах,
   тож виграш мізерний, і звірку з майданчиком по ній я не робив. Так само
   «Вид інструменту» проти нашого «Тип інструмента»: перш ніж міняти, треба
   спитати майданчик, як 25.08 уранці, — тоді хибна пара «Матеріал головки»
   зняла б 56 оферів із чинного фільтра.
2. **Чотири відсутні фільтри мають різну ціну.** «Вид» у Торцевих головках і
   «Робочий розмір» у двох категоріях ключів — це 943 + 309 оферів, тобто 23%
   фіду; «Предметів в наборі» — 290. Чи є для них джерело в характеристиках
   постачальника, я не міряв: це вже наступна робота, а не ця.
3. **Вибірка — перша сторінка видачі продавця** (до 60 позицій), з неї перші
   10 карток. «Бракує 0» означає «серед цих карток нічого нового», а не
   «конкурент більше нічого не заповнює». Межа виписана в самому звіті, щоб
   наступний читач не прочитав нуль ширше, ніж він є.
4. **`docs/rozetka_option_gaps.json` тепер лежить і на ноутбуці** — я скопіював
   його з сервера, щоб звіт рахував порівняння 222 → 7 сам, а не з моїх слів.
   Застереження від 25.08 чинне: файл перезаписується кожним прогоном
   `rozetka_category_options.py`, тож локальна копія старіє мовчки.

### Стан і що не робилось

**Фід не перезбирався й не публікувався** — цій роботі це не потрібно, вона
лише читає. `output/toptul_rozetka.xml` на сервері лишився від 17:07 (5512
оферів, 21899200 Б), тобто в тому самому стані, що й після пʼятого прогону.
NOIRE і Carvol не чіпались, усі їхні рухи — власний cron і всі ДО початку
роботи (17:30): `output/noire_rozetka.xml` 17:22 (`20 * * * *`),
`output/noire_prom.xml` 15:40, `data/carvol_rozetka.xml` 07:15,
`exports/carvol_epicentr.xml` 07:30.

Новий інструмент: `tools/toptul_competitor_fields.py` (MD5
`420efcc8…`, ноутбук=сервер звірено). Новий документ:
`docs/toptul_competitor_fields.md` (MD5 `b60595135a2a…`, ноутбук=сервер).
Сире зібране — `/tmp/toptul_competitor_raw.json`, наш зріз —
`/tmp/toptul_ours.json` на обох машинах.

**Потрібне від власника: `git push`** — шостий запис поспіль із тією самою
вимогою. Робота за 25.08 лежить у локальних комітах watchdog, а `git reset
--hard origin/main` о 07:30 26.08 зітре їх разом із неврахованими файлами.

## 25.08.2026, сьомий автономний прогін черги

Узято перший `[ ]` — «Сторож для фіду Carvol→Rozetka». Закрито `[x]`. Постановка
підтвердилась (у `watchdog.py` не було жодної згадки Carvol), але потрібних
перевірок виявилось не дві, а чотири, і четверту довелось додати через те, що
показав замір.

### Головне: три перевірки NOIRE-зразка сказали б «усе гаразд» про добу без публікації

Перед тим як копіювати зразок, я подивився, як цей фід узагалі доїжджає до
Rozetka. У логу `rozetka_github_sync.py` — **три «Git push: ❌ FAILED» поспіль**
(23, 24, 25.08), а в репозиторії `affilate_aggent` комітів по шляху
`data/carvol_rozetka.xml` за **23.08 немає взагалі**: 22 → 24 → 25.

Тобто цілу добу Rozetka читала ціни від 22.08, і жодна перевірка цього не
побачила б, якби була:

| що перевіряє зразок NOIRE | що воно казало 23.08 |
|---|---|
| локальний файл свіжий | ✅ зібраний о 07:15 того дня |
| raw-URL доступний | ✅ HTTP 200 |
| raw-URL не обрізаний | ✅ 40 МБ, повний розмір |

Усі три відповіді правдиві, і всі три — **не про те**. Це той самий клас, що й
правило CLAUDE.md «фід зібрано ≠ фід опубліковано», тільки з боку сторожа:
свіжість ЛОКАЛЬНОГО файлу нічого не каже про свіжість ОПУБЛІКОВАНОГО, а
raw-URL віддає вчорашній файл із таким самим кодом і розміром, як сьогоднішній.

Тому додано четверту перевірку, якої в зразку немає: `published_age_h()` бере
дату останнього коміту, що торкався саме цього шляху
(`api.github.com/.../commits?path=…`), і порівнює з тим самим порогом 26 год.
Береться дата коміту, а не час пушу: вона каже, коли зібрано ВМІСТ, який зараз
лежить за посиланням. Прайс, запушений із запізненням на добу, і є добової
давнини — хоч би коли він доїхав.

**Невідомий результат навмисно не є проблемою.** Ліміт GitHub без токена — 60
запитів на годину з IP, watchdog ходить туди 144 рази на добу (6/год), запас
є, але мережа буває. Тривога на кожен 403 навчила б власника прогортати
сповіщення не читаючи, тож `-1.0` означає «не знаю» і мовчить.

### Чому пуш падав — три різні причини, і жодна не була помічена

1. **23 і 24.08 — протухлий токен**: `remote: Invalid username or token`.
2. **25.08 — інша причина**: `git pull --rebase` відмовився через незакомічені
   зміни, тож крок `push` не виконався взагалі. Це повторюватиметься: будь-який
   незакомічений файл о 07:15 блокує публікацію.
3. **Права зараз робочі** — `git push --dry-run` на сервері проходить
   (`43be8de..bb561ef`), нічого не змінюючи.

**Чому ж 24 і 25.08 усе-таки опубліковані.** Не тому, що пуш спрацював, а тому,
що о 07:30 `carvol_epicentr_sync.py` робить свій push і **тягне за собою чужий
коміт**. Видно прямо в історії: `43be8de 07:30 sync: epicentr…` стоїть над
`e891d79 07:15 sync: prices+availability`. А 23.08 того ж не сталось, бо тоді
о 07:30 ще стояв `git reset --hard origin/main`, який знищував коміт за 15
хвилин до пушу. Тобто прайс Rozetka публікується **пасажиром**: прибрати або
зламати синхронізацію Єпіцентру — і він мовчки перестане доїжджати.

**Заразом уточнення до шести попередніх записів журналу:** щоденного
`git reset --hard origin/main` більше немає, він замінений на адресний
`git checkout origin/main -- <один файл>` (комміт b57f88e, на сервері вже
розгорнутий — перевірено в коді). Тобто застереження «о 07:30 26.08 роботу
зітре» більше не чинне; `git push` власника потрібен, але вже не терміново.

### Перевірка, яка знайшла б це, і перевірка, яка не знайшла

`check_sync_log()` мала б зловити «Git push: ❌ FAILED» — вона саме для цього й
писана. Не зловила жодного разу з трьох, бо **читає лише останній рядок логу**
(`lines[-1]`), а `rozetka_github_sync.py` друкує останнім рядок «URL: …».
Замір це показує прямо: `sync ok=True  msg="URL: https://raw.githubuser…"`.
Не чіпав (правило 8): зміна порогу спрацювання діючого алерту — окреме рішення,
записано в чергу.

### Що саме додано

`check_carvol_rozetka_feed()` — свіжість 26 год (добовий цикл о **07:15**, не
07:30, як казала постановка: `15 7 * * *`; о 07:30 йде Єпіцентр), мінімум
20 МБ при фактичних 40.6 МБ, доступність raw-URL і вік публікації. Три відмови
розділені й називаються поіменно, бо рішення в них різні: не зібрався / зібрався
обрізаним / зібрався, але не доїхав.

URL інший, ніж у трьох NOIRE: цей єдиний фід публікується в `affilate_aggent`,
а не в `noire-feed`.

### Позитивний контроль

| випадок | очікувано | результат |
|---|---|---|
| штатний прогін | ok | ok, «опубліковано 15 год тому» |
| файлу немає | тривога | «локального фіду Carvol немає» |
| файл 40 год давності | тривога | «не оновлювався 40 год» |
| файл обрізаний до 1 МБ | тривога | «обрізаний: 1000000 Б» |
| raw-URL не існує | тривога | «недоступний … HTTP 404» |
| **публікація давня** | тривога | «старша за 26 год (1706) — push не доїхав» |
| API недоступний | **тиші** | ok, «опубліковано невідомо» |
| та сама проблема двічі | 1 тривога | 1 |

Випадок «публікація давня» — **не синтетика**: узято шлях `CLAUDE.md` у тому
самому репозиторії, який справді давно не мінявся (1706 год). Три випадки з
восьми навмисно вимагають ТИШІ: перевірка, яка вміє лише тривожити, так само
непридатна, як та, що вміє лише мовчати.

**Стороння перевірка, що пояснила чужу константу.** `probe_raw` шле
`Accept-Encoding: identity` — заміряв, чому: без цього заголовка
`content-length` на цьому фіді дорівнює **784913** (gzip) замість **40650634**,
тобто перевірка розміру спрацьовувала б хибно щоразу. Коментар біля константи
казав це про NOIRE; на Carvol підтвердилось числом (правило CLAUDE.md 4 —
читати коментар біля параметра перед тим, як його чіпати).

### Наявні перевірки не зламані

Прогін до змін і після — тим самим інструментом (`/tmp/wd_probe.py`, з
заглушеною `tg()` і прапорами дедуплікації, відведеними в `/tmp/wdprobe_*`,
щоб не з'їсти тривогу справжнього cron; `check_git`/`check_services` не
викликались узагалі — вони комітять і перезапускають сервіси):

| перевірка | до | після |
|---|---|---|
| cron | ok=True | ok=True |
| sync | ok=True | ok=True |
| noire | ok=True «feed ok» | ok=True «feed ok» |
| noire-rz | ok=True «feed ok» | ok=True «feed ok» |
| noire-prom | ok=True «feed ok» | ok=True «feed ok» |
| **carvol-rz** | перевірки не існувало | **ok=True «feed ok»** |

Тривог надіслано 0 в обох прогонах. І головне — перевірено не лише своїм
інструментом: **справжній cron о 22:10 відпрацював із новим кодом** —
`[22:10:02] [carvol-rz] фід свіжий, raw-URL доступний, опубліковано 15 год тому`,
`[carvol-rz] ok=True feed ok`, `Watchdog END`, жодного `Traceback` у всьому
логу. Тимчасові файли контролю (копія фіду з підробленою датою, обрізана копія,
тестові прапори) з `/tmp` прибрані, справжні прапори дедуплікації не чіпались.

### Заміри стану фіду Carvol на 25.08

| Що | Значення |
|---|---:|
| локальний `data/carvol_rozetka.xml` | 40 650 634 Б, 07:15 |
| опублікований (raw, `identity`) | 40 650 634 Б |
| sha256 локального й опублікованого | **збігається** (`0b5b9f12…`) |
| вік публікації | 14.8 год |
| «Git push: ❌ FAILED» у логу | 3 з 8 прогонів |
| діб без публікації | **1** (23.08) |

Тобто **зараз** публікація правильна — і саме тому важливо, що сторож тепер
є: попередній розрив помітили лише через два дні й лише тому, що хтось пішов
дивитись у лог руками.

### Стан і що не робилось

Змінений один файл — `tools/watchdog.py` (MD5 `78042f89…`, ноутбук=сервер
звірено; попередній `892a05e3…`, копія `/tmp/watchdog.py.bak`). Фіди не
перезбирались і не публікувались: робота тільки читає. Carvol — читання, як
вимагає правило 4 черги. NOIRE не торкався: `output/noire_rozetka.xml` 21:22
(`20 * * * *`), `output/noire_prom.xml` 19:40, `output/noire_epicentr_phase1.xml`
20:05, `exports/carvol_epicentr.xml` 07:30 — усі рухи від власного cron і всі до
початку роботи (22:00–22:12). Єдиний запис у мережу за сесію — `git push
--dry-run`, який нічого не змінює.

**Потрібне від власника: `git push`** — сьомий запис поспіль. Терміновість
менша, ніж писали попередні шість (див. вище про скасований `reset --hard`),
але робота за 25.08 досі лише в локальних комітах watchdog: на сервері новий
`watchdog.py` закомічено о 22:08 (`0b90783`), origin про нього не знає.

## 31.08.2026, автономний прогін черги

Узято перший `[ ]` — «Публікація прайсу Carvol не доїжджає, і про це ніхто не
дізнається». Закрито `[x]`: обидві половини (сторож, який не бачить відмови, і
причина самої відмови) виправлені, розгорнуті на сервері й перевірені.

### Постановка підтвердилась повністю, і в логу є всі три відмови

Замір на сервері, до будь-яких змін: у `/tmp/rozetka_sync_cron.log` **14
прогонів від 18.08 до 31.08, з них три провалені — 23, 24 і 25.08**. Стара
`check_sync_log()`, прогнана на кожному з них окремо, каже `ok=True` **тричі з
трьох**. Тобто це не оцінка й не переказ постановки: та сама функція на тих
самих даних мовчить рівно там, де мала тривожити.

Причина точна. Функція бере `lines[-1]`, а кожен прогін закінчується рядком
`URL: https://raw.githubusercontent.com/…`. Рядок `Git push: ❌ FAILED` стоїть
за два рядки вище й ніколи не потрапляє в перевірку.

### Чому не можна ні читати весь лог, ні читати останній рядок

Лог дописується, а не перезаписується. Обидві очевидні межі неправильні, і
неправильні по-різному:

* останній рядок — сліпота, з якою й прийшли;
* весь файл — три відмови 23-25.08 тривожили б **вічно**, бо вони в ньому
  назавжди. Тривога, яку неможливо погасити, за тиждень навчає її не читати.

Тому додано `last_sync_run()`: межа прогону — рядок `=== Rozetka GitHub Sync
===`, перевіряється **останній прогін цілком**. Якщо маркера немає (лог
обрізали), береться хвіст у 40 рядків — довший за один прогін (~23 рядки):
краще подивитись зайве, ніж оголосити успішним прогін, якого не бачили.

**Ключ дедуплікації тепер містить дату прогону.** Без неї друга така сама
відмова наступного дня мовчала б — текст `Git push: ❌ FAILED` однаковий, а
прапор уже виставлений. Саме це й сталося б 24.08 після 23.08. З датою виходить
рівно одна тривога на кожен провалений прогін, і при цьому не 144 на добу від
десятихвилинного cron.

**У тривогу йде рядок ERROR, а не «FAILED».** Наслідок однаковий, а причини
різні й робити з ними треба різне: 23-24.08 — `remote: Invalid username or
token`, 25.08 — `cannot pull with rebase: You have unstaged changes`.

### Позитивний контроль сторожа: 11 випадків, матеріал справжній

`/tmp/sync_probe.py` ріже справжній лог по межах прогонів; `tg()` заглушена,
прапор відведений у `/tmp/syncprobe_*`, щоб не з'їсти тривогу справжнього cron.

| випадок | очікувано | результат |
|---|---|---|
| повний лог, остання доба ОК (а в історії 3 відмови) | **тиша** | ok, 0 тривог |
| лог по 25.08 — відмова, яку прогавили | тривога | 1, «cannot pull with rebase» |
| та сама відмова через 10 хв | 1 тривога всього | 0 (дедуплікація) |
| лог по 23.08 — протухлий токен | тривога | 1, «Invalid username or token» |
| 24.08: **та сама причина, інший прогін** | нова тривога | 1 |
| успіх після відмови | тиша + скидання прапора | ok, 0 |
| відмова після скидання | тривога знову | 1 |
| прогін впав із `Traceback`, слова FAILED немає | тривога | 1 |
| логу немає / лог порожній / хвіст без маркера | **тиша** | ok, 0 |

Чотири випадки з одинадцяти навмисно вимагають ТИШІ: перевірка, яка вміє лише
тривожити, така сама непридатна, як та, що вміє лише мовчати. Стара реалізація
прогнана поруч на тих самих трьох відмовах — `ok=True` тричі.

### Друга половина: причину відмови теж прибрано

Сторож помічає, але не виправляє — це було прямо сказано в постановці. Причина
25.08 не разова: `git pull --rebase` відмовляється, якщо в репозиторії є
незакомічені зміни, а о 07:15 вони там **норма**, а не збій — watchdog комітить
о :10 і :20, генератори пишуть між ними. Тобто кожен день був лотереєю.

`git_push()` у `agents/orders/rozetka_github_sync.py` перероблено, і кожна з
чотирьох змін закриває окремий спосіб не опублікувати прайс:

1. **Спершу `push`, і лише якщо його відкинуто — `pull --rebase --autostash`.**
   Раніше pull стояв беззастережно перед push і 25.08 завалив весь крок. У
   звичайний день pull узагалі не потрібен: у цей репозиторій пишемо ми й
   власник, розбіжність — виняток.
2. **`--autostash` у запасному pull:** чужа незакомічена робота не мусить
   блокувати публікацію цін.
3. **«nothing to commit» більше не означає «опубліковано».** На цьому стояв
   `return True`. Через нього непроштовхнутий учорашній коміт лишався лежати
   локально, а лог друкував `Git push: ✅ OK` — та сама брехня, від якої
   лікуємо сторожа, тільки з іншого боку.
4. **Успіх підтверджується вмістом, а не кодом виходу.** Після push робиться
   `git fetch origin main`, і blob файлу на диску звіряється з blob'ом у
   `FETCH_HEAD`. Це правило CLAUDE.md «фід зібрано ≠ фід опубліковано»,
   перенесене всередину самої публікації. Невдалий `fetch` — це WARNING, а не
   відмова: мережа буває, і тривога на кожен її збій знецінює лог.

### Позитивний контроль публікації: 8 випадків, дві реалізації поруч

`/tmp/push_probe.py` створює для кожного випадку **власний одноразовий
bare-репозиторій у `/tmp`**. Справжній репозиторій і справжній GitHub не
чіпаються, нічого не публікується. «Доїхав» означає: у гілці `main` того origin
лежить рівно той файл, що на диску — питання «фід опубліковано?», а не «команда
завершилась нулем».

| випадок | стара | нова |
|---|---|---|
| звичайний прогін, репо чисте | OK, доїхав | OK, доїхав |
| **чужі незакомічені зміни (відмова 25.08)** | FAIL, не доїхав | **OK, доїхав** |
| в origin з'явився чужий коміт | OK, доїхав | OK, доїхав |
| **origin попереду І чужа незакомічена робота** | FAIL, не доїхав | **OK, доїхав** |
| змін немає, але вчорашній коміт не доїхав | OK, **не доїхав ← бреше** | OK, доїхав |
| в origin чужий прайс, у нас змін немає | OK, **не доїхав ← бреше** | OK, доїхав |
| репо лишили на гілці `wip`, Rozetka читає `main` | OK, **не доїхав ← бреше** | **FAIL**, не доїхав |
| віддалений репозиторій недоступний | FAIL, не доїхав | FAIL, не доїхав |

Стара реалізація **бреше в трьох випадках із восьми** — повертає «опубліковано»,
коли в origin нічого не приїхало. Нова не бреше в жодному: там, де публікація
неможлива, вона каже FAIL і пише причину в лог, звідки її тепер бачить сторож.

**Випадок «гілка wip» — це і є позитивний контроль пункту 4.** Коміт і push
проходять успішно, код виходу нульовий, і тільки звірка вмісту показує, що в
`main` лежить старий файл. Без неї цей день теж минув би з `✅ OK`.

**Окремо перевірено, що `--autostash` не з'їдає чужу роботу**: у випадку
«origin попереду І чужа незакомічена робота» після прогону незакомічений рядок
на місці, слідів конфлікту немає, `git status` показує ту саму `M logs.txt`.

### Наявні перевірки не зламані, і код працює на сервері

| перевірка | до | після |
|---|---|---|
| cron | ok=True | ok=True |
| **sync** | ok=True «URL: https://raw.githubuser…» | ok=True «**2026-08-31 07:15 ok**» |
| noire | ok=True feed ok | ok=True feed ok |
| noire-rz | ok=True feed ok | ok=True feed ok |
| noire-prom | ok=True feed ok | ok=True feed ok |
| carvol-rz | ok=True feed ok | ok=True feed ok |

Тривог надіслано 0 в обох прогонах (`/tmp/wd_probe.py`, той самий інструмент,
що 25.08: `tg()` заглушена, прапори відведені, `check_git`/`check_services` не
викликаються — вони комітять і перезапускають сервіси). Рядок `sync` тепер
називає прогін замість того, щоб переказувати посилання.

Крок звірки вмісту прогнано **на справжньому репозиторії** (без push, лише
`fetch`): `FETCH_HEAD:data/carvol_rozetka.xml` = `8f060ff5…`, файл на диску =
`8f060ff5…` — збіг, тобто в бойовому стані нова перевірка мовчить.

І головне — перевірено не лише своїм інструментом: **справжній cron о 20:20
відпрацював із новим кодом**: `[sync] ok=True 2026-08-31 07:15 ok`, усі шість
перевірок `ok=True`, `Watchdog END`, жодного `Traceback` у всьому логу.

**Ще одна тиша, яку прибрано принагідно.** Текст тривоги йде в Telegram із
`parse_mode=HTML`, а причина береться з виводу git і цілком може містити `<`
(«fatal: repository &lt;origin&gt; not found»). Telegram відповідає на таке
HTTP 400, а `tg()` помилку ковтає — тобто тривоги просто не було б. Це той
самий різновид мовчання, який ця перевірка й лікує, тому причина тепер
екранується; контроль: рядок із `<origin>` і `&` дає в тексті `&lt;origin&gt;`
та `&amp;`, тривога 1.

### Стан публікації Carvol на 31.08

| Що | Значення |
|---|---:|
| локальний `data/carvol_rozetka.xml` | 40 650 648 Б, 07:15 |
| sha256 локального й опублікованого (raw, `identity`) | **збігається** (`9fd78e07…`) |
| blob на диску й у `origin/main` | **збігається** (`8f060ff5…`) |
| комміти по цьому шляху | 29, 30, 31.08 — щодня |
| провалених прогонів у логу | 3 з 14 (23, 24, 25.08) |
| `git push --dry-run` | «Everything up-to-date» — права робочі |

Тобто відтоді, як 25.08 знайшли розрив, публікація йде щодня. Це й робить
роботу вчасною, а не запізнілою: наступний збій буде названий у той самий
десятихвилинний цикл, а найчастіша його причина взагалі прибрана.

### Дотична проблема — записано, мовчки не бралось (правило 8)

**Шестигодинний звіт `send_report()` не містить жодного фіду.** Він приймає
`git_r, svc_r, cron_r, sync_r` — перевірок `noire`, `noire-rz`, `noire-prom` і
`carvol-rz` у ньому немає взагалі, тобто «Watchdog OK» кожні 6 годин нічого не
каже про чотири фіди. Миттєві тривоги по них працюють (кожна перевірка шле свою
власну), тож це не сліпота, а неповний звіт — але «OK», який не дивиться на те,
заради чого сторожа й писали, читається як більша обіцянка, ніж дає. Винесено в
чергу окремим пунктом і поставлено **в кінець**, після переміру карток
Єпіцентру: власна знахідка не має обганяти вже поставлену роботу.

### Стан і що не робилось

Змінено два файли, обидва розгорнуті на сервері (SKILL-22, рівень 2 — зворотне,
з доповіддю):

| файл | MD5 (ноутбук = сервер) | було | відкат |
|---|---|---|---|
| `tools/watchdog.py` | `3d46f3c7…` | `78042f89…` | `/tmp/watchdog.py.bak_20260831` |
| `agents/orders/rozetka_github_sync.py` | `29b07a5c…` | `8363011d…` | `/tmp/rozetka_github_sync.py.bak_20260831` |

**Нічого не публікувалось.** `rozetka_github_sync.py` жодного разу не
запускався — його нова `git_push()` перевірена виключно на одноразових
репозиторіях у `/tmp`. Єдиний дотик до мережі в бойовому репозиторії —
`git push --dry-run` і `git fetch`, обидва нічого не змінюють. Carvol — тільки
читання (правило 4 черги). NOIRE не торкався: `output/noire_rozetka.xml` 19:22
(`20 * * * *`), `noire_prom.xml` 19:40, `noire_epicentr_phase1.xml` 20:05,
`exports/carvol_epicentr.xml` 07:30, `data/carvol_rozetka.xml` 07:15 — усі рухи
від власного cron і всі до початку роботи (20:00–20:20). Фіди не перезбирались.

**Що зміниться завтра о 07:15 і про це варто знати заздалегідь.** Пуш, який
раніше падав, тепер проходитиме, а `git push` віддає в origin **усі** локальні
комміти гілки, не лише прайсовий. Тобто автокоміти watchdog (зокрема сьогоднішні
дві правки) поїдуть у GitHub самі. Це не нова поведінка — рівно так їх забирав
із собою пуш синхронізації Єпіцентру о 07:30, — але тепер це стане щоденним і
надійним. Якщо власник хоче лишити за собою рішення, що потрапляє в origin, це
окреме питання про `git push` у cron, а не про цю правку.

## 01.09.2026, автономний прогін черги

Узято перший `[ ]` — «Перезібрати фід TOPTUL: робота 25.08 у нього не
потрапила». Закрито `[x]`. Робота зроблена, але постановка була неправдива, і
саме це — головне, що варто записати.

### Постановка описувала копію на ноутбуці, а не фід на сервері

Перший замір, до будь-якої дії, — MD5 і дати на обох машинах:

| файл | ноутбук | сервер | MD5 |
|---|---|---|---|
| `tools/toptul_rozetka_generator.py` | 25.08 16:04 | 25.08 17:05 | `4039e25a…` збігається |
| `tools/toptul_filter_extract.py` | 25.08 12:09 | 25.08 16:31 | `27cc77a6…` збігається |
| `tools/rozetka_option_ids.py` | 25.08 12:06 | 25.08 16:31 | `a05f9ebb…` збігається |
| `data/rozetka_filter_values.json` | 25.08 15:37 | 25.08 16:31 | `5401802a…` збігається |
| `output/toptul_rozetka.xml` | **25.08 06:29** | **25.08 17:07** | різні |

Обидва числа з постановки («генератор 16:04», «фід 06:29») — з листингу
**ноутбука**, де фід справді старший за генератор. На сервері порядок
зворотний, і фід 17:07 зібраний уже правленим генератором.

Перевірено не датами, а вмістом — прямим пошуком у самому файлі (правило 8
черги: результат дивитись у ФІДІ):

| | ноутбук 06:29 | сервер 17:06 |
|---|---:|---:|
| `Розмір посадкового квадрата` | **0** | **1073** |
| `Кількість граней` | **0** | **678** |

1073 і 678 — рівно ті числа, які журнал 25.08 записав як результат
(`+1751 = 1073 + 678`). Тобто робота 25.08 у бойовий фід потрапила; не
потрапила вона лише в **застарілий локальний артефакт**, з якого й читали
постановку.

**Це та сама пастка, від якої застерігає правило CLAUDE.md «фід зібрано ≠ фід
опубліковано», тільки дзеркальна.** Там небезпека — повірити локальному
файлу як доказу, що на сервері добре. Тут — повірити локальному файлу як
доказу, що на сервері погано. Спільне: локальна копія не є твердженням про
сервер у жодний бік. Тому в кінці сесії копію синхронізовано (див. «Стан»),
щоб наступний прогін не поставив у чергу той самий неіснуючий дефект.

### Що зроблено по суті: перезбирання на свіжих даних

Хибна постановка не робить роботу зайвою — фід постачальника на сервері був
від **21.08 21:33**, тобто одинадцятиденної давнини, і всі заміри 25.08 йшли
по ньому. Виконано порядок із черги повністю.

**1. `toptul_filter_extract.py --selftest`** — позитивний контроль перед
роботою, без мережі й БД: **21 випадок, із них 6 із порожнім очікуванням,
0 збоїв.** Порожні випадки тут не формальність: серед них TORX і
п'ятигранник (грані не застосовні), «500 гр.» (вага, не грані), категорія
поза довідником. Перевірка, яка вміє лише знаходити, нічого не варта.

**2. Свіжий фід постачальника.** `toptul.online/products_feed.xml` →
`/tmp/toptul.xml` (38 489 987 Б, дата в шапці `2026-09-01 06:31`). Стару
копію збережено в `/tmp/toptul_20260821.xml.bak` — новий файл не затирав
єдине джерело до того, як його перевірено. Перевірка не за розміром:
`ET.parse` проходить, корінь `yml_catalog`, **6904 оффери** (було 6895),
`available="true"` — 5636, файл закривається `</yml_catalog>`.

**3. Генератор.** `resolve_fields()` дав ті самі ролі, що й раніше
(`name_ua`, `description_ua`, `vendorCode`, `vendor`), тобто постачальник
теги не перейменував. Довідник фільтрів піднявся: «4 категорії, Кількість
граней, Розмір посадкового квадрата».

**4. Валідатор.** `rozetka_xml_validator.py --no-xlsx`: **5520 офферів,
0 помилок, 0 попереджень, 100% валідних.**

### Критерій черги — усі три частини, кожна числом

Заміряно **тим самим інструментом на тому самому кеші довідників** по трьох
фідах, а не проти старого звіту: `docs/rozetka_option_gaps.json`
перезаписується кожним прогоном (застереження від 25.08), тому копії
відкладались у `/tmp/gaps_A|B|D.json`.

| фід | категорій | офферів | різних відсутніх фільтрів | вага | квадрат | грані |
|---|---:|---:|---:|---:|---:|---:|
| A — ноутбук 25.08 06:23 (постановка) | 132 | 5512 | 296 | 23075 | **1267** | **943** |
| B — сервер 25.08 17:06 | 132 | 5512 | 283 | 19920 | **93** | **0** |
| D — новий 01.09 06:36 | 132 | 5520 | 283 | 19952 | **93** | **0** |

* **обидві характеристики у фіді** — 1073 і 678 входжень;
* **валідатор** — 0 помилок, 0 попереджень;
* **`rozetka_category_options.py`** — обидва рядки зникли з переліку
  відсутніх фільтрів (A→D: −1174 і −943 ваги).

**Чому 93 мають лишитись і чому це добре.** Вага рахується за категорією.
93 — «Тріскачні ключі», усі 93 позиції ріжково-накидні й комбіновані на
міліметри, посадкового квадрата в них немає за суттю товару. Підставити
щось означало б вигадати значення (правило 5). Заразом цей залишок є
**позитивним контролем самого заміру**: він і далі повертає ненуль, тобто
інструмент не осліп на цих двох рядках.

**Різницю A vs B пояснено механічно, а не «схоже, все добре».** Різних
відсутніх фільтрів не 284→283, як писав журнал 25.08, а 296→283 — тобто
зникло 13 рядків, не один. Перелічено поіменно: `Кількість граней` (943),
`Тип викрутки` (229), `Діаметр форсунки` (205), `Тип біти` (183),
`Кількість відділень/кишень` (88), `Вид різі` (84), `Крок різі` (84),
`Тип світлодіодів` (40), `Клас лазера` (19), `Тип акумулятора` (12),
`Зусилля притискання пластиру` (7), `Температура нагрівання` (7),
`Тип панелі` (2). Тобто між збіркою 06:23 і 17:06 у генератор увійшли не
лише два фільтри, а й інша робота дня; журнальне «змінилось рівно два рядки
з 284» міряло проти іншої бази (`/tmp/toptul_rozetka_before.xml`), ніж
локальна ранкова копія.

**B vs D — побічної шкоди немає.** Жодного НОВОГО відсутнього фільтра не
з'явилось (множина `D − A` порожня), вага +32 і розкладається на 8 нових
офферів: `Теги +8`, `Вид +4`, три атрибути зарядних `+4`, решта по 1–2.

### Знайдено принагідно й виправлено: свіжі дані принесли російські назви

Перший прогін генератора на новому фіді дав у самоперевірці **не нуль**:
`назв характеристик російською: 3 різних, 6 вживань` — «Количество
заряжаемых аккумуляторов» (4), «Вид кухонных весов» (1), «Точность
измерения» (1). Це не регрес коду: постачальник додав нові товари з новими
характеристиками, яких у `toptul_translation` не було.

Виправлено наявним шляхом (`toptul_translate_load.py`), а не новим кодом.
Три його перевірки не декларативні й спрацювали: `--dry` підтвердив, що всі
три `src` є у фіді й жоден `dst` не впізнається строгою `RU`, і окремо
попередив про слово поза обома словниками — `ваг` у «Вид кухонних ваг».
Слово правильне (родовий множини від «ваги»), це прогалина українського
словника, не помилка перекладу; запис лишено.

**Після завантаження фід перезібрано — і саме це є суть цього пункту
черги.** Запис у БД без перезбирання дав би рівно той стан, який постановка
помилково приписувала серверу: виправлено, але не у фіді. Після
перезбирання самоперевірка: `назв товарів 0`, `назв характеристик 0`,
`значень 0`.

**Незалежне підтвердження, окремим інструментом.** `toptul_ru_audit.py`
читає XML напряму й не є частиною генератора: `0/0, 0/0, 0/0` і фінальне
«ЗБІГ: словникова ознака знаходить усе, що знаходить перелік». Нуль тут не
голослівний — той самий аудит на тому самому файлі показує **478 оферів із
російськими описами, 1206 різних слів, 4651 вживання** (`description_ua`),
тобто перевірка на цьому ж фіді вміє повертати великий ненуль. Це і є
матеріал наступного пункту черги, і його число збіглося з постановкою до
одиниці.

### Заміри

| Що | 25.08 (сервер) | 01.09 |
|---|---:|---:|
| Фід постачальника | 6895 оферів, 21.08 21:33 | **6904**, 01.09 06:31 |
| Офферів у фіді Rozetka | 5512 | **5520** |
| Тегів `param` | 45488 | **45533** |
| Розмір посадкового квадрата / Кількість граней | 1073 / 678 | **1073 / 678** |
| Валідатор | 0 / 0, 100% | **0 помилок, 0 попереджень, 100%** |
| Вага відсутніх фільтрів | 19920 | 19952 |
| Назви товарів / назв характеристик / значень російською | 0 / 0 / 0 | **0 / 0 / 0** |
| Непізнані слова (суржик) | 168 / 3441 | 170 / 3444 |
| Описи `description_ua` російською | 478 оферів | **478 оферів** (пункт 2 черги) |
| `toptul_translation`, назви | 687 | **690** |
| Пропущено: менше 3 характеристик | 1178 | **1179** |
| Пропущено: категорія без rz_id / review | 205 | 128 + 77 = **205** |

### Дотичне — записано, мовчки не бралось (правило 9)

1. **`toptul_ru_audit.py` не має прапорця `--feed`**, шлях подається
   позиційно, хоч сусідні інструменти беруть саме `--feed`. Дрібниця, але
   вона коштувала одного помилкового виклику; уніфікація — окрема правка.
2. **Два значення характеристик постачальника лишились без перекладу** —
   `кантер` (1) і `Минеральное` (1). У наш фід вони не потрапляють (заміри
   генератора й аудитора дають 0), тому не чіпав: додавати переклад рядка,
   якого у фіді Rozetka немає, означало б робити таблицю «повнішою» без
   наслідку.
3. **Непізнаних слів стало на 2 більше** (168→170, 3441→3444) — тим самим
   свіжим товаром. Кошик суржику й далі друкується поруч із нулями, тому
   «0 російських» неможливо прочитати як «фід чистий»: у ньому «відвертка»
   (133), «рожковий» (76), «удлінитель» (56). Це матеріал окремої роботи,
   і його немає в черзі.

### Стан і що не робилось

**Нічого не публікувалось.** Перевірено не на віру: у crontab сервера немає
жодного запису, який читає або пише `output/toptul_rozetka.xml` — там лише
`rozetka_github_sync` (`data/carvol_rozetka.xml`), `noire_stock_sync`
(`--publish-rozetka` / `--publish-prom`), `feed_sync`,
`carvol_epicentr_sync`, `price_updater`, `watchdog` і місячний `git gc`.

NOIRE не торкався, Carvol — тільки читання (правило 4): `noire_rozetka.xml`
06:22 (`20 * * * *`), `noire_epicentr_phase1.xml` 06:05, `noire_prom.xml`
31.08 19:40, `data/carvol_rozetka.xml` 31.08 07:15, `exports/carvol_epicentr.xml`
31.08 07:30 — усі рухи від власного cron і всі до початку роботи
(06:32–06:40).

**Локальну копію синхронізовано з сервером** — і це не косметика, а закриття
пастки, яка й породила хибну постановку: `output/toptul_rozetka.xml`
sha256 `f1081659…` ноутбук = сервер, 21 957 051 Б. Разом із ним
`docs/rozetka_option_gaps.json` (звіт D).

Змінених інструментів **немає** — жодного рядка коду цієї сесії. Змінилось:
фід (перезібрано), `toptul_translation` (+3 пари, у PostgreSQL, тобто
переживе перезбирання), `docs/rozetka_option_gaps.json` (звіт).
Копії для звірки на сервері: `/tmp/toptul_rz_A_laptop0825.xml`,
`/tmp/toptul_rz_B_server0825.xml`, `/tmp/gaps_A|B|C|D.json`,
`/tmp/toptul_20260821.xml.bak`, `/tmp/tr_0901.tsv`.

**Потрібне від власника: `git push`** — вимога та сама, що й у попередніх
записах. На сервері нове лежить у локальних комітах watchdog
(останній `f07e2b0`), origin про них не знає. Терміновості 26.08 більше
немає (`git reset --hard` скасовано комітом `b57f88e`), але
`tools/toptul_filter_extract.py`, `rozetka_option_ids.py` і
`data/rozetka_filter_values.json` досі існують лише поза git.

## 01.09.2026, другий автономний прогін черги

Узято перший `[ ]` — «Перекласти російські описи TOPTUL через NVIDIA».
Закрито `[x]`: критерій виконано і на ноутбуці, і на сервері.

### Перше, що показав замір: більшу частину вже зробив прогін, якого немає в журналі

До будь-якої дії — стан інструментів і БД. `tools/toptul_desc_translate.py`
(1392 рядки) не існує в git, датований **сьогодні 12:39**; правки в
`toptul_rozetka_generator.py` (11:49), `uk_lexicon_build.py` (11:52),
новий `data/toptul_uk_fixes.tsv`. У БД — таблиця `toptul_desc_translation`
на **719 речень**, записи від 04:07 до 11:54 UTC. Тобто між ранковим
записом і цією сесією пройшов прогін, який зробив основну роботу і **не
залишив жодного рядка в журналі**, а пункт черги лишився `[ ]`.

Це той самий дефект, що вже траплявся 25.08 («між двома записами був
прогін, який нікуди не записався»), тільки цього разу він коштував не
подвійної роботи, а невидимості: за журналом судити про стан було
неможливо, довелось виводити його з файлової системи й БД.

Тому цей запис описує і чужу зроблену частину (з коду й замірів, а не з
переказу), і те, що зроблено тут.

### Що вже було зроблено до цієї сесії

`tools/toptul_desc_translate.py` перекладає **по реченнях, а не по
картках**, і причина в розподілі: з 433 описів із російськими словами
**287 мають лише 1–2 російські слова** серед сотень українських. Речення —
найбільша одиниця, яку можна замінити, не чіпаючи сусідніх; решта 2328
речень лишаються байт у байт за побудовою. Прив'язка `оригінал |||
переклад` — та сама, якою 21.08 лікували зсув: приймається лише пара, ліва
частина якої дослівно збігається з надісланим рядком.

Окрема таблиця, а не `kind='desc'` у `toptul_translation`: там
`PRIMARY KEY (kind, src)`, а найдовше речення — 2009 символів кирилиці,
тобто ~4 КБ, і btree-індекс Postgres такого не приймає (межа 2704 Б). Ключ
— sha256, текст лежить полем.

Колонка `raw` (відповідь моделі ДО словника виправлень) — не архів, а
позитивний контроль: виправлення накладаються при записі, тож у `dst` їх
не буде за побудовою, і нуль сам собою нічого не доводив би.

### Що зроблено в цій сесії

**1. Розібрано залишок 29 вживань.** Аудит по фіду 11:49 давав 10 оферів /
12 різних слів / 29 вживань. Розкладено поіменно, а не «майже нуль»:

* **23 вживання — фід був на 5 хвилин старший за БД.** Речення «средств по
  диагностике и спец.», «ремонту колесного автотранспорта.» і довге
  «Наверно каждый третий человек…» уже мали переклад у таблиці (запис
  11:54), а фід зібрано об 11:49. Рівно та пастка, від якої застерігає
  правило 8 черги, тільки в мініатюрі: у БД виправлено, у фіді ні.
* **3 вживання — `ванадия`** у єдиному рядку з `ru_left>0`. Закрито
  наявним шляхом `--polish` (заміна ПО СЛОВАХ, решта речення байт у байт).
  Словоформа вже була в `toptul_translation kind='dword'`, тож **мережа не
  знадобилась узагалі: жодного запиту до NVIDIA цієї сесії не зроблено.**
* **6 вживань — не російські слова.** Це «cили» з **латинською** `c`
  (U+0063): токенізатор бачить «или» і лексикон чесно каже «російське».
  Перевірено покодово в шести оферах — усюди `c:0063 и:0438 л:043B и:0438`.
  Перекладом це не лікується, бо перекладати нічого: слово українське.

**2. Перезібрано фід і переміряно.** Далі — деплой на сервер і повторний
замір там (правило 6 черги).

### Критерій черги — обидві частини, числами

Замір **тим самим інструментом** по фіду до і після, а не проти старого
звіту. «До» — `/tmp/toptul_rz_before_desc.xml` (11:43, ще без перекладу
описів), «після» — свіжозібраний фід:

| `toptul_ru_audit.py`, `description_ua` | до | після |
|---|---:|---:|
| оферів із російськими словами | 478 | **6** |
| різних російських слів | 1217 | **1** |
| вживань | **4684** | **6** |
| з них справді російських | 4684 | **0** |

Постановка називала 4651 — різниця в 33 від того, що лексикон відтоді
виріс на 172 слова; міряно тим самим кодом на обох файлах, тому пара чисел
зіставна між собою.

**Друга частина критерію — хибні українські форми — нуль**, і нуль тут не
голослівний. `data/toptul_uk_fixes.tsv` (8 правил) під час збірки
спрацював **43 рази**: у назвах `бита` 20 і `відвертк*` 1, в описах `бита`
11 і `відвертк*` 1, у характеристиках `бита` 10. Після цього замір по
рядках, які пішли у файл, дає `0 різних, 0 вживань`.

Що перевірка вміє повертати ненуль — доведено окремо, на `raw`-відповідях
моделі: там ті самі правила знаходять **14 вживань** (`вікрутк*` 8,
`відвертк*` 6). Тобто модель справді помиляється в українській, як і
попереджала постановка, а нуль у фіді — наслідок виправлення, не сліпоти.

### Заміри

| Що | 01.09 ранок | 01.09 вечір |
|---|---:|---:|
| Описи `description_ua` російською | 478 оферів / 4684 вживання | **6 оферів / 6 вживань (0 справжніх)** |
| Назви товарів / характеристик / значень | 0 / 0 / 0 | **0 / 0 / 0** |
| Хибні українські форми у фіді | не мірялось | **0 різних, 0 вживань** |
| Непізнані слова (суржик) | 170 різних / 3444 | **166 / 3252** |
| Офферів / валідатор | 5520 / 0 / 0 / 100% | **5520 / 0 помилок, 0 попереджень, 100%** |
| `toptul_desc_translation` | — | **719 речень, 1037 вживань, `ru_left` 0** |
| `toptul_translation kind='dword'` | — | **290 словоформ** |
| Речень підмінено при збірці | — | **1037 у 472 оферах** |

Числа генератора й незалежного `toptul_ru_audit.py` збіглися до одиниці на
обох машинах, і аудит завершився кодом 0 із «ЗБІГ: словникова ознака
знаходить усе, що знаходить перелік».

### Розгорнуто на сервері, і фід звірено вмістом

П'ять файлів (`toptul_desc_translate.py` — новий; `toptul_rozetka_generator.py`,
`uk_lexicon_build.py`, `data/toptul_ru_lexicon.txt` — змінені;
`data/toptul_uk_fixes.tsv` — новий). MD5 ноутбук = сервер по всіх п'яти.
Копії попередніх версій — `/tmp/*.bak_20260901`, фід до правки —
`/tmp/toptul_rz_server_before_desc.xml`.

Фід постачальника на обох машинах **той самий файл** (`e90d3c9d…`), тому
збірки зіставні. Після перезбирання на сервері:

| | ноутбук | сервер |
|---|---|---|
| офферів | 5520 | 5520 |
| описи російською | 6 оферів / 6 | 6 оферів / 6 |
| валідатор | 0 / 0, 100% | 0 / 0, 100% |
| sha256 файлу | `f66ca99f…` | `41404c8d…` |
| **sha256 без шапки з датою** | **`8f83bd37…`** | **`8f83bd37…`** |

Різниця повних хешів — рівно рядок `<yml_catalog date=…>` (14:35 CEST проти
15:36 EEST). Тіло фіду збігається побайтово, і це сильніший доказ повного
розгортання, ніж збіг MD5 самих інструментів.

### Дотичне — записано, мовчки не бралось (правило 9)

**1. Мішане написання (гомогліфи) у 261 картці.** Виміряно, бо саме воно
дає останні 6 «російських» вживань: у `description_ua` **17 752 вживання**
слів, де частина літер латинські (`тa` 1636, `дoзвoляє` 290, `нaпpугa` 170),
**4140 різних слів**, 261 офер із 5520; у назвах — 105 вживань. Це не наша
робота й не наслідок перекладу — так лежить у фіді постачальника. Наслідок
подвійний: Rozetka такий текст пошуком не знайде, а наша ознака мови
приречена бачити в ньому російське. Поставлено в чергу окремим пунктом і в
**кінець** — власна знахідка не обганяє поставлену роботу.

**2. Залишок у перекладах: 269 речень, 290 різних слів, 662 вживання.** Це
слова оригіналу, що пережили переклад і невідомі українському словнику.
Частина справді російська («верхнее» 22, «покраски» 14, «краски» 13,
«тончайших» 9, «изготовлен» 8), частина — українські терміни, яких немає в
словнику («бокорізи», «круглогубці», «вороток»).

**Готовий прохід `--redo-residue` свідомо не запускався**, і це рішення, а
не забудькуватість. Він переписує речення цілком і приймає відповідь за
умовою «залишок став меншим» — а для правильного українського «вороток» ця
умова виконається заміною на будь-яке слово зі словника, тобто інструмент
зіпсує те, що чинити не треба. Безпечний шлях інший: спершу розсортувати ці
290 слів на російські й українські (перші — у лексикон, другі — ні), і аж
тоді дочищати `--polish`, який міняє РІВНО одне слово. Це та сама робота,
що вже названа в журналі як «кошик суржику», і вона досі не в черзі.

**3. Незавершений `git rebase` на сервері вбив watchdog.** У
`~/agent-system` триває інтерактивний rebase на `1bbef26` («Зведення
гілок»), і в `tools/watchdog.py` лишились маркери конфлікту. Кожен запуск
за cron падає з `SyntaxError` — **51 падіння, останній живий прогін о
07:10**, тобто нагляду немає вже вісім годин: ні тривог, ні шестигодинних
звітів.

Сам конфлікт однозначний — рядок `import re` (потрібен: `re.sub` у рядку
560), і файл із прибраними маркерами **дослівно дорівнює версії ноутбука**
(MD5 `8e3ccacd…`, 695 рядків). Попри це не чіпав, і причина конкретна:
доки rebase не завершено, HEAD відчеплений, а перший же оживлений watchdog
зробить `git add -A && git commit` **у цей стан**. Полагодити лише файл —
гірше, ніж лишити мертвим. Готове рішення лежить на сервері як
`/tmp/watchdog_resolved_20260901.py`, точні команди — у черзі, в розділі
питань власникові.

Публікації це не зачепило: `noire_stock_sync` і `rozetka_github_sync` мають
власні записи cron і працюють (`noire_rozetka.xml` 15:22, `carvol_rozetka.xml`
07:16). Мертвий саме нагляд.

### Стан і що не робилось

**Нічого не публікувалось.** У crontab сервера **жодного запису зі словом
`toptul`** (перевірено `crontab -l | grep -c toptul` → 0), а `*.xml` у
`.gitignore`, тож `output/toptul_rozetka.xml` не може поїхати в origin і
автокомітом.

NOIRE не торкався, Carvol — тільки читання (правило 4). Усі рухи чужих
фідів — від власного cron: `noire_rozetka.xml` 15:22 (`20 * * * *`),
`noire_epicentr_phase1.xml` 14:05, `noire_prom.xml` 11:40 (`40 7,11,15,19`),
`data/carvol_rozetka.xml` 07:16, `exports/carvol_epicentr.xml` 07:30. Робота
йшла 15:36–15:40 за часом сервера.

Мережа використовувалась лише для `ssh`/`scp` на сервер. **До NVIDIA не
було жодного запиту** — залишок закрився словоформою, яка вже лежала в БД.

**Потрібне від власника:**
1. завершити або скасувати rebase на сервері (команди в черзі) — доти
   немає нагляду;
2. `git push` — вимога та сама, що й у попередніх записах. Нове на
   ноутбуці не закомічено (push робить власник), на сервері не може бути
   закомічене взагалі, доки триває rebase.

## 01.09.2026, третій автономний прогін черги

Узято перший `[ ]` — «переміряти 1179 товарів, що випадають через менше 3
характеристик». Закрито `[x]`.

### Постановка підтвердилась повністю, і вперше без поправок

Замір до будь-якої дії, на сервері й тим самим генератором, яким збирається
фід: `пропущено: менше 3 характеристик: 1179`, офферів 5520. Числа збіглися
з постановкою до одиниці, тобто цього разу локальна копія не брехала —
`md5 tools/toptul_rozetka_generator.py` ноутбук = сервер (`4d2d7c48…`) ще до
початку роботи.

Дефект був саме там, де його назвала постановка: `if len(prm) < MIN_PARAMS`
стояв одразу за `collect_params()`, а видобування фільтрів — на 27 рядків
нижче. Оффер відсіювався ДО того, як міг би отримати «Робочий розмір», «Вид»
чи «Розмір посадкового квадрата», хоча коментар у блоці фільтрів стверджував
протилежне — що `MIN_PARAMS` бачить ці характеристики «так само, як усі
інші». Роботи 25.08 і 01.09 ці 1179 не торкнулись жодного разу.

### Що зроблено

**Перестановка, а не послаблення порога.** Новий порядок: характеристики
постачальника → назва → фільтри з назви й характеристик → перевірка
мінімуму. Назва мусить будуватись до перевірки, бо вона ж є запасним
джерелом значень для фільтрів; тому перед перевіркою лишились тільки ті
відсіви («без назви», «назва коротша 5»), які від кількості характеристик не
залежать.

**Лічильники не поїхали разом із кодом.** Усе, що рахувалось між назвою і
перевіркою (переклад назви, виправлення хибних форм, додані фільтри), тепер
накопичується в `pending` і потрапляє в `stats` лише для оффера, який пройшов
перевірку. Без цього числа «виправлень» почали б описувати не те, що пішло у
файл, — а саме на їх збіг із незалежним аудитом ми спираємось.

**Новий лічильник `фільтри врятували оффер від MIN_PARAMS`** — це не
прикраса: він мусить дорівнювати приросту офферів, інакше або перестановка
не спрацювала, або приріст має іншу причину. Дав **263** при прирості
5520 → 5783, тобто рівно 263.

**Прапорець `--drops FILE`** — перелік відсіяних із причиною, якого вимагає
критерій пункту. Зроблено в самому генераторі, а не окремим скриптом,
свідомо: другий код із власною копією правил розійшовся б із генератором і
описував би не той фід — та сама причина, з якої ознака російської
імпортується, а не пишеться вдруге.

### Знайдено при перевірці: повернуті товари принесли з собою російську

Перший же прогін після перестановки дав у самоперевірці **не нуль**:
11 назв товарів і 18 описів із російськими словами (72 вживання) проти
0 і 6 до змін. Це не регрес перекладу — це його межа: перекладачі описів і
назв відбирають рядки **з нашого фіду** (`collect()` читає
`output/toptul_rozetka.xml`), а ці 263 оффери у фіді ніколи не були, тому їх
ніхто й не перекладав. Тобто «фід чистий» було правдою про 5520 карток і
переставало бути правдою рівно тієї миті, коли їх стало 5783.

Закрито наявними шляхами, без нового коду:
* **описи** — `toptul_desc_translate.py --run`: 12 нових речень, 9 записано,
  3 відхилено запобіжником «латиниця не збігається». Ці 3 — гомогліфні
  («Bін є нeoбxідним…», латинські `B`, `o`, `x` усередині кириличних слів);
  модель нормалізувала їх у кирилицю, і запобіжник чесно відмовився приймати
  відповідь, у якій латиниця не така, як у вході. Це матеріал останнього
  пункту черги, не цього;
* **залишки в перекладах** — `--polish`: 3 речення з `ru_left=1`
  («Ремонтний комплект для **трещоток**»), 2 нові словоформи, після прогону
  рядків із залишками **0**;
* **назви** — 11 штук через `toptul_translate_export/load`, без мережі:
  «краскопультов» → «фарбопультів», «трещоток» → «тріскачок», «магнитная» →
  «магнітна», «диаметр» → «діаметр». Обидві жорсткі перевірки завантажувача
  пройдено (усі 11 `src` є у фіді, жоден `dst` не впізнається строгою `RU`);
  попередження було лише про слова поза обома словниками — «фарбопультів»
  і «ремкомплект», обидва вже вживані в каталозі.

Після цього самоперевірка й незалежний `toptul_ru_audit.py` знову дають
**0 / 0 / 0**, а описи — ті самі 6 вживань `или` з гомогліфа `cили`, що й
до змін.

### Заміри

| Що | до | після |
|---|---:|---:|
| Пропущено: менше 3 характеристик | **1179** | **916** |
| Офферів у фіді | 5520 | **5783** |
| `фільтри врятували оффер від MIN_PARAMS` | — | **263** |
| Валідатор | 0 / 0, 100% | **0 помилок, 0 попереджень, 100%** |
| Назви / назви характеристик / значення російською | 0 / 0 / 0 | **0 / 0 / 0** |
| Описи `description_ua` російською | 6 оферів / 6 | **6 оферів / 6** (той самий гомогліф) |
| Хибні українські форми | 0 | **0** |
| Дублікатів назв | 0 | **0** |
| Вага відсутніх фільтрів | 15469 | 15879 |
| Різних відсутніх фільтрів | 283 | **282** |
| `toptul_desc_translation` | 719 речень | **728**, `ru_left` 0 |
| `toptul_translation kind='title'` | +0 | **+11 пар** |

**Приріст ваги відсутніх фільтрів пояснено, а не списано на «більше
товарів».** Нових рядків у переліку **нуль** (множина «після мінус до»
порожня), +410 розкладаються на `Теги +223`, `Об'єм контейнера +222`,
`теги +28`, `Кількість зубців +25` — тобто на категорії, куди прийшли
повернуті оффери. Один рядок навпаки **зник**: «Розмір посадкового квадрата»
(−93) — ті самі 93 «Тріскачні ключі», які журнал 01.09 назвав залишком «за
суттю товару». Тепер у цій категорії характеристика є, бо разом із
поверненими офферами в неї потрапили позиції з квадратом.

### Позитивний контроль

**Твердження «0 попереджень на 5783 офферах» перевірено на здатність
спрацювати.** Валідатор попереджає при `<3` тегів `<param>` (рядок 300), і
саме на це спирається висновок, що всі 263 повернуті оффери справді мають
три характеристики. Побудовано файл із 5 офферів, у першого зрізано
характеристики до двох: **4 чисті → 0 попереджень, зіпсований → рівно 1**.
Без цього «0 попереджень» було б твердженням про мовчазну перевірку.

**Перелік відсіяних теж має власний позитивний контроль.** Серед 916 є
**71 оффер, який фільтр таки отримав** (1 характеристика → 2, усе одно
менше трьох). Це доводить, що видобування фільтрів тепер доходить до
відсіяних, а не лише до тих, хто виживає, — інакше перестановки могло б і
не статись, а приріст пояснювався б чимось іншим.

**Числа ноутбука й сервера збіглися до одиниці** (5783 / 916 / 263), а
перелік відсіяних — побайтово (`diff` без різниць).

### Чому саме 916 лишились — з даних, а не словами

| причина | товарів |
|---|---:|
| постачальник не дав жодної характеристики (лише «Бренд») | 491 |
| постачальник дав одну характеристику (+ «Бренд») | 354 |
| фільтр додано, але характеристик усе одно 2 | 71 |

74 категорії, найбільші — «Знімачі» 226, «Фарбопульти» 69, «Інструмент для
змащувальних матеріалів» 65, «Рихтувальне обладнання» 44. Значення не
вигадувались (правило 5): у постачальника їх немає ані в характеристиках,
ані в назві. Перелік власникові — `docs/toptul_minparams_drops.md` (звід за
категоріями й причинами) і `docs/toptul_minparams_drops.tsv` (усі 916 SKU з
набором характеристик і назвою).

### Дотичне — записано, мовчки не бралось (правило 9)

**Наш відсів суворіший за вимогу Rozetka на 425 товарів.** Генератор
дописує тег «Країна-виробник товару» кожному офферу, у якого своєї країни
немає, — але робить це ПІСЛЯ перевірки, і в `MIN_PARAMS` країна не
рахується. Перевірено по переліку відсіяних: країну отримали б **усі 916**,
а тих, у кого рівно 2 характеристики, — **425**. Тобто у файлі в них було б
три теги `<param>`, і валідатор (та сама перевірка, що дала попередження в
позитивному контролі вище) не сказав би нічого. Порахувати країну означало
б повернути 425 карток, де по суті одна справжня характеристика плюс бренд
і країна. Це рішення про якість проти кількості, тому винесено в питання
власникові, а не зроблено.

### Стан і що не робилось

**Нічого не публікувалось.** У crontab сервера немає жодного запису зі
словом `toptul` — фід `output/toptul_rozetka.xml` нікуди не їде сам.
NOIRE не торкався, Carvol — тільки читання (правило 4).

**Розгорнуто на сервері й звірено вмістом**, а не датами:

| | ноутбук | сервер |
|---|---|---|
| `md5 toptul_rozetka_generator.py` | `0b31ddd2…` | `0b31ddd2…` |
| офферів / відсіяних | 5783 / 916 | 5783 / 916 |
| валідатор | 0 / 0, 100% | 0 / 0, 100% |
| **sha256 фіду без рядка з датою** | **`d34c1e52…`** | **`d34c1e52…`** |

Змінено один файл — `tools/toptul_rozetka_generator.py` (перестановка,
`pending`, `--drops`, коментарі). Копії попередніх версій:
`tools/toptul_rozetka_generator.py.bak_20260901_minparams` на ноутбуці,
`/tmp/gen_old.py.bak_20260901` на сервері. Фіди до змін —
`/tmp/toptul_rz_before_minparams.xml` (сервер),
`/tmp/toptul_rz_laptop_before_minparams.xml` (ноутбук); звіти фільтрів —
`/tmp/gaps_E_before.json`, `/tmp/gaps_F_after.json`, `/tmp/gaps_G_final.json`.
Мережа — `ssh`/`scp` і NVIDIA (12 речень, ключ лише на ноутбуці).

**Потрібне від власника:** те саме, що й у попередніх записах — `git push`,
і завершити або скасувати rebase на сервері (доти немає нагляду watchdog).
Нове цієї сесії: `docs/toptul_minparams_drops.md` і `.tsv`.

## 01.09.2026, четвертий автономний прогін черги

Узято перший `[ ]` — «шестигодинний звіт watchdog нічого не каже про фіди».
Закрито `[x]`; на сервері не активовано свідомо, причина — нижче.

### Постановка підтвердилась заміром, а не читанням коду

Перед правкою знято знімок звіту на шести сценаріях
(`/tmp/wd_report_probe.py`, вивід — `/tmp/wd_report_before.txt`). Два з них і
є суть дефекту:

| сценарій | звіт ДО правки |
|---|---|
| фід Rozetka відстав на 9 год, решта гаразд | `🔔 Watchdog OK` |
| публікація Carvol не доїхала до GitHub | `🔔 Watchdog OK` |

Тобто звіт не просто мовчав про фіди — він **стверджував «OK» про годину, коли
фід був мертвий**. Це той самий клас, що й правило 8 черги («перевіряти
результат у фіді, а не в коді»), тільки з боку нагляду.

Друга половина дефекту, якої в постановці не було: негайні тривоги, на які
звіт покладався, надсилаються **один раз на несправність** — дедуплікація по
файлу-прапорцю (`NOIRE_RZ_FLAG` і сусідні) порівнює новий текст із записаним і
мовчить, доки він не змінився. Пропустив сповіщення о 03:00 — і наступні шість
годин система про це не нагадає жодним словом. Тому рядок у звіті потрібен
навіть тоді, коли тривога вже пішла.

### Що зроблено

`send_report()` приймає ще чотири аргументи — результати `check_noire_feed`,
`check_noire_rozetka_feed`, `check_noire_prom_feed`, `check_carvol_rozetka_feed`
— і друкує блок «Фіди» з рядком по кожному. Три рішення, кожне з причиною:

1. **Рядок друкується завжди, і в доброму звіті теж.** Перелік лише проблемних
   читався б як «решта гаразд», хоча означав би ще й «перевірка не
   відпрацювала»: зниклий рядок видно, зниклу проблему — ні.
2. **Фіди входять у загальний присуд** (`all_ok`), а не дописуються збоку.
   Інакше заголовок лишився б «Watchdog OK» над `❌ Rozetka NOIRE` — гірше за
   мовчання, бо власник читає саме заголовок.
3. **Нових перевірок не додано.** Звіт бере ті самі об'єкти, які `main()` уже
   отримує від чотирьох наявних перевірок; мережа при звіті не чіпається, бо
   всі запити вже зроблено вище по коду.

### Позитивний контроль — на обидва боки

**Що перевірка вміє повернути ненуль**, показано на сценаріях D і E: єдина
несправність — фід, і звіт перевернувся з `🔔 OK` на `🚨` з іменем фіду й
причиною. **Що вміє повернути нуль** — сценарій A: чотири `✅`, заголовок «OK».

**Наскрізний прогін зі СПРАВЖНІМИ перевірками** (`/tmp/wd_report_live.py`), бо
синтетичні словники доводять лише малювання рядків. Викликано самі
`check_*_feed()`; на ноутбуці шляхи серверні, тож усі чотири чесно повернули
«локального фіду немає», і рівно ці тексти опинились у звіті. Заразом
підтвердилось, що опубліковані фіди доступні: жодна перевірка не додала
«raw-URL недоступний».

**Не зламано наявне** — вимога постановки «прогін до змін і після». Не-фідова
частина всіх шести звітів **побайтово та сама**, крім тих самих D і E, де
зміна й потрібна. `git diff` торкнувся трьох місць: рядок docstring,
`send_report()`, виклик у `main()` (+43/−6). Жодна з чотирьох перевірок фідів,
`probe_raw()` і `published_age_h()` не змінені. Інших викликів `send_report()`
у репозиторії немає (перевірено пошуком по всіх `*.py`).

### Заміри

| Сценарій | ДО | ПІСЛЯ |
|---|---|---|
| усе гаразд | `🔔 OK`, 0 рядків про фіди | `🔔 OK`, **4 рядки ✅** |
| сервіс упав + git | `🚨`, 0 рядків про фіди | `🚨`, ті самі проблеми + **4 рядки** |
| cron + sync | `🚨`, 0 рядків про фіди | `🚨`, ті самі проблеми + **4 рядки** |
| **лише фід Rozetka відстав** | **`🔔 Watchdog OK`** | **`🚨` + `❌ Rozetka NOIRE (не оновлювався 9 год)`** |
| **лише публікація Carvol не доїхала** | **`🔔 Watchdog OK`** | **`🚨` + `❌ Carvol → Rozetka (…push не доїхав)`** |
| усе погано | 4 рядки проблем | 4 рядки + **4 ❌ фіди** |

Вивід звіту на ноутбуці й на сервері **збігся побайтово** (md5
`c3faaeb6…` після, `a0705d09…` до — на обох машинах), синтаксис перевірено
`ast.parse` і локально, і серверним `venv/bin/python3`.

### Чому на сервері не активовано

Правило 6 черги вимагає міряти на сервері, і замір там зроблено — але **лише
в ізоляції**, окремим файлом у `/tmp`. Запустити сам `watchdog.py` не можна ні
там, ні тут, і причини різні:

* **сервер** — триває той самий незавершений rebase (перевірено цією сесією:
  `.git/rebase-merge` на місці, `HEAD` відчеплений, у `tools/watchdog.py` два
  маркери конфлікту, `SyntaxError` при кожному запуску за cron). Перший же
  оживлений watchdog зробить `git add -A && git commit` **у цей стан**. Це те
  саме рішення, що й у попередньому записі, і воно не змінилось;
* **ноутбук** — `main()` теж почав би з `check_git()`, тобто з
  `git add -A && git commit` у робочому репозиторії з незакомоміченою роботою
  трьох сесій. Тому обидва прогони зроблено імпортом модуля з підміненими
  `tg()` і `save_report_ts()`: жодного повідомлення в Telegram, жодного коміту,
  мережа — тільки HEAD-запити перевірок у наскрізному прогоні.

**Готовий файл підмінено на сервері в `/tmp`**, щоб команда, яку власник уже
має в черзі, розгортала НОВУ версію: `/tmp/watchdog_resolved_20260901.py` тепер
дорівнює ноутбучній (MD5 `8d41c821…` на обох машинах), а попередня версія без
фідів збережена поруч як `.bak_prefeeds` (MD5 `8e3ccacd…`). Це staging у
`/tmp`, а не активація: `tools/watchdog.py` на сервері не чіпався, cron і далі
падає на маркерах конфлікту, як і падав.

### Дотичне — записано, мовчки не бралось (правило 9)

**`check_rozetka_messages()` у звіт теж не потрапляє.** Перевірка нових
звернень Rozetka продавцеві (додана 01.09) повертає такий самий словник
`ok/msg`, але `main()` лише логує її. Ціна пропуску тут інша, ніж у фідів —
повідомлення дедуплікуються по id і не зникають з кабінету, — тож у звіт її не
додавав: постановка називала саме чотири фіди, а розширювати обсяг самому
черга забороняє. Матеріал для окремого пункту.

### Стан і що не робилось

**Нічого не публікувалось.** Фіди не перезбирались і не чіпались: NOIRE,
Carvol і TOPTUL не торкався жодним записом (правила 2–4). Змінено один файл —
`tools/watchdog.py` на ноутбуці; копія до правки — `/tmp/watchdog_before_report.py`
(MD5 `8e3ccacd…`). Мережа — `ssh`/`scp` на сервер і читання опублікованих
фідів HEAD-запитами в наскрізному контролі. До NVIDIA запитів не було.

**Потрібне від власника:** те саме — `git push` і завершити або скасувати
rebase на сервері. Доти нагляду немає, а нова версія звіту лежить у `/tmp` й
не працює.

## 01.09.2026, п'ятий автономний прогін черги

Узято перший `[ ]` — «мішане написання (гомогліфи) у 261 картці TOPTUL».
Закрито `[x]`.

### Правило приймає рішення СЛОВНИКОМ, а не більшістю літер

Перше, що довелось відкинути, — очевидний підхід «переводити меншість у
більшість». Дві позиції з фіду його вбивають, і обидві справжні:
`CTAHДAPT` має 7 латинських літер проти однієї кириличної `Д`, а правильна
відповідь — кирилиця (`СТАНДАРТ`); `МхF` має дві кириличні проти однієї
латинської, а правильна відповідь — латиниця (`MxF`, різьба М×F). Правило
про більшість дало б неправильну відповідь на обох.

Тому `tools/homoglyph.py` вирішує в три кроки: який напрямок узагалі
**можливий** (латинські `b d f g j l n q r s u v w z` кириличних двійників
не мають, і навпаки), а коли можливі обидва — вирішує український словник
на 1.55 млн словоформ. `Hі` → `Ні` (є в словнику), `Tуpe` → `Type` (немає).
Це той самий словник, що вже є ознакою мови, а не другий.

**Запобіжник, без якого правило псує дані.** Напрямок «у латиницю»
зіпсував би `Vрес` — це `Об'єм ресивера (Vрес): 100 літрів`, тобто законна
суміш латинської позначки й українського скорочення, а не гомогліфи. У
`V` кириличного двійника немає, отже єдиний можливий напрямок — латиниця, і
вийшло б `Vpec`. Відрізнити правилом про довжину чи більшість не вийшло
(`МхF` — контрприклад), тому стоїть окрема умова: **у латиницю не
переводимо, якщо суцільний кириличний відрізок довший за 2 літери**. Один
зіпсований символ у латинському коді (`GIZMATIС`, `Batterу`, `LVМP`) дає
відрізок 1–2, ціле слово — 3 і більше. На всьому фіді запобіжник спрацював
рівно на `Vрес` і на жодному з решти 16 випадків латинізації, які
переглянуто **всі до одного**, а не вибіркою.

### Помилка, яку зробив у цій же сесії, і що вона зламала

Перед тим як ставити виправлення ДО пошуку в словнику перекладів, зробив
замір: чи є мішане написання в ключах `toptul_translation` і
`toptul_desc_translation`. Замір відповів «жодного в 3010 рядках», і на цій
підставі виправлення поставлено перед пошуком.

**Відповідь була фактом про мій запит, а не про базу.** Дані діставались
через `psql -c "COPY ... kind||E'\t'||src"`, а COPY екранує табуляцію
назад у `\t`; `line.partition('\t')` справжньої табуляції не знайшов, і
перевірялося порожнє поле. Це рівно те, від чого застерігає правило
позитивного контролю, і в моєму власному лічильнику була підказка, яку я
проминув: `Counter()` порожній ЦІЛКОМ, тобто не було навіть ключа `kind` —
перевірка не знайшла нічого не тому, що чисто, а тому, що не дивилась.

Ціна виявилась негайною й вимірною. Ключі мішані (44 токени), і перший же
прогін генератора **втратив переклад назви**: `Вороток 1/2" Г-образный
большой (Мотор Сiч) В12ГМС` з латинською `i` перестав збігатися з ключем, і
у фід повернулось російське «Г-образный большой» замість «Г-образний
великий». Помітив це не лічильник — лічильник мовчав, бо переклад не
зникає, він просто перестає застосовуватись, — а рядок у самоперевірці
«назв із російськими словами: 1», якого перед тим не було.

Виправлено двома незалежними речами, і обидві потрібні:
* `tools/toptul_homoglyph_normalize.py` — разова міграція обох таблиць
  (9 рядків: 1 `title`, 1 `value`, 7 описів). Ідемпотентна, зіткнення
  ключів — відмова, а не мовчазне перезаписування; контрольний прохід після
  запису показує 0;
* `_hg()` у генераторі — словники нормалізуються ще й **при завантаженні**.
  Міграція лікує сьогоднішній стан, запобіжник — завтрашній фід
  постачальника.

### Мішане написання ХОВАЛО російський текст, і це головна знахідка

Постановка казала: у фіді 6 «російських» вживань, і всі 6 — це `cили` з
латинською `c`, де ознака мови бачить російське `или`. Тобто очікувалось,
що виправлення зробить 6 → 0.

Вийшло навпаки: **6 → 47**. Хибні спрацювання справді зникли, але разом із
ними зникло й маскування. `Размери`, `Материал`, `Цвет`, `бокорезы`,
`прецизионные`, `электромонтажных` були у фіді весь час — написані
навпіл латиницею, вони розсипались на уламки, яких не знає жоден словник, і
ознака мови на них мовчала. Ті самі 47 вживань лежали у фіді й учора, коли
пункт «перекласти російські описи» закривався з числом 6.

Це не привід лишати як є, тому переклад догнано тим самим інструментом:
`toptul_desc_translate.py --run` — 17 нових речень, записано 16, одне
відхилено власною перевіркою («латиниця не збігається» — модель зачепила
`PROTESTER IPS-2002`). Описів російською у фіді: **47 → 15** (2 оффери).

`collect()` у перекладачі теж нормалізує текст — інакше він шукав би
російське в зіпсованому рядку й давав 731 «російське» речення проти 745
справжніх, тобто помилявся в обидва боки одночасно.

### Регресія, яку зробив і зловив на звірці

Перша версія дала назву `Набір для відновлення геометрії клапана
GIZMATIC3045 ХЗСО (GIZMATIС3045)` — артикул двічі. Причина в тому, що
`build_name()` вирізає артикул із тексту назви точним збігом: текст уже
виправлено (`GIZMATIC3045`), а тег `<article>` за постановкою лишається
постачальницьким (`GIZMATIС3045`), і два рядки перестали бути рівними.
Виправлено: вирізаються **обидві форми**, сира й нормалізована. Знайшлось
це не з коду, а зі звірки назв до/після — тієї самої, якої вимагала
постановка.

### Позитивний і негативний контроль

**Самоперевірка без мережі й БД** — `tools/homoglyph.py --selftest`: 17
токенів із ФІДУ, які мусять виправитись, проти 11, які мусять лишитись
недоторканними (`TOPTUL`, `GAAI0802`, `Toyota`, `викрутка`, а також `Vрес`,
`тORIN`, `Беzdротова`, `PROЗ` — ті, на яких правило свідомо зупиняється).
Плюс чистий український рядок, який не має змінитись жодним символом.

**Що перевірка вміє повернути ненуль** — та сама перевірка на тих самих
даних дала 17 902, потім 87: вона рухалась разом із виправленням.

**Негативний контроль — `output/noire_rozetka.xml`** (лише читання; 4147
карток, 592 тис. слововживань української прози). Мішаних токенів там **34
у 31 оффері**, і, переглянувши всі шість різних, **хибних тривог нуль** —
усі шість справжні: `Cклад`→`Склад` (10), `мiститься`→`міститься` (6),
`Рassion`→`Passion` (бренд із кириличною `Р`), `ХXXL`→`XXXL`, `ХXL`→`XXL`,
`cилует`→`силует`. Тобто заразом знайшлось те, чого не шукали — у фіді на
модерації лежать назва бренду й два розміри, набрані двома абетками.

### Заміри

| Що | До | Після |
|---|---:|---:|
| Мішаних токенів у фіді (фактичних) | 17 902 | **87** |
| різних токенів | 4 133 | **27** |
| офферів із мішаним написанням | 269 | **61** |
| Артикулів змінено | — | **0** з 5783 |
| Брендів (`<vendor>`) змінено | — | **0** з 5783 |
| Офферів / категорій / цін / фото змінено | — | **0** |
| Назв змінено | — | 40 |
| Описів змінено | — | 233 |
| Значень характеристик змінено | — | 2 |
| Описів російською (`description_ua`) | «6» (насправді 47) | **15** у 2 офферах |
| Назв, назв і значень характеристик російською | 0 / 0 / 0 | **0 / 0 / 0** |
| Хибних українських форм | 0 | **0** |
| Офферів / валідатор | 5783 / 0 / 0 | 5783 / **0 помилок, 0 попереджень, 100%** |

Числа генератора й незалежного `homoglyph.py --audit` збіглися до одиниці
(87 і 87) — два різні проходи по тих самих даних.

### Що лишилось і чому саме воно

87 вживань у 27 токенах, розібрані по класах у
`docs/toptul_homoglyph_left.md` (+ `.tsv` із SKU). Спільне в усіх: заміна
**не виводиться з написання**, а вигадувати заборонено (правило 5).
Найбільший клас — кирилична `З` замість цифри 3 (`X-431 PROЗ` = `PRO3`), і
він насправді більший за перелік: коли `З` стоїть серед цифр (`1З50 Нм`,
`4ЗЗMHz`, `T8З502`), токен виходить суто кириличним і в замір про «суміш»
не потрапляє взагалі — груба оцінка по фіду **~300 вживань у 95 офферах**.
Поруч — ~320 `220 B` з латинською `B`. Обидва винесені питанням власникові,
мовчки не бралися (правило 9): це інший клас дефекту, ніж поставлений, і
кожен потребує свого правила з власним позитивним контролем.

Окремо варте слова обмеження самого підходу: латинський код, набраний
**суцільно** кирилицею, мішаним не є за побудовою. `ТЗ0` у переліку
`T10, T15, T20, T25, T27, ТЗ0, T40` — це `T30` двома кириличними літерами,
і жодне правило про суміш його не знайде.

### Стан і що не робилось

`output/toptul_rozetka.xml` перезібрано на сервері о 22:06 — 5783 оффери,
22 932 039 Б, `sha256 7a75f203…`, ноутбук = сервер. **Не публікувався.**
NOIRE і Carvol не торкався: `output/noire_rozetka.xml` (21:22) і
`noire_prom.xml` (19:40) — від власного cron (`20 * * * *` і
`40 7,11,15,19`), `data/carvol_rozetka.xml` (07:16) з тією самою датою, що
й до сесії; робота йшла 20:40–22:10. Негативний контроль читав фід NOIRE,
не змінюючи його.

MD5 ноутбук=сервер звірено по чотирьох файлах: `homoglyph.py`
(`2cb75a6b…`), `toptul_rozetka_generator.py`, `toptul_desc_translate.py`,
`toptul_homoglyph_normalize.py`. Копія генератора до правки —
`/tmp/gen_before_homoglyph.py` (`0b31ddd2…`), фід до правки —
`/tmp/toptul_before_homoglyph.xml` на обох машинах.

У БД змінено 9 рядків двох таблиць перекладу (міграція вище) і додано 16
рядків у `toptul_desc_translation` (728 → 744). Не комітилось — push робить
власник.

**Потрібне від власника:** те саме, що й учора — `git push` і завершити або
скасувати rebase на сервері; доти watchdog мертвий, а робота дня живе лише
в робочому каталозі. Плюс три питання про залишки зіпсованого тексту, див.
кінець `docs/work_queue.md`.

## 02.09.2026, автономний прогін черги

Узято перший `[ ]` — «розібрати 49 помилок 400 при заповненні з фіду Prom».
Закрито `[x]`: причину названо поіменно, виправлено, всі 49 карток заповнено.

### Причину ховало власне обрізання повідомлення

Постановка вимагала спершу розширити повідомлення — і саме воно було
єдиною перешкодою. `r2.text[:140]` різав відповідь рівно на слові `"erro`,
тобто **обрізання закінчувалось за один символ до поля, у якому Єпіцентр
називає винуватця**. Повний текст дає його одразу:

```
{"code":400,"message":"…Invalid request parameters.",
 "errors":{"attributeValues[5249]":["validation.attribute.invalid"]}}
```

Одне й те саме повідомлення в **усіх 49** відповідях, і всі 49 карток — з
однієї категорії `9476` (Вакуумні помпи). Тобто це не 49 різних випадків, а
один дефект, помножений на 49.

### Причина: тип атрибута, а не значення

`data/epicentr_attribute_sets.json`, набір 9476:

| код | назва | тип | обовʼязковий |
|---|---|---|---|
| 3369 | Призначення | `select` | так |
| **5249** | **Тип помпи** | **`multiselect`** | так |

Інструмент клав у `value` рядок-valuecode незалежно від типу. Для `select`
це правильно, для `multiselect` — ні: майданчик чекає **список кодів**.
Значення при цьому було цілком законне — воно взялось із того самого
довідника опцій, який віддає API, тому перевірка «значення поза довідником»
його пропускала й не мала жодних підстав не пропустити.

Форму підтверджено не здогадкою, а **зразками з кабінету**: на картках, де
multiselect уже заповнений менеджером, значення виглядає як
`['54be518cd67142070bd0345093c0bf0a']`, а там, де вибрано два —
`['bb9cc791…', '3f8ee63c…']`. Тобто список і в однині.

Виправлення — один рядок: `[vc] if at.get('type') == 'multiselect' else vc`.

### Позитивний контроль

Перевірка вміє повертати обидва результати на тих самих даних і тих самих
картках, з різницею рівно в одному: форма значення.

| | рядок (як було) | список (як стало) |
|---|---:|---:|
| PUT прийнято | **0 з 49** | **49 з 49** |
| PUT відхилено 400 | 49 | 0 |

**Результат перевірено там, де він споживається** (правило 7) — не за кодом
відповіді, а зворотним `GET /v2/pim/products/{id}` по всіх 49 картках:
`5249` заповнений у **49 з 49**, значення лежить списком.

Замір обовʼязкових прогалин по цих же 49 картках:

| | до | після |
|---|---:|---:|
| карток з порожнім `5249` | 49 | **0** |
| карток з порожнім `3369` | 49 | **0** |
| сумарно порожніх обовʼязкових полів | 98 | **0** |
| карток, де заповнено ВСІ обовʼязкові | 0 | **49** |

Обидві прогалини зникали разом, бо PUT атомарний: через один негодящий
атрибут не записувалось і законне «Призначення». Тому 49 карток стояли не з
однією прогалиною, а з двома.

### Масштаб пастки ширший за 49 карток

Порахував, скільки карток у «Наповненні контентом» узагалі стоять у
категоріях з **обовʼязковим** `multiselect`: **469 із 3118**, у 20
категоріях. Найбільші — `9616` (109), `9526` (107), `9620` (57), `9476` (54),
`9630` (42); атрибут `78` повторюється в семи категоріях.

Спрацював дефект лише на 49 тому, що для решти 420 фід Prom значення не дає
(або воно вже стояло) — тобто до PUT справа не доходила. Виправлення знімає
пастку наперед: як тільки з'явиться джерело значень для `78` чи `12097`,
старий код поклав би ці картки так само мовчки й гуртом.

### Що змінено в інструменті

`tools/epicentr_fill_from_prom.py` (183 → 203 рядки, MD5 `0efc611c…`):

* `value` для `multiselect` — списком (сама причина);
* повний текст відповіді замість `[:140]`. Обрізання тут коштувало доби:
  помилка була названа поіменно ще 01.09, просто в невидимій частині рядка;
* `--dump-fails FILE` — JSONL із **тілом запиту** і повною відповіддю.
  Без тіла запиту не видно, яке саме значення пішло, а лише що воно погане;
* `--only SKU,SKU` — прогін по переліку. Окремо друкує SKU з переліку, яких
  немає серед enrich+Prom, інакше «нуль оброблено» читалось би як «усе
  гаразд».

`--only` і `--dump-fails` і є те, чим цей розбір зроблено: 49 карток
переганялись чотири рази (відтворення, зразок, виправлення, звірка), і без
них це були б чотири прогони по 2715 карток.

### Стан і що не робилось

**Нічого не публікувалось.** Жоден фід не перезбирався й не чіпався: NOIRE,
Carvol і TOPTUL не торкався (правила 2–4). Зміни пішли тільки в картки
Єпіцентру в статусі `enrich` через API кабінету — рівень 1 за SKILL-22
(«заповнення характеристик карток у статусі enrich»), під запобіжник
`consent.py` не підпадає.

Інструмента немає ні на сервері, ні в crontab (перевірено) — він запускається
з ноутбука вручну, тож звіряти MD5 з сервером нема з чим.

**Статусів не міняв.** Ці 49 карток тепер не мають жодної обовʼязкової
прогалини, тобто формально готові до модерації, але переведення — рівень 3 і
окремий пункт черги (`tools/epicentr_moderate_ready.py`). Мовчки не брався.

Не комітилось — push робить власник.

## 02.09.2026, другий автономний прогін черги

Узято перший `[ ]` — «закрити найбільшу групу з тих, де ознака є в даних».
Заповнення зроблено й звірено з кабінетом; переведення на модерацію впирається
в згаслий дозвіл, тому пункт `[!]`, а не `[x]`.

### Перше, що показав замір: роботу вже зроблено, і сліду про це немає

Прогін `epicentr_fill_from_prom.py --plan --params` о 10:07 дав **0 полів у 0
картках** — там, де звіт о 06:59 і постановка в черзі обіцяли 1037 і 545.
Спокуса прочитати нуль як «інструмент зламався» була прямою, але правило
позитивного контролю вимагає пояснити нуль механічно, а не оголосити.

Пояснення знайшлось у `/tmp`: `fill_run.log` від **08:06** — «записано: 542,
яких полів додано: 1034 у 32 різних полях», плюс `gaps_BEFORE_fill.json`
(07:52) і `gaps_AFTER_fill.json` (08:23), плюс `ready_after_fill.json` (09:08).
Тобто сесія між 07:50 і 09:10 виконала цей пункт **повністю**, але не
позначила його в черзі й не написала жодного рядка в журнал. Це той самий
дефект, що й 25.08 («між двома записами був прогін, який нікуди не записався»),
тільки цього разу він коштував би подвійної роботи: не перевіривши `/tmp`, я
переганяв би 1759 карток удруге.

Нуль тут — не мовчання перевірки, а її правильна відповідь: заповнювати
більше нічого. Та сама команда на тих самих даних давала 1037 о 06:39, записала
1034 о 08:06 і дає 0 о 10:07 — вона рухалась разом зі станом кабінету.

### Що саме перевірено, бо чужий лог результатом не є

Правило 7 вимагає перевіряти там, де результат споживається, а не в лозі
інструмента, який його зробив. Тому три незалежні перевірки в кабінеті.

**1. Порівняння аудитів до/після** (`epicentr_enrich_audit.py`, обидва читають
кабінет, а не фід):

| | до 07:52 | після 08:23 |
|---|---:|---:|
| порожніх обовʼязкових полів у 1760 картках | 7263 | **6229** |
| карток без жодної прогалини | 52 | **371** |
| карток, де прогалин стало більше | — | **0** |

Різниця полів рівно **1034**, закрилось повністю рівно **319** карток.

**2. Розбіжність зі звітом — 3 поля, і вона порахована, а не списана.** Звіт
обіцяв 1037 полів у 545 картках і 322 закритих. Виявилось 1034/542/319, різниця
рівно 3 — це `AL40571`, `MD1137`, `F12441`, три картки, заповнені ще о 06:5x як
наскрізний контроль звіту. У `gaps_BEFORE_fill.json` вони вже стоять без
прогалин, тобто їхні 3 поля порахувались у звіті й не могли порахуватись удруге.
1037 − 3 = 1034, 545 − 3 = 542, 322 − 3 = 319 — сходиться в усіх трьох числах.

**3. Адресна звірка значень, а не лише лічильника прогалин.** `/tmp/verify_fill.py`
бере 12 випадкових закритих карток, GET-ом дістає їх із кабінету й порівнює
код у полі з кодом довідника для значення, яке обіцяв звіт: **21 поле, 21 збіг,
0 розбіжностей, 0 порожніх обовʼязкових**. Це відповідь на інше питання, ніж
«прогалина зникла»: прогалину закриває будь-яке значення, а нас цікавить саме
те, що виведене з наших даних.

**Позитивний контроль поруч, тими самими очима:** 6 випадкових карток із тих,
де прогалини лишились, — **30 порожніх обовʼязкових полів**. Перевірка вміє
повертати ненуль на тих самих даних і тим самим кодом. Заразом на двох із них
(`SO2503`, `SO6781`) видно, що обіцяні поля таки заповнені правильно, а картка
все одно не готова — бо їй бракує інших полів (`Кількість`, `Обʼєм`). Поле і
картка — різні одиниці, і саме тому 1034 поля дали лише 319 карток.

### Готових до модерації — 371, і це число підтверджене двома різними кодами

`epicentr_moderate_ready.py --plan` о 10:14 обійшов усі 1760 карток окремими
запитами: **готові до модерації — 371**. Це інший код, ніж аудит прогалин
(інший обхід, інші поля відповіді), а перелік SKU збігся **до одного**: 371 із
371, симетрична різниця порожня. Перелік — `docs/epicentr_moderate_ready_20260902.tsv`
(категорія, код, SKU, id кабінету, і чи закрито сьогодні).

Найбільші групи готових: Насадки на член 88, Засоби для орального сексу 63,
Еротичні приколи та сувеніри 60, Екстендери та помпи 52, Фетиш та BDSM 42.

### Чому пункт `[!]`, а не `[x]`

Друга половина постановки — «готові переводити на модерацію через
`tools/epicentr_moderate_ready.py`». Інструмент під запобіжником
`consent.py`, і **дозвіл `epicentr_moderate` згас о 09:03** (виданий о 05:03 на
чотири години під учорашні 1358 карток). Стан зараз — `⛔ немає дозволу`.

Дозвіл собі не видавав. Запобіжник саме для цього й існує: він захищає від
випадкової дії, і агент, який його обходить, перетворює його на прикрасу.
Перехід односторонній — назад із «На модерації» повертає лише модератор.

Власникові лишилось дві команди:
```bash
python3 -m shared.utils.consent --allow epicentr_moderate --hours 4 \
  --why "02.09: 371 картка після заповнення з sexopt_extracted_params"
python3 tools/epicentr_moderate_ready.py          # перевірить кожну ще раз
```
Другу команду безпечно запускати без переліку: вона не бере готовність із
файлу, а перепитує кабінет по кожній картці й недозаповнені не рухає.

### Що лишається після заповнення — 6229 прогалин у 1389 картках

Класи не переміряв заново — вони збігаються зі звітом 06:59 мінус закриті:
36 `prom-num` + 32 `db-num` + 257 `text` + 733 `prom-inval` + 403 `db-inval` +
265 `text-many` + 65 `binary-hint` + 2290 `binary` + 2148 `none` = **6229**,
тобто рівно те, що лишилось після зняття 1037 − 3.

Найбільші категорії з прогалинами: Вібратори 298, Олія для еротичного масажу
130, Аксесуари для страпонів 109, Аксесуари до інтимних іграшок 107, Ерекційні
кільця 89. Найчастіші поля: Призначення 401, Конструкція 366, Матеріал 340,
Вид 334, Колір виробника 322, Кріплення на присоску 316, Підігрів 313.

**Жодне з них не заповнюється тим самим способом**, тому наступний крок — не
повторення цього прогону, а рішення власника з кінця
`docs/epicentr_remaining_gaps.md`: Так/Ні без ознаки в тексті (2290),
таблиця відповідностей для значень поза довідником (1136), джерело для
`Колір виробника` (322, у наших даних немає як класу). Числові поля (68) —
наша робота й окремий пункт, мовчки не брався (правило 9).

### Стан і що не робилось

**Нічого не публікувалось.** Жоден фід не перезбирався: `noire_rozetka.xml`
(15.08 20:30), `noire_prom.xml` (17.08 05:41), `carvol_rozetka.xml` (01.09
05:52), `toptul_rozetka.xml` (01.09 21:13), `exports/noire_epicentr.xml` (23.08)
— усі з тими самими датами, що й до сесії. NOIRE на Rozetka, Carvol і TOPTUL не
чіпав (правила 2–4).

**Статусів карток не міняв** — жодного PATCH на `moderating`.
**Записів у кабінет цією сесією не робив**: усі мої прогони
`epicentr_fill_from_prom.py` були з `--plan`, решта — GET. Записав ті 1034 поля
прогін 08:06, не цей.

Змінено два файли документації: `docs/epicentr_moderate_ready_20260902.tsv`
(новий) і шапка `docs/epicentr_remaining_gaps.md` — там тепер сказано, що
таблиці описують стан ДО заповнення, інакше документ читався б як перелік
роботи, яку ще треба зробити. Код не змінювався взагалі, тому звіряти MD5 з
сервером нема чого: `epicentr_fill_from_prom.py` і `epicentr_moderate_ready.py`
на сервері немає й у crontab не стоять — запускаються з ноутбука вручну.

**Потрібне від власника:** дозвіл на модерацію (дві команди вище) і те саме, що
й учора, — `git push` і завершити або скасувати rebase на сервері; доти
watchdog мертвий, а робота дня живе лише в робочому каталозі.

## 02.09.2026, третій автономний прогін черги

Узято перший `[ ]` — «TOPTUL: гомогліфи у 261 картці». Закрито `[x]`.

### Перше, що показав замір: поставлений клас закрито 01.09, а критерій — ні

Аудит поточного фіду дав те саме, що й запис 01.09: **175 вживань мішаних
токенів (87 фактичних) у 61 оффері, 27 різних**. Тобто літерне правило
(«латинська літера всередині кириличного слова») своє відпрацювало, а
критерій пункту — «нуль змішаних слів у фіді» — не виконано й наполовину.

Найбільший залишок лежав поруч і в перелік мішаних не потрапляв **за
побудовою**: кирилична `З` на місці цифри 3. `З60 хв`, `1З мм`,
`ЗЗ8x2З7хЗ12 мм` — суцільно кириличні токени, мішаними вони не є, і жодне
правило про суміш абеток їх не побачить. Замір цього класу новим проходом:
**554 вживання, 136 різних токенів, 90 офферів** — уп'ятеро більше за все,
що лишалось у мішаних.

### Правило вирішує сусідство кожної `З`, а не токен цілком

Спокуса — «токен із цифрою, отже `З` теж цифра». Три контрприклади з фіду
її вбивають, і ціна помилки тут найвища з усіх, що траплялись:

* `ЗМЗ406` — двигун ЗМЗ-406, цифри в токені є, `З` законна;
* `РАЗВ7.01`, `РАЗВ1719В` — артикули розвальцьовок, 200+ вживань;
* прийменник `з`/`З` — **20 080 вживань** у фіді.

Тому вирішує не токен, а місце: у токені мусить бути цифра або латиниця
(інакше це слово); попередня літера не кирилична (саме це рятує `ЗМЗ406`,
де друга `З` стоїть після `М`, і всі `РАЗВ*`); і хоч одне з — інших
кириличних у токені немає, або поруч цифра, або поруч роздільник розмірів
`х` між цифрами. Прохід зліва направо по вже зміненому рядку, тому в
`4ЗЗMHz` друга `З` спирається на першу, яка вже стала цифрою.

### Помилка, яку зробив, і яку зловив сирий фід

Правило перевірялось на **готовому** фіді, і там 125 різних замін виглядали
бездоганно. Прогін по **сирому** фіду постачальника показав інше: `зa` →
`3a` у 120 вживаннях, `Зa` → `3a` у 20, `poз` → `po3` у 22, `зapaз` →
`3apa3`, а бренд `BAЗ` (латинські `B`, `A`) → `BA3`. У сирому тексті слова
набрані навпіл латиницею, тож `зa` має обидві абетки й проходить першу
умову.

У конвеєрі цього не сталося б **через порядок**: літерний прохід іде першим
і всі п'ять випадків знімає. Але спиратись на порядок не можна — наступний
інструмент викличе правило на сирому рядку, і це буде та сама пастка, яку
01.09 вже ставив порядок «виправлення перед пошуком у словнику». Тому
запобіжник у самому правилі: якщо в токені немає цифри, а літерне правило
впізнає в ньому кириличне слово, — не чіпаємо. Після нього з переліку зникли
рівно 14 токенів-слів (`зa`, `poз`, `Зa`, `TЗ`, `BAЗ`, `зapaз`, `Poз`,
`paз`, `Зi`, `ЗiP`, `XЗCO`, `PЗ`, `paзa`, `Зappy`) і не додалось жодного
нового.

### Позитивний і негативний контроль

**Перегляд усіх замін, а не вибірки.** На готовому фіді — 125 різних пар, на
сирому — 178; переглянуто кожну. Жодного українського слова, жодного
артикула `РАЗВ*`, `ЗМЗ406` недоторканий.

**Самоперевірка без мережі й БД** — `tools/homoglyph.py --selftest`: до
17+11 літерних рядків додано **14 цифрових виправлень і 18 недоторканних**,
усі з фіду. Серед недоторканних — обидва класи, на яких правило могло б
хибно спрацювати: артикули (`РАЗВ7`, `ЗМЗ406`) і слова сирого фіду (`зa`,
`BAЗ`). Плюс два текстові контролі: чистий рядок із прийменником `з` поруч
із цифрами не змінюється жодним символом, а зіпсований змінюється **саме
там, де треба** (`X4З1 PROЗ` виправляється, `220 B` лишається).

**Негативний контроль — `output/noire_rozetka.xml`** (лише читання;
1 867 966 слововживань української прози). Замін **0**. Нуль пояснений
механічно, а не «схоже, все добре»: у цьому корпусі **161 426 токенів
містять `з`/`З`**, і в **жодного** з них немає ані цифри, ані латинської
літери в тому ж токені, тобто всі відсіюються на першій умові й до розбору
сусідства не доходять. Що перевірка вміє повертати ненуль — той самий
скрипт на фіді TOPTUL дає 1108 замін, на сирому фіді постачальника — 1232.

### Другий раз за день той самий клас помилки — і цього разу мій

Перед міграцією словників перевірив, чи є цифровий дефект у
`toptul_desc_translation`. Власний запит відповів **0 рядків**, тоді як
штатний інструмент казав **8**. Спокуса була повірити своєму запиту.

Причина виявилась у ньому: `get_connection()` віддає `RealDictCursor`, і
`for src, dst in cur.fetchall()` розпаковував не значення, а **назви
колонок** — перевірялись рядки `'src'` і `'dst'`. Нуль був фактом про мій
запит, а не про базу. Рівно те, від чого застерігає правило позитивного
контролю, і рівно те, на чому 01.09 спіткнулась перевірка мішаного в
ключах (`COPY` екранував табуляцію). Після виправлення запиту — ті самі 8
рядків, з видимим `QCЗ.0`, `З60 хв`, `ЗЗ8x2З7хЗ12` в обох половинах пари.

Міграцію проведено (`toptul_homoglyph_normalize.py --apply`, 8 рядків,
контроль ідемпотентності 0). Фід від неї не залежить — генератор нормалізує
ключі при завантаженні, — і це не припущення: перезбірка після міграції дала
**побайтово ті самі оффери**, різниця файлів рівно одна — дата в шапці.
Потрібна вона для `toptul_desc_translate.py`, який відрізняє перекладене від
неперекладеного за самим рядком: без міграції ці 8 речень назавжди
виглядали б неперекладеними й поїхали б до моделі вдруге.

### Заміри

| Що | До | Після |
|---|---:|---:|
| Кирилична `З` на місці цифри (вживань) | **554** | **0** |
| різних токенів / офферів | 136 / 90 | **0 / 0** |
| Мішаних токенів у фіді | 175 | **83** |
| різних токенів / офферів | 27 / 61 | **16 / 34** |
| Артикулів (`<article>`, `vendorCode`) змінено | — | **0** з 5783 |
| Цін / категорій / фото / характеристик / наявності | — | **0** змін |
| Назв змінено | — | 4 |
| Описів змінено | — | 90 |
| Офферів у фіді | 5783 | 5783 |
| Валідатор | 0 помилок, 0 попереджень | **0 / 0, 100%** |
| Описів російською | 2 оффери, 15 вживань | 2 оффери, 15 вживань |
| Назв / назв і значень характеристик російською | 0 / 0 / 0 | **0 / 0 / 0** |
| Хибних українських форм | 0 | **0** |

Звірка двох фідів зроблена **поле за полем по всіх 5783 офферах**, а не
підсумками: питання було не «чи стало краще», а «чи змінилось рівно те, що
ми міняли». Артикули, ціни, категорії, фото, характеристики й наявність —
нуль розбіжностей.

**Побічний результат, якого не планував.** Дві назви позбулися артикула,
який стояв у них двічі: `…380В BETA55-З80 BRIGHT (BETA55-380)` →
`…380В BRIGHT (BETA55-380)`, те саме з `86З0 220V`. Причина та сама, що й у
регресії 01.09 з `GIZMATIC3045`: `build_name()` вирізає артикул точним
збігом, а зіпсоване написання робило текст назви й тег `<article>` різними
рядками. Тепер вони рівні — і дубль зник сам.

### Клас `B` (латинська «B» замість «В») — виміряний, але НЕ роблений

Запис 01.09 пропонував правило «цифра плюс пробіл плюс `B`». Замір показав,
що так робити не можна, і це тепер число, а не побоювання: з **572 вживань
у 109 офферах** ознаку напруги поруч мають 270, а **302 у 67 офферах** —
не мають, і це суцільно законна латиниця: `PT-29B`, `H-827B-1.7`,
`AT-4041B`, `JCA-350B`, `Фреза 20B 140º`, `Фреза 45B 90º`. Наївне правило
зіпсувало б більше, ніж виправило. Зворотний бік теж є: серед «без ознаки»
трапляється справжня напруга (`Споживана потужність: 380B, 2,2 кВт`), отже
й ознака напруги неточна.

Окремо варте уваги: у характеристиках цього класу **0 вживань**, тобто на
фільтри Rozetka він не впливає взагалі — усе в описах і назвах. Це знижує
його вагу проти того, як він виглядав учора. Мовчки не брався (правило 9),
розбір — у `docs/toptul_homoglyph_left.md`.

### Стан і що не робилось

`output/toptul_rozetka.xml` перезібрано на сервері о 11:40 — 5783 оффери,
22 931 391 Б, `sha256 35578718…`. **Не публікувався.** Перевірено окремо, що
його не чіпає жоден запис crontab: у розкладі сервера немає ні генератора
TOPTUL, ні синхронізації цього файлу.

**NOIRE, Prom і Carvol не торкався.** `output/noire_rozetka.xml` (11:22) і
`output/noire_prom.xml` (11:40) оновились під час сесії, але **своїм
власним cron** — `20 * * * *` і `40 7,11,15,19` відповідно; `data/carvol_rozetka.xml`
(07:15) з тією самою датою, що й до сесії. Що моя правка на них не впливає,
перевірено не словами: `homoglyph.py` імпортують рівно три інструменти, усі
три — TOPTUL (`toptul_rozetka_generator.py`, `toptul_desc_translate.py`,
`toptul_homoglyph_normalize.py`); жоден генератор NOIRE його не бачить.
Негативний контроль читав фід NOIRE, не змінюючи його.

Змінено один файл коду — `tools/homoglyph.py` (18 854 → 31 257 Б, MD5
`b389ba9b…` ноутбук = сервер). Копія до правки — `tools/homoglyph.py.bak_20260902`
на ноутбуці, фіди до й після — `/tmp/toptul_before_z.xml` і
`/tmp/toptul_after_z.xml` на сервері. `toptul_rozetka_generator.py` не
змінювався взагалі (MD5 той самий, що й 01.09): цифрове правило ввімкнулось
само, бо генератор викликає `fix_text()`, а не власну копію логіки.

У БД змінено 8 рядків `toptul_desc_translation` (міграція вище).
Оновлено `docs/toptul_homoglyph_left.md` і `.tsv`. Не комітилось — push
робить власник.

**Потрібне від власника:** те саме, що й учора, — `git push` і завершити або
скасувати rebase на сервері (`git status` показує «interactive rebase in
progress», 4 команди попереду); доти watchdog мертвий. Плюс рішення по
класу `B` вище: чи варта робота 270 вживань в описах, коли фільтри цього
класу не бачать.

## 02.09.2026, четвертий автономний прогін черги

Узято перший `[ ]` — «переміряти 1179 товарів, що випадають (менше 3
характеристик)». Закрито `[x]`.

### Пункт стояв у черзі вдруге, і замір це підтвердив, а не здогад

Постановка називала 1179 і казала «після заповнення фільтрів частина мала
повернутись». Прогін генератора на сервері **до будь-якої дії** дав **916**,
а не 1179: перестановка `MIN_PARAMS` уже зроблена 01.09, і саме вона повернула
263 оффери. Тобто пункт у плані на 02.09 — копія вже закритого; це видно з
першого ж заміру, тому переписувати роботу не довелось.

Що з цього НЕ випливало й що й було справжньою роботою сесії: чи повернув
когось наступний шар — гомогліфи (літери 01.09, цифри `З`→`3` 02.09). Назви
для 916 відсіяних вони таки переписали, а от у фід не повернули **жодного**.

### Числа

| Що | до (01.09) | зараз |
|---|---:|---:|
| Пропущено: менше 3 характеристик | **1179** | **916** |
| Офферів у фіді | 5520 | **5783** |
| `фільтри врятували оффер від MIN_PARAMS` | — | 263 |
| Набір відсіяних SKU | 916 | **ті самі 916, до одного** |
| Те саме з ВИМКНЕНИМИ гомогліфами | — | **916 / 5783, той самий набір** |

**Арифметика замкнута, а не «сходиться приблизно»**: 6904 оффери
постачальника = 5783 у фіді + 916 (менше 3 характеристик) + 128 (категорія
без `rz_id`) + 77 (недовірений рівень). Інших причин відсіву в лозі **нуль** —
перевірено переліком усіх рядків `пропущено`, а не сумою навмання.

**Два незалежні прогони поспіль дали побайтово однаковий перелік**
(md5 `78091224…`), тобто число не плаває від запуску до запуску.

### Чому гомогліфи не повернули нікого — з даних, а не «схоже, не мали»

Перевірено не порівнянням із учорашнім файлом, а **вимкненням самого
виправлення**: окремим скриптом у `/tmp` `fix_text()` підмінено на тотожність
і запущено той самий генератор. Результат — **916 відсіяних і 5783 оффери,
набір SKU ідентичний** тому, що дає ввімкнене виправлення. Різниця двох
переліків — 13 рядків, і в усіх тринадцяти змінилась **лише колонка
«назва»** (`Poзcуxapювaч клaпaнів BAЗ 2110` → `Розсухарювач клапанів ВАЗ
2110`); колонки з кількістю й набором характеристик не змінились у жодному.

Порівняння «файл проти вчорашнього файлу» такого доказу не дало б: між
двома прогонами змінився не лише гомогліфний шар. Механізм зрозумілий:
`MIN_PARAMS` рахує
характеристики, а гомогліфи правлять текст. Вплинути вони могли б лише через
видобування фільтрів із назви — і саме цього не сталось: у тих 13 картках,
чия назва змінилась, набір характеристик з гомогліфами й без **однаковий**,
тобто виправлений текст не приніс жодного значення з довідника. Це вужче
твердження, ніж «фільтри тут не працюють узагалі»: серед 916 є 71 картка, яка
фільтр таки отримала, — просто до трьох їй усе одно не вистачило.

### Чого бракує решті 916 — новий розбір за рівнями

Замість «постачальник не дав» (це правда, але не відповідь на «що нам
робити») перелік розкладено на те, у що впирається кожен товар. Інструмент —
`tools/toptul_minparams_report.py`, читає TSV, який пише САМ генератор, і
довідник фільтрів; власних правил відсіву не має свідомо, інакше описував би
не той фід.

| рівень | товарів |
|---|---:|
| B: характеристика довідника без правила для категорії | 425 |
| B: значення довідника стоїть у назві, правила немає | 178 |
| A: категорії немає в довіднику фільтрів Rozetka | 158 |
| C: правило є, джерело перевірено, значення немає | 155 |

Тобто «даних немає» — це лише **313 з 916** (рівні A і C). Решта впирається в
нашу роботу: у `RULES` є правила «Вид»/«Тип» рівно для **5 категорій**, тоді
як у довіднику Rozetka «Тип» є у **70**, «Вид» — у **36**.

### Запас на 87 карток — і він перевірений генератором, а не пробою

Найбільша діра: «Знімачі» (226 відсіяних, найбільша категорія завалу) мають у
довіднику `Тип` зі значеннями `Знімач` / `Ключ` / `Набір`, а назва товару
рівно цим словом і починається. Проба «значення довідника стоїть у назві
окремим словом» знайшла 178 таких карток, з них **87 дійшли б до трьох
характеристик**.

**87 — не оцінка проби.** Окремим скриптом у `/tmp` (репозиторій не чіпався)
у `RULES` тимчасово підставлені правила «точне слово з довідника» для тих
8 пар «категорія × характеристика», де проба дала збіги, і запущено
**справжній генератор**: `916 → 829` відсіяних, `5783 → 5870` офферів. Різниця
рівно 87, тобто прогноз проби й поведінка генератора збіглися до одиниці.
Без цього кроку 87 було б твердженням про мою пробу, а не про фід.

**Мовчки правило не додавав (правило 9).** Перегляд збігів показав хибні за
суттю: `Утримувач рулону паперу для інструментальної візки` → «Тип: Візки»
(тримач ДЛЯ візка, не візок), `Ключі TORX Г-подібні` → «Тип: Ключі» в
«Імбусових ключах» — формально правда, але правило треба звіряти очима на
кожній категорії, як зроблено для наявних п'яти. Винесено в питання
власникові з ціною роботи й числом.

### Позитивний контроль

**Проба вміє й НЕ знаходити.** `--selftest` без мережі й БД: 7 випадків, з них
**4 з порожнім очікуванням** — `Ключі свічкові` не мусить дати «Ключ»
(інше слово), `Фарбопульт електричний` не мусить дати «Електричні» (інша
форма), числові значення «Робочого розміру» в назві не шукаються взагалі.
Останнє — не лінощі, а рішення 01.09: число в назві буває довжиною, а не
робочим розміром. Самоперевірка падає, якщо порожніх випадків менше трьох.

**Перелік відсіяних перевірено на здатність брехати.** Три SKU з переліку
шукались у фіді — 0 входжень кожен; той самий пошук на офері, узятому з
самого фіду, дає 1. Тобто нуль тут — факт про фід, а не про зламаний `grep`.

**Числа ноутбука й сервера збіглися побайтово**: звіт і перелік кандидатів,
зібрані на обох машинах, мають однакові md5 (`921780bf…`, `02bd04f8…`);
`md5sum` генератора, `homoglyph.py`, довідника фільтрів, словників — теж
однакові на обох машинах ще до початку роботи.

### Стан і що не робилось

**Нічого не публікувалось і жоден фід не перезбирався.**
`output/toptul_rozetka.xml` на сервері лишився тим самим файлом, що й після
прогону 11:40 (22 931 391 Б, `sha256 35578718…`, час не змінився): усі чотири
прогони генератора цієї сесії (два заміри, підстановка правил, вимкнені
гомогліфи) писали у `/tmp`. NOIRE, Prom і Carvol не
торкався (правила 2–4), у БД не писав.

Додано два файли на ноутбуці: `tools/toptul_minparams_report.py` (є й на
сервері, MD5 звірено виводом) і `docs/toptul_minparams_candidates.tsv`.
Оновлено `docs/toptul_minparams_drops.md` і `.tsv` — у переліку тепер чисті
назви після гомогліфів. **Версію `.tsv` від 01.09 при цьому перезаписано**
(файл не в git, копії не лишилось); порівняння наборів SKU зроблено ДО
перезапису, а доказ про гомогліфи спирається не на нього, а на прогін із
вимкненим виправленням. Наявний код не змінювався: `toptul_rozetka_generator.py`,
`toptul_filter_extract.py`, `homoglyph.py` мають ті самі MD5, що й уранці.
Мережа — лише `ssh`/`scp` на сервер; до NVIDIA й до API майданчиків запитів
не було.

**Потрібне від власника:** те саме, що й учора, — `git push` і завершити або
скасувати rebase на сервері (`tools/watchdog.py` там досі `UU`, тобто нагляду
немає). Плюс нове рішення: чи братись за правила «Вид»/«Тип» ще для 8
категорій (+87 карток) зараз, чи після відповіді Rozetka на фід від 01.09.

## 02.09.2026, п'ятий автономний прогін черги

Узято перший `[ ]` — «шестигодинний звіт watchdog нічого не каже про фіди».
Закрито `[x]`.

### Правку вже було зроблено — це видно з першого заміру, а не з памʼяті

Постановка казала: «`send_report()` бере лише git/svc/cron/sync». Це правда
про **закомічену** версію (`git show HEAD:tools/watchdog.py`, сигнатура з
чотирьох аргументів), але не про ту, що працює. У робочому дереві ноутбука й
на сервері лежить версія з фідами, і вони **побайтово однакові**
(md5 `8d41c821…` на обох машинах). Тобто пункт описував стан репозиторію, а
не стан системи; змінювати `watchdog.py` цієї сесії не довелось — жодного
рядка в ньому не змінено, md5 наприкінці той самий, що й на початку.

Роботою сесії стало те, чого бракувало насправді: **доказ, що звіт таки
показує фіди**, і прогін «до / після», якого вимагала постановка.

### Що показує звіт над сьогоднішнім станом — два тексти поруч

Обидва зібрані з тих самих вхідних даних (сервіси живі, git чистий, публікація
Carvol відстала на 33 години — реальний стан сервера на 16:00):

```
ДО (HEAD)                          ПІСЛЯ (те, що на сервері)
🔔 Watchdog OK                     🚨 Watchdog
✅ ×3  ✅ sync                      Фіди:
                                   ✅ Єпіцентр / Rozetka / Prom NOIRE
                                   ❌ Carvol → Rozetka (опублікована
                                      версія старша за 26 год (33))
```

Стара версія над недоставленою публікацією друкує **«Watchdog OK»** — і це не
реконструкція, а вивід самої старої функції, викликаної її ж сигнатурою.

### Перевірку зроблено інструментом, а не оком: `tools/watchdog_report_selftest.py`

Звіт надсилається раз на 6 годин і лише коли всі перевірки відпрацювали, тож
«подивитись, як він виглядає зі зламаним фідом» означало б чекати пів дня і
підгадати аварію. Інструмент збирає звіт із підставлених результатів, а
`tg()` і `save_report_ts()` підмінює заглушками: **жодного повідомлення
власникові й жодного зсуву шестигодинного розкладу**.

Сім випадків, `7/7` на обох машинах (файл побайтово однаковий, md5
`46beea29…`): рядок по кожному з чотирьох фідів є і в доброму звіті;
зламаний фід при цілій решті знімає заголовок «OK»; причина доїжджає текстом,
а не самою позначкою ❌; сервіси, git, cron і sync не загубились.

### Позитивний контроль — два шари, бо одного було замало

**Шар 1.** Ті самі сім випадків проти версії ДО правки: провалено **6 із 7**.

Цього недостатньо, і це варто назвати вголос: стара `send_report` падає на
`TypeError`, бо приймає чотири аргументи замість восьми. «Провалено 6/7» тоді
доводить лише те, що **змінився підпис функції**, а не те, що змінився зміст
звіту. Перевірка, яка ловить не те, що обіцяє, — саме той дефект, проти якого
написано SKILL-21.

**Шар 2.** Тому стара функція викликається **її власною сигнатурою**, і
читається текст: у ньому є «Watchdog OK» і немає ані блоку «Фіди:», ані
жодної з чотирьох назв. Дефект із черги відтворено дослівно, а не за описом.

**Контроль порожнечі.** Два з семи випадків очікують ВІДСУТНІСТЬ ознаки
(зелений звіт не має друкувати 🚨 і не має згадувати «git sync»), інакше
перевірка, яка вміє лише знаходити, і на чистому місці сказала б «знайшов».

### Прогін на реальному стані — там, де звіт споживається

`--live` викликає **справжні перевірки** на сервері й показує текст, не
надсилаючи його. Це відповідь на правило 7 черги: підставні випадки доводять,
що звіт уміє показати фід, але не що він показує **сьогоднішній** фід.

Прогін о 16:04 дав `🚨` із живим `❌ Carvol → Rozetka` — тобто ненульовий
результат на дефекті, який існує прямо зараз, а не на вигаданому.

Знешкоджено, щоб прогін лишався читанням: `check_git()` не викликається
взагалі (він робить авто-коміт), `check_services()` замінено на `is-active`
без перезапуску, файли-прапорці дедуплікації переставлено в тимчасову теку —
інакше мій прогін «погасив» би тривогу, якої справжній watchdog ще не
надсилав. Перевірено: перелік прапорців до і після прогону має однаковий md5
(`80028eb8…`), `/tmp/watchdog_last_report.txt` лишився з 12:32.

### Знайдено побічно: watchdog не працював 29 годин, і мовчання виглядало як «усе гаразд»

Головне за сесію, і знайдено не в коді, а в логу.

**01.09 07:10 → 02.09 12:40 watchdog падав на кожному запуску.** У
`tools/watchdog.py` на сервері лишились маркери конфлікту злиття, рядок 22:
`>>>>>>> 88cc6ce (auto: watchdog sync 2026-08-31 20:20)` →
`SyntaxError: invalid decimal literal`. **176 запусків cron поспіль, 0
успішних.** За ці 29,5 години не надіслано **жодного** звіту й **жодної**
тривоги.

| Що | Значення |
|---|---:|
| Останній звіт від cron | **01.09 07:00** |
| Падінь `SyntaxError` у лозі | **176** |
| Тиша | **29,5 год** |
| Перший живий прогін після | 02.09 12:40 |

Звіт від 12:32 сьогодні є (файл-позначка часу оновлено), але **сліду в
`/tmp/watchdog.log` не лишив** — тобто це ручний запуск під час розбору
конфлікту, а не cron. Наступний автоматичний — близько **18:32**.

Це саме той клас, який лікує пункт черги, але на рівень вище: звіт навчився
не брехати про фіди, а **відсутність звіту** й далі читається як тиша. Мертвий
watchdog і здорова система дають власникові однаковий вигляд екрана. Правку
мовчки не робив (правило 9) — винесено в питання.

### Стан і що не робилось

**Нічого не публікувалось, жоден фід не перезбирався, жодного повідомлення в
Telegram від мене не пішло.** `output/noire_rozetka.xml`, NOIRE, Prom і Carvol
не чіпав (правила 2-4), у БД не писав, cron не змінював.
`tools/watchdog.py` не редагувався — md5 `8d41c821…` на обох машинах, той
самий, що вранці.

Додано один файл: `tools/watchdog_report_selftest.py` (ноутбук і сервер,
md5 звірено). Мережа — `ssh`/`scp` на сервер плюс читання raw-URL і GitHub API
всередині `--live`.

**Потрібне від власника:** `git push` (у репозиторії досі лежить стара
`send_report` — на сервері код новий, у git старий, і наступне злиття може
відкотити правку). Плюс два рішення нижче.
