# Черга задач для автономного продовження

Позначки: `[ ]` очікує · `[~]` у роботі · `[x]` зроблено · `[!]` рішення власника

`tools/resume_worker.sh` бере ПЕРШИЙ `[ ]`, помічає `[~]`, виконує, ставить
`[x]` або `[!]` і дописує замір у `docs/work_journal.md`.
Осиротілий `[~]` робітник повертає в чергу сам.

## План на 02.09.2026

Стан на ранок: NOIRE на Єпіцентрі — 2674 опубліковано, 1358 щойно відправлено
на модерацію, **1760 лишаються** в «Наповненні контентом».

- [x] **Розібрати 49 помилок 400 при заповненні з фіду Prom.**
      Причина: атрибут 5249 «Тип помпи» має тип `multiselect`, а інструмент
      слав рядок замість списку. Виправлено, всі 49 карток заповнено,
      обовʼязкових прогалин у них 0. Деталі — журнал 02.09.
      `tools/epicentr_fill_from_prom.py` записав 1532 картки, 49 відхилено з
      «Invalid request parameters». Перевірено: та сама картка БЕЗ змін
      приймається (200), отже справа в конкретному значенні, яке ми додаємо.
      Артикули видно в `/tmp/fillprom.log` (або перезапустити з `--limit`).
      Повідомлення про помилку в інструменті обрізане до 140 символів —
      спершу розширити, інакше причину не побачити.
      Готово = названо причину поіменно й або виправлено, або записано в
      перелік із поясненням, чому ці картки не заповнюються.

- [x] **Порахувати, чого бракує решті 1760 карток.**
      Зроблено: `docs/epicentr_remaining_gaps.md` (+ `.tsv` порядково).
      Головне число: **1037 прогалин у 545 картках** заповнюються значеннями,
      які в нас уже є і дозволені довідником; **322 картки** закриваються
      повністю. Джерело — не фід Prom (він дає лише 19), а
      `sexopt_extracted_params`, з якої фід і збирається. Деталі — журнал 02.09.
      Не «скільки», а ЯКИХ САМЕ полів і в яких категоріях. Найбільші групи:
      Вібратори 301, Засоби для еротичного масажу 130, Еротичні приколи 117,
      Аксесуари для страпонів 109, Аксесуари до інтимних іграшок 107.
      Згрупувати «категорія × набір відсутніх полів», як робилось 24.08 —
      тоді пʼять груп покривали 60% завалу.
      Готово = `docs/epicentr_remaining_gaps.md`: перелік груп із числами й
      позначкою, чи є значення в наших даних (назва, опис, фід Prom) — це
      відповідь на питання власника «чого нам не вистачає, щоб завершити».

- [!] **Закрити найбільшу групу з тих, де ознака є в даних.**
      Заповнення **зроблено й звірено з кабінетом**: 1034 поля в 542 картках,
      порожніх обовʼязкових 7263 → 6229, карток без прогалин 52 → **371**.
      Розбіжність зі звітом (обіцяв 1037/545/322) — рівно 3 картки, заповнені
      раніше як наскрізний контроль; арифметика сходиться в усіх трьох числах.
      Записав прогін 02.09 08:06, який не лишив запису в журналі; ця сесія
      його знайшла, перевірила й задокументувала. Деталі — журнал 02.09.
      **Чекає на власника:** дозвіл `epicentr_moderate` згас о 09:03, тому
      371 готова картка стоїть у «Наповненні контентом». Перелік —
      `docs/epicentr_moderate_ready_20260902.tsv`. Дві команди:
      ```bash
      python3 -m shared.utils.consent --allow epicentr_moderate --hours 4 \
        --why "02.09: 371 картка після заповнення з sexopt_extracted_params"
      python3 tools/epicentr_moderate_ready.py
      ```
      Дозвіл собі не видавав: перехід односторонній, а запобіжник, який агент
      обходить сам, перестає бути запобіжником.

- [x] **TOPTUL: гомогліфи у 261 картці.**
      Літерний клас закрито 01.09, цього прогону закрито найбільший
      залишок — **кирилична `З` на місці цифри 3: 554 вживання у 90
      офферах → 0**. Мішаних токенів у фіді 175 → **83** (27 → 16 різних,
      61 → 34 оффери). Артикулів, цін, категорій, фото й характеристик
      змінено 0 із 5783; валідатор 0/0, 100%. Деталі — журнал 02.09.
      **Нуля не досягнуто, і це не «майже»**: решта 83 розкладена поіменно
      в `docs/toptul_homoglyph_left.md` — 20 зіпсований текст поза
      гомогліфами, 10 вставлені `FF`, 51 законна суміш (`Vрес`, `тORIN`),
      2 заморожений артикул. Заміна в них не виводиться з написання,
      а вигадувати заборонено (правило 5).
      Фід перезібрано на сервері, **не публікувався**; жоден cron його не
      чіпає — перевірено.

- [x] **TOPTUL: переміряти 1179 товарів, що випадають («менше 3 характеристик»).**
      Переміряно тим самим генератором на сервері: **1179 → 916**, офферів
      5520 → **5783**. Приріст дала перестановка `MIN_PARAMS` 01.09; робота з
      гомогліфами (01.09 літери, 02.09 цифри) не повернула **жодного** —
      набір із 916 SKU той самий до одного, змінились лише тексти назв.
      Арифметика замкнута: 6904 оффери постачальника = 5783 + 916 + 128
      («категорія без rz_id») + 77 («недовірений рівень»), інших причин 0.
      Перелік із причиною — `docs/toptul_minparams_drops.md` (оновлено) і
      `.tsv` (916 SKU). Деталі — журнал 02.09.
      **Знайдено запас на 87 карток** (`docs/toptul_minparams_candidates.tsv`):
      у їхніх назвах стоїть значення, яке Є в довіднику Rozetka, а правила
      для категорії немає (`Знімачі` 79 із 226). Число не прогноз — генератор
      із тимчасово підставленими правилами дав 916 → **829**. Мовчки не
      робив (правило 9): серед збігів є хибні за суттю («Утримувач рулону
      паперу для інструментальної **візки**» → «Тип: Візки»), тож правило
      треба на кожну категорію окремо й з переглядом очима.

- [x] **Шестигодинний звіт watchdog нічого не каже про фіди.**
      Правку вже було зроблено 01.09 і розгорнуто: `watchdog.py` на ноутбуці
      й на сервері побайтово однаковий (md5 `8d41c821…`), і цієї сесії в
      ньому **не змінено жодного рядка**. Постановка описувала закомічену
      версію, а не робочу. Бракувало доказу — його й зроблено:
      `tools/watchdog_report_selftest.py`, **7/7 на обох машинах**.
      Прогін «до / після» на сьогоднішньому стані: стара версія над
      публікацією Carvol, відсталою на 33 год, друкує **«Watchdog OK»**;
      нова — `🚨` з рядком `❌ Carvol → Rozetka`. Позитивний контроль у два
      шари (сам «провалено 6/7» доводив лише зміну сигнатури, тому стару
      функцію викликано її ж підписом і прочитано текст). `--live` на сервері
      зібрав звіт із **реальних** перевірок — ненульовий результат на дефекті,
      що існує зараз. Телеграм і розклад не зачеплено, прапорці дедуплікації
      з тим самим md5 після прогону. Деталі — журнал 02.09.
      **Побічно знайдено серйозніше:** watchdog **не працював 29,5 год**
      (01.09 07:10 → 02.09 12:40) — маркери конфлікту в рядку 22,
      `SyntaxError`, **176 падінь cron поспіль, 0 звітів, 0 тривог**. Мовчки
      не правив (правило 9) — у питаннях нижче.

## Правила цієї черги

1. **Межі — SKILL-22.** Прочитати перед першою дією.
2. **НІЧОГО НЕ ПУБЛІКУВАТИ.** Окремо про TOPTUL: фід опубліковано 01.09,
   Rozetka ще не відповіла. Нова публікація до відповіді зробить зворотний
   звʼязок безглуздим — ми не знатимемо, що саме перевіряли.
3. **Не чіпати `output/noire_rozetka.xml`** — 4147 карток на модерації.
4. **Carvol — тільки читання.**
5. **Не вигадувати значення характеристик.** Немає в даних — лишити порожнім
   і виписати перелік власникові.
6. **Позитивний контроль обовʼязковий:** перевірка, що дала нуль, не
   зарахована, доки не доведено, що вона вміє повернути ненуль (SKILL-21).
7. **Перевіряти результат там, де він споживається** — у фіді чи кабінеті,
   а не в коді. 25.08 код виправили, фід лишився старий, задача виглядала
   закритою тиждень.
8. **Спершу шукати дані у власних фідах** (SKILL-25): 01.09 це дало 1532
   картки за пів години проти дня роботи з правилами.
9. Знайшов дотичну проблему — записати в журнал, не братися мовчки.
10. Черга вичерпалась — зупинитись, нову роботу собі не вигадувати.

## Відкриті питання власникові

- **Латинська `B` замість `В` у TOPTUL** (клас `B`): 572 вживання у 109
  офферах, з них 302 — законна латиниця в назвах моделей (`PT-29B`,
  `H-827B`, `Фреза 45B`), тому наївне правило зіпсувало б більше, ніж
  виправило. У характеристиках цього класу **0**, тобто на фільтри Rozetka
  він не впливає. Розбір — `docs/toptul_homoglyph_left.md`, розділ B.
- **Правила «Вид»/«Тип» ще для 8 категорій TOPTUL** (02.09): повертають у фід
  87 карток із 916 відсіяних, перевірено прогоном генератора (916 → 829).
  Робота на пів дня — правило пишеться на кожну категорію окремо й
  переглядається очима, бо серед збігів є хибні за суттю. Перелік —
  `docs/toptul_minparams_candidates.tsv`. Питання: братись зараз чи після
  відповіді Rozetka на фід від 01.09.
- **Мертвий watchdog виглядає як здорова система** (02.09): 29,5 год тиші
  01.09-02.09 ніхто не помітив, бо відсутність звіту нічим не відрізняється
  від спокійної доби. Звіт навчився не брехати про фіди, але сам факт «звіту
  не було» ловити нічим. Лікується сторожем ззовні watchdog (перевірка віку
  `/tmp/watchdog_last_report.txt` окремим записом cron або systemd-таймером) —
  півгодини роботи. Питання: робити зараз чи разом із наступною правкою
  watchdog.
- **У звіт не входить `check_rozetka_messages()`** (02.09): результат
  рахується щоцикла й пишеться в лог, але в шестигодинний звіт не потрапляє —
  той самий клас сліпоти, що й фіди до 01.09. Повідомлення від Rozetka
  надсилаються один раз на новий `id`; пропустив сповіщення — і звіт про це
  мовчить. Правка на кілька рядків, але це зміна поведінки живого
  сповіщувача, тому не мовчки.
- **Стара `send_report` досі в git** (02.09): на сервері код новий, у
  репозиторії закомічена версія без фідів. Наступне злиття гілок здатне
  відкотити правку — саме таке злиття 31.08 і лишило маркери конфлікту, які
  вбили watchdog на добу. Потрібен `git push` із ноутбука.
- **Фото** — вимога обох майданчиків. Відкладено рішенням власника 01.09.
- **Стоп-категорії Rozetka**: 1507 товарів у 16 категоріях, лист написано.
- **Запити на відгук Rozetka**: нуль на 22 відвантажені замовлення; методу
  в API немає, лише з кабінету вручну.
- **RozetkaPay**: майданчик просить увімкнути онлайн-оплату окремим листом.
