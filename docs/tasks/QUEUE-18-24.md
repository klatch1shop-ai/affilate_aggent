# Черга завдань 18–24 (з досліджень 19–20.09)

Джерела: `docs/COMMAND_MATRIX.md` (фази A–E), `docs/research/SOCIAL_CONTENT_2026.md`,
скіли `video-analysis`, `skill-37-marketplace-attributes`. Тести пишу перед постановкою кожного.

| № | Завдання | Навіщо | Стан |
|---|---|---|---|
| 18 | **Ядро контент-заводу**: теми з питань покупців і каталогу, перевірка сценарію (заборонені твердження, числа лише з фактів, позначка ШІ), субтитри, команда ffmpeg | просування в соцмережах без ризику «неавтентичного» контенту | ⏳ у Codex |
| 19 | **Пакетний аналіз відео конкурентів**: `content/competitors.py` — список посилань → Gemini (перший прохід) → зведення: гачки, довжина, формат, теми; вихід — таблиця й теми для нас | дослідження показало: «що працює» треба брати з реальних роликів ніші, а не з припущень | тести писати |
| 20 | **Перенесення звітів у Obsidian** (`content/ingest.py`): звіт → сутності й поняття → `wiki/`, рядок у `log.md`; ідемпотентно, без перезапису чужих тверджень | сховище встановлено 20.09; знання мають накопичуватись, а не губитись у scratchpad | тести писати |
| 21 | **Гібридний розбір команд** (фаза A/B матриці): правила → Gemini як запасний із каталогом команд і перевіркою параметрів; заперечення («не треба») → уточнення | тренд 2026: гібрид дає точність LLM зі швидкістю правил | тести писати |
| 22 | **R2-дії через підтвердження** (фаза C): ТТН у замовлення Rozetka, «в обробці», продовження резерву, позначки чатів — усе через `helper/confirm.py` | перші дії із записом, з кнопкою й журналом | після 21 |
| 23 | **R3-сценарії** (фаза D): ТТН Нової Пошти, лист постачальнику, відповідь покупцю (чернетка Gemini → ✅), повернення | те, заради чого будувався механізм підтвердження | кожне — з окремим дозволом власника на `live` |
| 24 | **Голос** (фаза E): голосове повідомлення → Groq Whisper → той самий розбір; голос ніколи не підтверджує R2/R3 | початкова мета власника — голосове керування | після 21–23 |

**Паралельно, без Codex (роблю сам):**
- дозаповнення атрибутів для решти косметики на Rozetka (≈230) і Prom (≈260) — метод SKILL-37;
- підключення `helper/confirm.py` і кнопок «✅ / ❌ / прочитав» в оболонці бота;
- перевірка `POST /messages/create` (відповідь покупцю) на живому чаті — лише з дозволу власника.

## Додано 20.09 (з досліджень про автономний цикл і відео)

| № | Завдання | Навіщо |
|---|---|---|
| 25 | **Пам'ять гіпотез**: таблиця й модуль (привід → гіпотеза → докази → рішення → факт через 7/14/30 днів → оцінка) | без звірки з фактом система не вчиться |
| 26 | **Спір «за / проти»**: два прогони на тих самих доказах + зведення «за / проти / чого бракує» | головне запозичення з TradingAgents; одна модель погоджується сама з собою |
| 27 | **Ризик-вето**: правила власника як перевірки над планом дії | поріг ціни, три стани наявності, заморожений зміст фідів, політики майданчиків |
| 28 | **Воронка товару**: один звіт по SKU (замовлення, повернення, питання, відгуки, ціна конкурентів) | зімкнути товарну й контентну воронки |
| 29 | **Озвучка й дубляж**: сценарій → `edge-tts` (укр. голос) → тайминг під субтитри → зведення ffmpeg; режим «переклад чужого відео» лише для внутрішнього вивчення | перевірено: голоси `uk-UA-Ostap/Polina` працюють безкоштовно |
| 30 | **Монтаж із шаблонів**: зум/акценти, підкладка, титри з характеристик товару поверх TASK-18 | щоб ролики не виглядали як слайдшоу (вимога YouTube до «неавтентичного» контенту) |
| 31 | **Перевірка фактів за фото**: Gemini читає упаковку з фото товару → звірка з даними постачальника (вага, обʼєм, склад) → перелік розбіжностей | 20.09 знайдено: у постачальника 190 г, на упаковці 180 g — помилка пішла б у картку й у ролик |
| 32 | **Радар трендів**: список запитів → `yt-dlp ytsearch` → нові ролики → `video-analysis` → щотижневий звіт «що змінилось у ніші» | власник: «як знаходити свіжі відео й підходи» |

## Стан на 20.09.2026, 12:10 (після другого блоку роботи з Codex)

| № | Завдання | Модуль | Тестів | Стан |
|---|---|---|---|---|
| 18 | ядро контент-заводу | `content/topics,script,subtitles,ffmpeg_cmd` | 20 | ✅ прийнято |
| 19 | розбір відео конкурентів | `content/competitors.py` | 9 | ✅ прийнято |
| 20 | звіти → Obsidian | `content/ingest.py` | 15 | ✅ прийнято |
| 25 | пам'ять гіпотез | `research/hypotheses.py` | 26 | ✅ прийнято |
| 26 | спір «за / проти» | `research/debate.py` | 14 | ✅ прийнято |
| 27 | ризик-вето | `research/veto.py` | 17 | ✅ прийнято |
| 28 | воронка товару | `research/funnel.py` | 14 | ⏳ у Codex |
| 29 | озвучка | `content/voice.py` | 6 | ✅ прийнято |
| 30 | монтаж із шаблонів | `content/edit_plan.py` | 20 | тести написані |
| 31 | факти за фото упаковки | `content/photo_facts.py` | 28 | ✅ прийнято |
| 32 | радар трендів | `content/trends.py` | 16 | ✅ прийнято |
| 33 | **дослідження**: що може змінити дані назовні | `docs/research/AUDIT-WRITE-PATHS.md` | — | у черзі |

**Залишилось з фаз матриці команд:** 21 (гібридний розбір), 22 (R2 через підтвердження),
23 (R3-сценарії), 24 (голос) — після того, як кнопки `helper/confirm.py` під'єднані до бота.

**Уроки цього блоку** (записані в скіл `codex-worker`, пастки 11–12):
- не комітити приймальні тести завдання, яке ще не запущене: перевірка «решта тестів»
  падає на `ModuleNotFoundError` і спалює спроби (так згоріло 3 спроби на TASK-26);
- не писати у репозиторій, поки в ньому працює Codex — чернетки тримати у scratchpad.

**Ліміти 20.09:** наш запобіжник 15/добу піднято змінною оточення до 25 на прогін;
справжні ліміти OpenAI на той момент — 14 % за 5 год і 30 % за тиждень.
