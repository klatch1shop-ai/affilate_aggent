# Черга масивних завдань для Codex (agent-system)

**Затверджено власником 19.09.2026:** «підтверджую, дай йому, склади йому перелік масивних завдань».
**Ведуть:** рецензент (Claude). Процес — скіл `codex-worker`. Codex отримує завдання лише
з файлу `TASK-NN-*.md` із тестами, написаними наперед; цей перелік — план, а не завдання.

**Принцип відбору.** Codex пише **ядро**: чисту логіку з підставленими ззовні HTTP, часом і
базою, перевірену тестами. **Підключення** до живих агентів, бота, cron і кабінетів робить
Claude окремим кроком і лише з дозволу власника. Тому кожен пункт має колонку «живий крок».

| # | Завдання | Навіщо власнику | Обсяг (оцінка) | Живий крок після | Стан |
|---|---|---|---|---|---|
| 02 | Команди бота: намір → відповідь | голосове/текстове керування | 1 файл, 16 тестів | підключити до бота | ✅ прийнято 629f842 |
| 03 | Єдиний чат покупців — ядро | усі повідомлення покупців в одному Telegram | 8 файлів, 63 тести | опитувач + підключення (дозвіл) | ✅ прийнято f769678 |
| 04 | Епіцентр `/v1/offers` — ядро синхронізації цін і наявності | замість сліпого фіду — результат по кожному артикулу | 2 файли, 49 тестів | пробні 1–3 артикули (дозвіл) | ✅ прийнято (TASK-04, 181 с) |
| 04b | Доробка 04 (прогалини завдання, знайдені рецензією): `pending` старше 14 діб → «застаріло», не питати вічно; `forbidden` «артикул не знайдено» — відкладати, а не слати щоразу (тисячі зайвих записів); нормалізація `sku` у відповіді | без цього перша ж жива синхронізація зациклиться на чужих артикулах | 1 файл, ~12 тестів | — | заплановано, перед живим запуском 04 |
| 05 | **Адаптери API** (лише читання): Rozetka — замовлення й чати; Нова Пошта — статус ТТН (з повтором на «To many requests», без телефонів); Prom — повідомлення й замовлення | дає живі дані і 03, і 02; 401 → видима помилка, а не «нічого нема» | 4–5 файлів, ~50 тестів | прогін лише читанням на сервері | заплановано |
| 06 | **Сервісний шар бота:** `fetchers` для 02 із адаптерів 05; один цикл опитувача чату (зібрати → картка → надіслати → `link_tg`); нагадування «⏰ без відповіді»; маршрут реплаю власника → `plan_reply` | робить 02 і 03 придатними до підключення | 3–4 файли, ~40 тестів | підключення до `main.py` (дозвіл) | заплановано |
| 07 | **Ревізор замовлень «жодне не загубилось»**: зводить замовлення майданчиків, відправлені постачальникам, ТТН і скасування → перелік аномалій (підтверджене без ТТН > N год; відправлене постачальнику без ТТН до 21:00; скасоване, але постачальнику лист пішов; оплачене без відправки; ТТН без руху > 3 днів) | головна тривога власника — замовлення, що «відпадає по дорозі» | 3 файли, ~40 тестів | щоденний звіт у Telegram (дозвіл) | заплановано |
| 08 | **Перевірка нового фіду Rozetka до модерації** (SKILL-36, гейти 3–6 як код): одиниці в характеристиках; опис лише про одну одиницю; узгодженість назва ↔ характеристики ↔ опис (розміри, площа); фото (кількість, розмір з метаданих, дублі URL); заборонені слова; порожні обов'язкові | після модерації зміст не змінити — усе ловити ДО подачі (dropoffice, наступні постачальники) | 4 файли, ~50 тестів | прогін на наявних фідах (лише читання) | заплановано |
| 09 | **Характеризаційні тести** наявної логіки замовлень без зміни коду: `toptul_supplier` (дата ТТН, рядок оплати, три стани наявності), `payment_paid`, розподіл позицій за постачальниками, `np_create_ttn` (поштомат, вага, післяплата) | страховка перед будь-якою правкою агентів | лише тести, ~60 | — | заплановано; потребує окремої теки для тестів Codex |
| — | Пілот: облік кампаній, витрат і комісій (KROK-10 V0) | потрібен до першого платного розміщення | ~8 файлів | — | рішення менеджера пілота |

**Відкинуто:** дефект `_is_adj` у генераторі ключів Prom — у коді вже є вузьке правило
`_strict_pair` (перевірено 19.09). Прогін моделей Ollama/LLM — не для Codex.

**Порядок:** 04 → 05 → 06 (разом дають готові до підключення чат і команди) → 07 → 08 → 09.
Між завданнями — перевірка квоти (`--status` + справжні відсотки OpenAI з журналу сесії).
