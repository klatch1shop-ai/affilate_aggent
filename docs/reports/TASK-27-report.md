# Звіт TASK-27

## Що зроблено

- Створено `research/veto.py`: константи з інтерфейсу завдання, `check`, `verdict`, `report`.
- Сім правил перевіряються у порядку `RULES`, кожне додає не більше одного порушення. Режим `test` не послаблює правил; підтвердження приймається лише як `True`.
- Формування повідомлень і звіту використовує імпортований `tg_dispatcher.privacy.mask_private`.
- Модуль не друкує, не пише файлів і не виконує мережевих дій. Тести та інші модулі не змінено.

## Перевірка

Для обох запусків встановлено `PYTHONDONTWRITEBYTECODE=1` та `PYTEST_ADDOPTS='-p no:cacheprovider'`, щоб не створювати байткод і кеш pytest.

Команда:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTEST_ADDOPTS='-p no:cacheprovider' venv/bin/python -m pytest tests/test_veto.py -q
```

Дослівний вивід (код завершення 0):

```text
...................                                                      [100%]
19 passed in 0.03s
```

Команда:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTEST_ADDOPTS='-p no:cacheprovider' venv/bin/python -m pytest tests/ -q -k 'not veto'
```

Дослівний вивід (код завершення 0):

```text
........................................................................ [ 12%]
........................................................................ [ 25%]
........................................................................ [ 37%]
........................................................................ [ 50%]
........................................................................ [ 62%]
........................................................................ [ 75%]
........................................................................ [ 88%]
....................................................................     [100%]
572 passed, 19 deselected in 0.38s
```

## Самостійні рішення

- Ціни порівнюються через `Decimal(str(value))`, без попереднього округлення; у повідомленні відображаються два десяткові знаки зі стандартним округленням Decimal (`ROUND_HALF_EVEN`). Числові рядки приймаються; `NaN`, нескінченність і булеві значення трактуються як нечислова ціна та зупиняють дію.
- Контакти маскуються також у повідомленнях про некоректні значення та у готовому звіті, щоб приватні дані не потрапили у відповідь через метадані дії.
- Для повторів захищеного шляху повертається лише перше порушення; порожні значення складових заголовка звіту пропускаються.

## Що не вдалося, питання

Невиконаних пунктів і питань немає. Початковий `git status --short` був порожнім. Зовнішніх дій, встановлення пакетів, комітів та операцій із гілками не виконувалося.
