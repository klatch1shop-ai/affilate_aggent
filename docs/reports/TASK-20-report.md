# Звіт TASK-20: підготовка нотаток сховища

**Дата:** 20.09.2026

## Що зроблено

- Створено `content/ingest.py` з інтерфейсом `VAULT`, `slugify`, `parse_report`, `wikify`, `build_note`, `log_line`, `plan`.
- Реалізовано розбір секцій і дати, формування шляхів, YAML-заголовка та журналу, визначення пропусків і конфліктів без перезапису.
- Тіло нотатки, включно із заголовками секцій, проходить імпортований `mask_private`, потім `wikify` один раз для всього тіла.
- Модуль не друкує, не записує файлів і не звертається до мережі. Тести й інші модулі не змінювалися.

## Перевірка

Для уникнення побічних файлів використано `PYTHONDONTWRITEBYTECODE=1` та `PYTEST_ADDOPTS='-p no:cacheprovider'`.

Команда:

```sh
PYTHONDONTWRITEBYTECODE=1 PYTEST_ADDOPTS='-p no:cacheprovider' venv/bin/python -m pytest tests/test_ingest.py -q
```

Дослівний вивід (код завершення 0):

```text
................                                                         [100%]
16 passed in 0.01s
```

Команда:

```sh
PYTHONDONTWRITEBYTECODE=1 PYTEST_ADDOPTS='-p no:cacheprovider' venv/bin/python -m pytest tests/ -q -k 'not ingest'
```

Дослівний вивід (код завершення 0):

```text
........................................................................ [ 12%]
........................................................................ [ 25%]
........................................................................ [ 38%]
........................................................................ [ 51%]
........................................................................ [ 64%]
........................................................................ [ 77%]
........................................................................ [ 90%]
....................................................                     [100%]
556 passed, 16 deselected in 0.35s
```

## Самостійні рішення

- Календарно неможлива дата повертається як `None`; перевірка через стандартний `datetime.date`.
- Наявні `[[...]]` залишаються незмінними; першою для нового посилання вважається перша згадка поза ними.
- Значення YAML з двокрапкою та іншими спеціальними символами екрануються через `json.dumps(..., ensure_ascii=False)`, щоб зберегти кирилицю та лапки.
- Маскування застосовано до тіла за контрактом завдання; метадані заголовка й джерела збережено як передані викликачем.

## Що не вдалося, питання

Немає. Обидві перевірки успішні. Початковий робочий каталог був чистий; змінено лише два дозволені файли. Зовнішніх дій і git commit не виконувалося.
