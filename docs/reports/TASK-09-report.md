# Звіт TASK-09

## Що зроблено

- `tg_dispatcher/ai_brain/intents.py`: додано 17 намірів читання та параметри
  `status`, `source`, `goods_tab`, `article`. Уточнення біля номерів, наявність
  у постачальника й однозначні комбінації перевіряються в порядку §5.1.
  Артикул шукається в сирому тексті зі збереженням дефісів, лише за відсутності
  NOIRE SKU. Контракт `parse`, старі параметри та `mutating=False` збережено.
- `tg_dispatcher/ai_brain/catalog.py`: створено каталог усіх 25 намірів, рівні
  ризику, порядок груп, `by_group()` та фільтровану `help_text()` без команди `help`.
- Змінено лише два дозволені модулі й цей звіт. Тести не змінювалися.
  Комітів, операцій із гілками, встановлення пакетів і зовнішніх дій не було.

## Перевірка

Запуски без запису байткоду та кешу pytest.

```sh
PYTHONDONTWRITEBYTECODE=1 PYTEST_ADDOPTS='-p no:cacheprovider' venv/bin/python -m pytest tests/test_intents_v2.py tests/test_voice_intents.py -q
```

Дослівний вивід, код завершення 0:

```text
........................................................................ [ 56%]
.......................................................                  [100%]
127 passed in 0.05s
```

```sh
PYTHONDONTWRITEBYTECODE=1 PYTEST_ADDOPTS='-p no:cacheprovider' venv/bin/python -m pytest tests/ -q -k "not intents_v2 and not voice_intents"
```

Дослівний вивід, код завершення 0:

```text
........................................................................ [ 31%]
........................................................................ [ 62%]
........................................................................ [ 93%]
..............                                                           [100%]
230 passed, 127 deselected in 0.23s
```

Додатковий локальний запуск Python з `assert` перевірив варіанти статусів,
відмінки назв постачальників, артикул `ABCDEF12345678-Z`, впевненість 1.0 для
однозначних фраз і порожню довідку для множин `set()`, `{'help'}`, `{'unknown'}`.
Код завершення 0, дослівний вивід:

```text
Додаткові перевірки параметрів, пріоритетів і порожньої довідки: успішно
```

`git diff --check`: код завершення 0, вивід порожній.
На початку робоче дерево було чисте; сторонніх змін не виявлено.

## Самостійні рішення

- Питання й відгуки віднесено до групи «Покупці», стан фідів — до «Система».
  Назви старих команд і їхні `needs` відповідають `commands.COMMANDS`.
- Каталог не імпортує виконавців команд і не підключається до живого бота:
  це окремий модуль метаданих у межах фази A1.
- Для наявності у постачальника також підтримано прийменник «в»; для лічильників —
  російські «сколько» й «количество». Вони використовують ті самі пріоритетні правила.
- Додано специфічний ключ «грошей на баланс», щоб фраза з «скільки» визначала
  `balance` зі звичайною впевненістю 0.8, зберігаючи старий механізм оцінки ключових слів.
- Для суперечливих значень одного нового параметра використано перший збіг
  у таблиці; старе правило пріоритету ТТН над номером замовлення збережено.

## Що не вдалося, питання

Немає. Обидві перевірки приймання успішні; усі 42 старі тести збережено зеленими.
