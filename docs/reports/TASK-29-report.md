# Звіт TASK-29

## Що зроблено

- Створено `content/voice.py`: константи `VOICES`, `PAUSE`, функції `plan_lines`, `synthesize`, `align`, `concat_list` за інтерфейсом завдання.
- Синтез і вимір тривалості передаються функціями. Модуль не виконує мережевих викликів.
- Помилка синтезу однієї репліки повертається окремим сегментом із назвою типу винятку, без тексту винятку; наступні репліки обробляються.
- Створено цей звіт. Тести й інші файли не змінювалися; комітів і зовнішніх дій не виконано.

## Перевірка

Кеш pytest і запис байткоду вимкнено, щоб не створювати побічних файлів.

```sh
PYTHONDONTWRITEBYTECODE=1 PYTEST_ADDOPTS='-p no:cacheprovider' venv/bin/python -m pytest tests/test_voice.py -q
```

Дослівний вивід (код завершення 0):

```text
......                                                                   [100%]
6 passed in 0.01s
```

```sh
PYTHONDONTWRITEBYTECODE=1 PYTEST_ADDOPTS='-p no:cacheprovider' venv/bin/python -m pytest tests/ -q -k "not voice"
```

Дослівний вивід (код завершення 0):

```text
........................................................................ [ 17%]
........................................................................ [ 34%]
........................................................................ [ 51%]
........................................................................ [ 68%]
........................................................................ [ 85%]
.............................................................            [100%]
421 passed, 51 deselected in 0.33s
```

## Самостійні рішення

- Шляхи мають послідовні імена `line_0000.mp3`, `line_0001.mp3` тощо в `out_dir`. Підготовка каталогу залишається викликачеві або переданому синтезатору; модуль сам файлів і каталогів не створює.
- Валідація голосу передує обходу реплік; прямий код перевіряється за шаблоном `xx-XX-NameNeural` із латинськими літерами в назві.
- Винятки `probe` поширюються викликачеві: завдання визначає відновлення лише після помилок `synth`.
- Кожен запис concat завершується переносом рядка; порожній список дає порожній рядок.

## Що не вдалося, питання

Немає. Обидві перевірки пройшли. Початковий `git status --short` був порожнім.
