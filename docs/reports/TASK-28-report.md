# Звіт TASK-28

## Що зроблено

- Створено `research/funnel.py`: константи `WINDOWS`, `KNOWN_GAPS`, функції `build` та `report`.
- Реалізовано включні часові вікна, підрахунок замовлень, виконаної суми, викупу, повернень, відкритих питань і відгуків, порівняння цін та впорядкований перелік прогалин.
- Звіт українською форматує суми й відсотки та показує лише відомі ціни. Відсутні показники не підмінюються нулями.
- Модуль працює лише з переданими даними: без мережі, запису на диск і друку в stdout. Тести та інші модулі не змінено; коміт не створювався.

## Перевірка

Змінні оточення вимикають створення байткоду та кешу pytest.

```bash
PYTHONDONTWRITEBYTECODE=1 PYTEST_ADDOPTS='-p no:cacheprovider' venv/bin/python -m pytest tests/test_funnel.py -q
```

Дослівний вивід (код завершення 0):

```text
.........                                                                [100%]
9 passed in 0.02s
```

```bash
PYTHONDONTWRITEBYTECODE=1 PYTEST_ADDOPTS='-p no:cacheprovider' venv/bin/python -m pytest tests/ -q -k 'not funnel'
```

Дослівний вивід (код завершення 0):

```text
........................................................................ [ 12%]
........................................................................ [ 24%]
........................................................................ [ 36%]
........................................................................ [ 48%]
........................................................................ [ 60%]
........................................................................ [ 73%]
........................................................................ [ 85%]
........................................................................ [ 97%]
...............                                                          [100%]
591 passed, 9 deselected in 0.38s
```

## Самостійні рішення

- Якщо мінімальна ціна конкурента дорівнює нулю, `price_gap = None`: відсоткова різниця від нульової бази не визначена. Відомі ціни залишаються у звіті, порівняння у відсотках не виводиться. Цей крайовий випадок не визначений завданням і не покритий приймальними тестами.
- За відсутності конкурентів `min_price` і `min_seller` дорівнюють `None`.
- Часовий відбір використовує лише задану нижню межу `created >= now - timedelta(days=N)`.

## Що не вдалося, питання

Немає невиконаних пунктів або блокувальних питань. Обидві перевірки успішні. До початку роботи робоче дерево було чистим.
