("1. РОЗПОДІЛ РОБОТИ (ДЕРЕВО РІШЕНЬ)\n\nОзнаки задачі (вирішальні критерії): вартість помилки, необхідність творчого планування, обсяг контексту, наявність специфічних платформних API.\n\nДерево рішень:\n1. Чи є логіка жорсткою (парсинг конкретного формата XML від постачальника, переклад статусів замовлень, арифметичне зведення цін)?\n   -> **Детермінований код (без LLM)**. Python-скрипт або SQL-запит.\n   *Ефект:* 0 токенів, 100% надійність. Вимірюється відсутністю викликів LLM у логах.\n\n2. Чи вимагає задача генерації коду/скриптів за чітким ТЗ і наявними приймальними тестами, де є високий ризик галюцинацій архітектури?\n   -> **Codex (GPT)** у пісочниці.\n   *Ефект:* Економія лімітів Claude Opus, перевірка автотестами. Вимірюється кількістю спроб (ціль — з 1-ї спроби, як у вас зараз).\n\n3. Чи це масова рутинна обробка великих об'єктів (розбір текстів, категоризація товарів, фільтрація відео, порівняння характеристик без складного програмування)?\n   -> **Gemini (AI Studio, безкоштовний тариф)**.\n   *Ефект:* Нульова вартість (до 429 ліміту). Вимірюється відсотком задач, знятих з платних моделей.\n\n4. Чи вимагає задача архітектурного планування, декомпозиції, написання складних ТЗ для інших моделей, прийняття фінальних рішень та інтеграції?\n   -> **Claude Code (Opus)**.\n   *Ефект:* Мінімізація помилок на старті (бо вартість переробки висока). Вимірюється кількістю успішно прийнятих модулів з першого разу.\n\n---\n\n2. ЧОГО НЕ ВАРТО РОБИТИ ГОЛОВНОМУ АГЕНТУ ВЗАГАЛІ\n\n*   **Читання повного лістингу великих файлів коду чи логів у пошуках багу.** Замість цього Opus має формувати точний пошуковий запит або делегувати пошук Codex/скриптам через grep/ripgrep.\n*   **Генерація масиву рутинного коду (boilerplate).** Написання простих CRUD-ендпоінтів чи стандартних міграцій бази даних через Opus — спалювання дорогих токенів. Це робить Codex.\n*   **Пряме виконання довгих обчислень чи тестування «в голові».** Claude не повинен симулювати виконання тестів чи SQL-запитів. Для цього є пісочниця Codex та локальна база Postgres.\n\n*Очікуваний ефект:* Зменшення споживання токенів Claude Opus на 30–40%.\n*Як виміряти:* Динаміка токенів на одну сесію Claude Code (за логами CLI).\n\n---\n\n3. ВИВІД ІНСТРУМЕНТІВ\n\nПрийоми роботи з довгим виводом:\n1. **Заборона виводу у контекст за замовчуванням (Truncation / Head-Tail).** Усі інструменти (pytest, git diff, psql) повинні налаштовуватись так, щоб повертати лише **статус (успіх/помилка)** та перші/останні 5 рядків помилки, якщо вона є.\n2. **Збереження у файли-артефакти.** Повний вивід тестів чи логів записується у `tmp/test_output.log`, а в контекст іде лише рядок: `Tests failed: see tmp/test_output.log for lines 45-50`. Агент читає файл точково через `sed` або `tail`, якщо це необхідно.\n3. **Суммаризація через дешеву модель.** Якщо потрібен аналіз великого логу помилок, його спочатку пропускають через скрипт, що витягує стек-трейс, або відправляють на локальну Ollama/Gemini для стиснення до 3 тез.\n\n*Очікуваний ефект:* Скорочення довжини сесії Claude на довгих виводах тестів. Вимірюється кількістю токенів у блоках `tool_output`.\n\n---\n\n4. ПАМʼЯТЬ І СТИСНЕННЯ\n\n*Припущення:* Ви використовуєте вбудоване автостиснення Claude Code або власну систему коротких файлів-фактів.\n\nЯк організувати:\n1. **Жорсткий поділ пам'яті:**\n   *   *Що лишається:* Структура БД (схема), поточний статус завдань (`docs/tasks/`), список критичних правил/інваріантів (наприклад, «не чіпати захищені тести»). Це має бути в перших рядках індексу.\n   *   *Що видаляється:* Деталі пройдених рефакторингів, проміжні думки агента, старі стек-трейси, текст завдань, які вже задеплоєні і закриті (вони зберігаються в git і `docs/research/`).\n2. **Архівізація через файли:** Замість покладатись на пам'ять автостиснення, агент зобов'язаний перед завершенням важливого етапу записати підсумок у `docs/research/TASK-NN-SUMMARY.md`. У наступній сесії завантажується лише посилання на цей файл, а не вся історія дискусії.\n\n*Очікуваний ефект:* Відсутність галюцинацій після стиснення, агент не перечитує старий код. Вимірюється зменшенням кількості запитань агента «Нагадайте, яка в нас структура…» на початку сесії.\n\n---\n\n5. ПИСЬМОВІ ЗАВДАННЯ ДЛЯ РОБІТНИКА\n\n1. **Шаблон завдання (Contract-First):** Завдання для Codex має складатися з чотирьох блоків: Контекст (що саме змінити), Інваріанти (чого НЕ можна чіпати), Шляхи до файлів, і **Приймальні тести** (які вже лежать у `tests/` і є незмінними).\n2. **Дешева перевірка на суперечності:** Перед тим як давати завдання Codex, пропустіть текст завдання (`TASK-NN.md`) та опис архітектури через локальну модель (наприклад, `qwen2.5:7b` через Ollama) з промптом: *«Знайди логічні суперечності між цим завданням і наявними обмеженнями»*. Це коштує 0 грошей і займає 5 секунд.\n\n*Очікуваний ефект:* Зменшення кількості невдалих спроб Codex з 3 до 1-2. Вимірюється лічильником спроб у запуску (`attempts_count`).\n\n---\n\n6. ПЕРЕХРЕСНА ПЕРЕВІРКА МОДЕЛЕЙ\n\nСенсу порівнювати «дві думки» для простих задач немає. Перехресну перевірку варто застосовувати **тільки для архітектурних рішень та складних SQL-запитів до Postgres** (наприклад, оптимізація вибірки 10k товарів за 4 майданчиками).\n\nЯк оформлювати:\n- Дати ту саму задачу Claude Opus і Codex (або Gemini Pro) **без контексту рішень один одного**.\n- Запит: *«Запропонуй дизайн модуля синхронізації залишків для Prom та Rozetka. Надай лише кроки і можливі ризики зациклення»*.\n- **Звірка:** Claude Opus (як головний менеджер) отримує обидва варіанти і дається промпт: *«Порівняй варіант А і варіант Б. Знайди слабке місце в кожному з погляду надійності бази даних»*.\n\n*Очікуваний ефект:* Виявлення архітектурних сліпих зон однієї з моделей. Вимірюється зменшенням кількості факапів при роботі з реальним складом і постачальниками.\n\n---\n\n7. ЩО ВИМІРЮВАТИ\n\nТри показники для щотижневого аудиту:\n1. **Вартість одного успішного модуля (Token Cost per Module):** Загальна сума використаних токенів (у доларах за API або еквівалент лімітів Claude) / кількість прийнятих модулів за тиждень. Рахується за логами використання API / CLI.\n2. **Середня кількість спроб Codex на одне завдання (Average Codex Attempts):** Сума спроб по всіх завданнях / кількість завдань. Ціль — тримати біля 1.1–1.3. Рахується звітними скриптами запускача.\n3. **Частка код-рев'ю без правок (First-Try Acceptance Rate):** Відсоток завдань, які Claude прийняв з першого разу після роботи Codex, без ручних виправлень влаником. Рахується за коммітами у гілці (`git log`).\n\n---\n\n8. ЩО В НАШІЙ АРХІТЕКТУРІ ВИ ВВАЖАЄТЕ ПОМИЛКОЮ\n\n*Припущення щодо вашої системи:* У вас 1 людина керує процесом, і головна проблема — не написати код, а утримати контекст і не витратити весь час на менеджмент агентів.\n\n1. **Зберігання всіх 95 чужих скілів на диску з вивантаженням індексу (15 КБ) у кожну сесію.**\n   *Чим обійдеться:* Коли система виросте і скілів стане 500, індекс розростеться, з'їдаючи корисний контекст Opus. Агент почне «галюцинувати» і підключати не ті інструкції.\n   *Виправлення:* Прибрати індекс з автозавантаження. Скіли мають підтягуватись динамічно лише тоді, коли Claude явно викликає інструмент пошуку за ключовим словом.\n\n2. **Ручне прийняття рішень «кому віддати задачу» інтуїтивно.**\n   *Чим обійдеться:* Людина втомлюється. Наприкінці дня ви віддасте рутинну задачу Opus (бо лінь думати) або складну задачу Gemini (отримавши непрацюючий код і втративши час на перевірки).\n   *Виправлення:* Записати дерево рішень (пункт 1) у вигляді короткого чеклисту прямо в стартовий системний промпт Claude Code або зашити в скрипт-диспетчер завдань.\n\n3. **Захист лише тестів від змін Codex (тести зафіксовані, але сам Codex працює у вільній пісочниці).**\n   *Чим обійдеться:* Коли база виросте до 50k товарів, а модулів стане 100, Codex у пошуках вирішення задачі почне ламати сумісність у суміжних модулях, які не покриті поточними тестами.\n   *Виправлення:* Додати перевірку регресії (запуск повного пакета старих тестів перед прийняттям будь-якого нового модуля).", 'gemini-flash-lite-latest')