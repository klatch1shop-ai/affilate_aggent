# Живий прогін нових модулів на справжніх даних — 20.09.2026

Правило (пастка 10 скіла `codex-worker`): зелені тести не доводять, що я правильно зрозумів
дані. Після приймання TASK-27 і TASK-28 прогнав `research/funnel.py` і `research/veto.py`
на робочій базі Postgres. **Лише читання**, нічого не змінено.

## Що працює як задумано
- `veto.check` на справжньому товарі `ТР0607ТОНК` (ціна постачальника 9.00 ₴):
  спроба поставити нижче порогу → `stop` із поясненням; ціна з націнкою → `ok`.
- `funnel.report` чесно перелічив, чого ми не знаємо, замість того щоб показати нулі
  як факти.

## Що знайшов у даних (це важливіше за самі модулі)

| Факт | Джерело |
|---|---|
| `competitor_prices` — **5 рядків**, усі з `sku = NULL`, останній від 28.06.2026 | запит `select count(*), count(sku) from competitor_prices` |
| `market_prices` — 5875 SKU, але **останній збір 11.05.2026** (4 місяці тому) | `select max(analyzed_at) from market_prices` |
| `market_prices.our_price` розійшовся з `my_products.price_our` (напр. `UF-0301`: 2220 проти 1130) | join двох таблиць |
| `orders` у нашій БД — **14 записів** (3 `confirmed`, 11 `check_needed`), це журнал агента, а не історія продажів | `select status, count(*) from orders` |

**Висновок для воронки:** у `docs/research/AUTONOMOUS_RESEARCH_LOOP.md` рядок
«ціни конкурентів 🟡 є інструмент, потрібен регулярний збір» треба читати як ❌:
інструмент є, але даних за останні 4 місяці немає, а ті, що є, не прив'язані до наших SKU.
Поки це так, крок «докази» в циклі гіпотез спиратиметься лише на наші власні дані.

**Не перевірено:** чи `market_prices` наповнює якийсь регулярний процес, який просто зупинився,
чи його ніколи не ставили в розклад. Це наступне питання до розбору `cron`/`systemd` на сервері.
