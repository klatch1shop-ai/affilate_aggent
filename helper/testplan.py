"""Інструкція для ручного тесту бота: що надіслати і що має прийти у відповідь."""

STEPS: list[tuple[str, str]] = [
    ('статус системи', 'перелік сервісів із ✅/❌ — це systemd на ноутбуку'),
    ('стан фідів', 'три фіди (rozetka, prom, epicentr) з віком у годинах'),
    ('нові замовлення', 'замовлення Rozetka у статусі «нові»; якщо порожньо — так і напише'),
    ('скасовані за тиждень', 'лише за 7 днів, а не всі — тут ловили помилку періоду'),
    ('наявність SO3142', 'назва, наявність, кількість, ціна з нашої бази'),
    ('баланс', 'гроші на рахунку Rozetka'),
    ('чати без відповіді', 'питання покупців, що чекають; телефони мають бути приховані'),
    ('бекап', 'дата, розмір і вік останньої копії Postgres (має бути < 26 год)'),
    ('модерація', 'товари, що не пройшли модерацію Rozetka'),
    ('що ти вмієш', 'повний перелік підключених команд по групах'),
    ('/воронка SO3142', 'усі факти по товару + чесний перелік, чого ми не знаємо'),
    ('/гіпотези', 'відкриті припущення й звірки, яким настав час (поки має бути порожньо)'),
    ('/самоперевірка', 'один блок з ✅/❌ по кожному новому модулю — його й скопіюйте'),
]

NOTES = (
    'Бот **нічого не пише** у майданчики: етап 1 — лише читання, відповіді покупцям у режимі чернетки.',
    'Під нагадуванням про чат покупця є кнопка «✅ Прочитав» — після неї чат мовчить добу '
    'або доки покупець не напише нове.',
    'Якщо команда відповідає «не зрозумів» — напишіть точну фразу зі списку: '
    'розбір поки за ключовими словами, Gemini як запасний ще не підключений.',
)


def test_text() -> str:
    """Пронумерована інструкція для ручної перевірки."""
    lines = ['🧪 Перевірка бота — надсилайте по черзі, відповіді копіюйте:', '']
    for i, (what, expect) in enumerate(STEPS, 1):
        lines.append(f'{i}. `{what}`')
        lines.append(f'   → {expect}')
    lines.append('')
    lines.extend('• ' + n for n in NOTES)
    return '\n'.join(lines)
